diff --git a/e2e/accessibility.spec.ts b/e2e/accessibility.spec.ts
index 4eb4d0b..c4b6b38 100644
--- a/e2e/accessibility.spec.ts
+++ b/e2e/accessibility.spec.ts
@@ -3,9 +3,9 @@ import { createAxeBuilder } from './support/axeBuilder';
 import { installRouteMocks } from './support/routeMocks';
 
 const ROUTES = [
-  { path: '/', heading: "The hierarchy view isn't built yet" },
-  { path: '/login', heading: "Sign in isn't built yet" },
-  { path: '/does-not-exist', heading: 'Page not found' },
+  { path: '/', heading: 'home.title' },
+  { path: '/login', heading: 'login.title' },
+  { path: '/does-not-exist', heading: 'notFound.title' },
 ];
 
 test.describe('accessibility', () => {
diff --git a/e2e/error-boundary.spec.ts b/e2e/error-boundary.spec.ts
index e68316a..5c37eec 100644
--- a/e2e/error-boundary.spec.ts
+++ b/e2e/error-boundary.spec.ts
@@ -26,7 +26,7 @@ test('a forced route chunk failure renders the boundary with a correlation id an
 
   const alert = page.getByRole('alert');
   await expect(alert).toBeVisible();
-  await expect(alert).toContainText('Something went wrong');
+  await expect(alert).toContainText('errorSurface.title');
 
   const correlationIdText = await alert.locator('p').last().textContent();
   expect(correlationIdText?.trim()).toMatch(CORRELATION_ID_PATTERN);
@@ -45,13 +45,13 @@ test('a forced route chunk failure renders the boundary with a correlation id an
   );
   expect(routeViewedBeforeRetry).toHaveLength(0);
 
-  const retryButton = page.getByRole('button', { name: 'Try again' });
+  const retryButton = page.getByRole('button', { name: 'errorSurface.retry' });
   await retryButton.focus();
   await expect(retryButton).toBeFocused();
   await page.keyboard.press('Enter');
 
   await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-    "Sign in isn't built yet",
+    'login.title',
   );
   await expect(alert).toBeHidden();
 
diff --git a/e2e/not-found.spec.ts b/e2e/not-found.spec.ts
index 72f7880..08597c2 100644
--- a/e2e/not-found.spec.ts
+++ b/e2e/not-found.spec.ts
@@ -12,15 +12,15 @@ test('an unknown path renders the not-found route with a working link home', asy
   await page.goto('/this-path-does-not-exist');
 
   await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-    'Page not found',
+    'notFound.title',
   );
 
-  const homeLink = page.getByRole('link', { name: 'Back to home' });
+  const homeLink = page.getByRole('link', { name: 'notFound.linkHome' });
   await expect(homeLink).toBeVisible();
   await homeLink.click();
 
   await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-    "The hierarchy view isn't built yet",
+    'home.title',
   );
   expect(records).toEqual([]);
 });
diff --git a/e2e/placeholder-routes.spec.ts b/e2e/placeholder-routes.spec.ts
index 5fb4329..00c993a 100644
--- a/e2e/placeholder-routes.spec.ts
+++ b/e2e/placeholder-routes.spec.ts
@@ -13,15 +13,17 @@ test.describe('placeholder routes', () => {
     await page.goto('/');
 
     await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-      "The hierarchy view isn't built yet",
+      'home.title',
     );
-    await expect(page).toHaveTitle('Hierarchy');
+    await expect(page).toHaveTitle('home.documentTitle');
     await expect(page.getByRole('main')).toBeVisible();
     await expect(
-      page.getByRole('link', { name: 'Skip to main content' }),
+      page.getByRole('link', { name: 'layout.skipLink' }),
     ).toBeAttached();
 
-    expect(await page.evaluate(() => document.documentElement.lang)).toBe('en');
+    expect(await page.evaluate(() => document.documentElement.lang)).toBe(
+      'zxx',
+    );
     expect(await page.evaluate(() => document.documentElement.dir)).toBe('ltr');
 
     expect(records).toEqual([]);
@@ -37,9 +39,9 @@ test.describe('placeholder routes', () => {
     await page.goto('/login');
 
     await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-      "Sign in isn't built yet",
+      'login.title',
     );
-    await expect(page).toHaveTitle('Sign in');
+    await expect(page).toHaveTitle('login.documentTitle');
 
     expect(records).toEqual([]);
   });
@@ -61,7 +63,7 @@ test.describe('placeholder routes', () => {
     await page.keyboard.press('Tab');
 
     await expect(
-      page.getByRole('link', { name: 'Skip to main content' }),
+      page.getByRole('link', { name: 'layout.skipLink' }),
     ).toBeFocused();
   });
 });
diff --git a/e2e/right-to-left.spec.ts b/e2e/right-to-left.spec.ts
index ebcc74b..033bbf9 100644
--- a/e2e/right-to-left.spec.ts
+++ b/e2e/right-to-left.spec.ts
@@ -4,12 +4,18 @@ import { recordConsole } from './support/consoleRecorder';
 import { forceDirection } from './support/forceDirection';
 import { installRouteMocks } from './support/routeMocks';
 
-// English is the only shipped locale in this phase (invariant 65), so
-// there is no product path that switches direction - forceDirection.ts
-// overrides the browser-rendered `dir` directly to prove invariant 68's
-// claim (logical properties mirror correctly with no other code change)
-// independently of locale-driven derivation, which invariant 66 covers
-// separately at the unit level and in placeholder-routes/accessibility.
+// English is the only shipped PRODUCT locale (invariant 65) - a
+// non-product-facing `test` locale (Locale.Test, tag 'zxx') exists for
+// unit/e2e assertions only (see ARCHITECTURE.md's decision log), reachable
+// solely through this Playwright project's `locale: 'zxx'` context option,
+// never through any in-app control. There is still no PRODUCT path that
+// switches direction - forceDirection.ts overrides the browser-rendered
+// `dir` directly to prove invariant 68's claim (logical properties mirror
+// correctly with no other code change) independently of locale-driven
+// derivation, which invariant 66 covers separately at the unit level and
+// in placeholder-routes/accessibility. This spec's headings assert the
+// key-echoed catalogue strings the `zxx` locale produces, same as every
+// other spec under this project - unrelated to the RTL mechanism itself.
 //
 // TECH.md's plan for this invariant also names a "mirrored inline-start
 // indicator" assertion (an element whose computed padding-inline-start
@@ -26,8 +32,8 @@ import { installRouteMocks } from './support/routeMocks';
 // (invariant 68's structural precondition - every logical property
 // resolves relative to it), across every route this phase ships.
 const ROUTES = [
-  { path: '/', heading: "The hierarchy view isn't built yet" },
-  { path: '/login', heading: "Sign in isn't built yet" },
+  { path: '/', heading: 'home.title' },
+  { path: '/login', heading: 'login.title' },
 ];
 
 test.describe('right-to-left', () => {
diff --git a/e2e/telemetry-buffer.spec.ts b/e2e/telemetry-buffer.spec.ts
index c78758e..7366232 100644
--- a/e2e/telemetry-buffer.spec.ts
+++ b/e2e/telemetry-buffer.spec.ts
@@ -10,7 +10,7 @@ test('the buffer sink records exactly one route_viewed for the initial load and
 
   await page.goto('/');
   await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-    "The hierarchy view isn't built yet",
+    'home.title',
   );
 
   const records = await page.evaluate(
@@ -34,11 +34,11 @@ test('local and session storage are empty after visiting both routes', async ({
 
   await page.goto('/');
   await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-    "The hierarchy view isn't built yet",
+    'home.title',
   );
   await page.goto('/login');
   await expect(page.getByRole('heading', { level: 1 })).toHaveText(
-    "Sign in isn't built yet",
+    'login.title',
   );
 
   const storageLengths = await page.evaluate(() => ({
diff --git a/playwright.config.ts b/playwright.config.ts
index 5425adf..ca9e4df 100644
--- a/playwright.config.ts
+++ b/playwright.config.ts
@@ -27,7 +27,11 @@ const developmentServer = {
 // than one project list filtered per invocation.
 const chromiumProject = {
   name: 'chromium',
-  use: { ...devices['Desktop Chrome'], baseURL: 'http://127.0.0.1:4173/' },
+  use: {
+    ...devices['Desktop Chrome'],
+    baseURL: 'http://127.0.0.1:4173/',
+    locale: 'zxx',
+  },
   testDir: './e2e',
   testIgnore: [
     '**/development-console.spec.ts',
diff --git a/specs/ARCHITECTURE.md b/specs/ARCHITECTURE.md
index 92c4943..8ec027a 100644
--- a/specs/ARCHITECTURE.md
+++ b/specs/ARCHITECTURE.md
@@ -173,6 +173,7 @@ Each entry: the choice, why, and what was rejected.
 - **App entry budget revised to 150 kB gzipped** (2026-08-13, phase 1) - wiring the real router in M5 step 31 measured the entry at 112.77 kB, busting the 100 kB budget by 12.77 kB. An isolated esbuild probe (`createBrowserRouter` + `RouterProvider` alone, tree-shaken, react/react-dom externalized) put react-router's own weight at ~31 kB gzipped - the original 85-90 kB estimate undercounted react-router specifically, this was not a tree-shaking regression or a code defect. Per this section's own rule, a wrong estimate is revisited with the user rather than silently raised; the user set the corrected number at 150 kB, leaving headroom over the measured 112.77 kB rather than pinning the limit to the exact current build. Rejected: a code-splitting mitigation to defer router construction, which was not pursued because the router must exist to render the very first route - there is no idle window before it that deferral could exploit.
 - **Two design tokens darkened past their mockup literals: `border-field` and `ink-faint`** (2026-08-13, phase 1) - `docs/reference.md`'s pulled values (`#E2DDEE` field border, `#8E8AA0` faint ink) hold 1.33:1 and 3.34:1/4.31:1 (light/dark) against the surfaces they sit on, both short of invariant 72's AA floor (3:1 for UI boundaries, 4.5:1 for body text - the correlation id `ErrorState` renders in `ink-faint`, per invariant 92, is body text). `theme.css` re-declares both tokens (`border-field` `#8F89A3`, `ink-faint` `#726E85` light / `#847F95` dark) with `contrastPairs.test.ts` entries proving the new values clear AA in both themes. Rejected: keeping the mockup literals and accepting the AA failure, which invariant 70's mockup-fidelity goal does not outrank invariant 72's binding accessibility floor; and reusing an existing darker token (`ink-muted`) for the correlation id instead of darkening `ink-faint`, which would collapse two intentionally distinct text weights into one.
 - **`eslint-plugin-jsx-a11y`'s peer dependency is overridden to accept eslint 10** (2026-08-14, phase 1) - `npm ci` failed in CI (though not locally, since local `node_modules` had been installed incrementally and never re-validated from a clean state) with an ERESOLVE conflict: `eslint-plugin-jsx-a11y@6.10.2`'s `peerDependencies` cap `eslint` at `^9`, while this project pins `eslint@^10.8.0`. Confirmed against the npm registry that 6.10.2 is jsx-a11y's latest release and has no eslint-10-compatible version. `package.json` now carries a narrow `overrides` entry (`eslint-plugin-jsx-a11y.eslint -> $eslint`) that relaxes only this one peer edge, verified against a genuinely clean `rm -rf node_modules && npm ci` (both in an isolated worktree and on the real repo) followed by a full `npm run verify` and `npm run e2e` pass, including the `jsx-a11y` rules themselves still firing correctly under eslint 10. Rejected: downgrading `eslint` to `^9`, which would give up the newer major this project deliberately adopted for no defect of eslint's own; and `--legacy-peer-deps`, which would silence strict peer-conflict detection for every dependency in the tree, not just this one known-safe pairing.
+- **A key-echoing `test` locale ships in the same build that deploys to production** (2026-08-14, i18n-test-locale loop) - the entry above holds that a token second *product* locale would prove less than exercising the real one; this does not reopen that trade-off, because `test` is not a product locale at all. Unit and e2e assertions previously hardcoded a copy of the real English strings, which could drift silently whenever the catalogue's prose changed and the test's copy did not. `Locale.Test` (tag `'zxx'` - not `'test'` itself, which is not valid BCP-47 syntax and fails `Intl.getCanonicalLocales`) is a generated i18next resource bundle - one function walks each real English catalogue's key shape and replaces every leaf with its own dot-path, so `t('notFound.title')` resolves to the literal string `'notFound.title'` under it - which turns every such assertion into a structural check against the real catalogue instead of a hand-maintained copy of its prose. It is the default `buildTestRuntime` (`src/app/testing/renderRoute.tsx`) builds and the locale Playwright's `chromium` project requests via its context `locale` option; it is reachable only through those two paths, never through a real browser's language preference or an in-app control - `detectLocale` (`src/platform/internationalization/detectLocale.ts`) matches a visitor's `navigator.languages` against `Locale`'s members and falls back to `Locale.English` when nothing matches, which is what happens for every real visitor today, since `'zxx'` (ISO 639-2 for "no linguistic content; not applicable") does not correspond to a spoken language and no real browser reports it. Rejected: keeping hand-authored English assertions, the status quo this entry replaces and the drift risk that motivated the change; and leaning on i18next's own missing-key fallback to echo keys, which `vitest.setup.ts`'s empty-`missingKeyReports` assertion forecloses - a real fallback reports every key as missing rather than echoing it silently. The chosen tag went through two corrections during review before landing on `'zxx'`: `'test'` fails `Intl`'s BCP-47 grammar check outright, and its first replacement, `'pseudo'`, passes that grammar check but is not a registered IANA subtag and fails axe-core's registry-based `html-lang-valid` rule - verified directly by running real axe-core against both, not by syntax inspection alone.
 - **The deploy job's post-deploy assertion is dropped; it never verified anything a deployment could fail** (2026-08-14, phase 1) - invariant 123 originally had the deploy job compare Wrangler's `deployment-url` output against `deployment.json`'s recorded production hostname, on the theory that a misconfigured Pages project (invariant 126) would report a preview URL there instead. Reality, confirmed against the real project after merge: `wrangler pages deploy` always reports the deployment's own permanent per-deployment URL (`https://<hash>.hierarchy-tree.pages.dev`), never the production alias hostname, regardless of whether Cloudflare has correctly flagged that deployment `Production` internally - `wrangler pages deployment list` showed `Environment: Production, Branch: main` for the exact deployment the assertion rejected. The comparison was therefore unsatisfiable by construction; it would fail identically for a correctly configured project and a misconfigured one, so it verified nothing. The step now only records both URLs to the job summary for visibility. Invariant 126a's post-merge `e2e:deployed` run remains the actual verification that the production hostname serves the deployed build - it fetches that real URL directly, which is the one check in this pipeline that can actually distinguish success from failure here. Rejected: querying the Cloudflare API for the deployment's `environment` field, or fetching the production hostname and diffing it against the build's own asset hashes, both of which would restore an early, wrangler-output-only signal - not pursued because `e2e:deployed` already performs the more direct version of the second option immediately afterward, and a second implementation of the same check was judged not worth the added API surface for a signal the next step gives for free.
 
 ## 7. Deliberately not built
@@ -184,5 +185,5 @@ Named so the omissions read as decisions, with the seam that makes each cheap la
 - **Offline / service worker** - a 9KB public payload does not justify a cache lifecycle; the repository layer is where a persistent cache would attach.
 - **Response caching** - dedupe, TTL and stale-while-revalidate, withdrawn from the roadmap by the decision log entry above; the design is preserved in [CACHE.md](./CACHE.md). The repository per resource is the seam: caching attaches behind it without a caller changing.
 - **Search, filtering, org editing** - outside GOAL.md. The tree domain is pure and already returns the row model a filter would narrow.
-- **A second locale, SSR, a component library** - infrastructure supports each; none is needed to satisfy the goal.
+- **A second *product* locale, SSR, a component library** - infrastructure supports each; none is needed to satisfy the goal. (A test-only locale for unit/e2e assertions ships separately - see the decision log.)
 - **Vendor telemetry** - the facade defines the contract; a real sink is a swap, not a rewrite.
diff --git a/specs/ROADMAP.md b/specs/ROADMAP.md
index e3887f8..8cc40f9 100644
--- a/specs/ROADMAP.md
+++ b/specs/ROADMAP.md
@@ -112,7 +112,7 @@ Carried from `ARCHITECTURE.md` section 7 so that reading only this file does not
 - **Offline and service workers** - a 9KB public payload does not justify a cache lifecycle. The repository layer is where a persistent cache would attach.
 - **Response caching** - in-flight dedupe, TTL and stale-while-revalidate. Withdrawn from the roadmap on 2026-08-13: the design cost more than the one 9KB request it saves. Every phase fetches through the http client directly, and the repository per resource is the seam a cache attaches behind later. The withdrawn design is kept in [CACHE.md](./CACHE.md) for whenever that is worth doing.
 - **Search, filtering, org editing** - the tree domain already returns the row model a filter would narrow, but none of this is asked for.
-- **A second locale, SSR, a component library** - the infrastructure supports each; none is needed.
+- **A second *product* locale, SSR, a component library** - the infrastructure supports each; none is needed. (A test-only locale for unit/e2e assertions ships separately - see ARCHITECTURE.md's decision log.)
 - **Vendor telemetry** - the observability facade defines the contract; a real sink is a one-line swap.
 
 ---
diff --git a/specs/i18n-test-locale/PLAN.md b/specs/i18n-test-locale/PLAN.md
new file mode 100644
index 0000000..98fb842
--- /dev/null
+++ b/specs/i18n-test-locale/PLAN.md
@@ -0,0 +1,61 @@
+# Plan: i18n-test-locale - Key-echoing test locale, locale enum, browser-locale detection
+
+## Brief
+
+Add a `Locale` const-object enum (English/Test), a generated key-echoing `test` catalogue (tag `'zxx'`,
+non-mutating, derived from the real English catalogues so it never drifts), a `detectLocale` helper wired
+for real into `createRuntime.ts`, and make the test locale the default for `buildTestRuntime` (unit tests)
+and Playwright's `chromium` project (e2e). See `SPEC.md` for the full 20-invariant behavior spec and the
+G1 review history (7 findings across 2 rounds, all resolved - notably the locale tag went through two
+corrections: `'test'` -> `'pseudo'` -> `'zxx'`, the last verified via both `Intl` and a real executed
+axe-core run).
+
+## Steps
+
+Each step's tests trace to SPEC.md invariants by number. Full list per step below; `loop step add` calls
+follow.
+
+1. **`Locale` const-object enum** (`src/platform/internationalization/locale.ts`) - invariant 1.
+   Tests: `locale.test.ts::Locale > exposes English and Test members with the correct string values`
+2. **`detectLocale` helper** (`src/platform/internationalization/detectLocale.ts`) - invariants 2, 3, 4, 5.
+   Tests: `detectLocale.test.ts::detectLocale > matches a region-qualified candidate to its base subtag`,
+   `detectLocale.test.ts::detectLocale > falls back to English when no candidate matches`,
+   `detectLocale.test.ts::detectLocale > respects preference order over match order`
+3. **`createKeyEchoCatalogue` generator** (`src/platform/internationalization/createKeyEchoCatalogue.ts`) -
+   invariants 6, 7. Tests:
+   `createKeyEchoCatalogue.test.ts::createKeyEchoCatalogue > replaces every leaf with its own dot-path key`,
+   `createKeyEchoCatalogue.test.ts::createKeyEchoCatalogue > never mutates the source catalogue`
+4. **Wire `detectLocale` into `createRuntime.ts`** - invariant 8.
+   Tests: `createRuntime.test.ts::createRuntime > resolves the runtime language from navigator.languages`
+5. **Fix `loadTranslations.ts` (both features): register under the active language, echo under
+   `Locale.Test`** - invariants 9, 10. Tests:
+   `loadTranslations.test.ts::loadTranslations > registers the key-echoed catalogue under Locale.Test`
+   (auth), `loadTranslations.test.ts::loadTranslations > registers the key-echoed catalogue under
+   Locale.Test` (hierarchy)
+6. **`buildTestRuntime` defaults to `Locale.Test`; update every dependent unit test assertion** -
+   invariants 11, 12, 13, 14. Tests (updated in place, key-echo assertions are this step's red-first
+   tests against the current English-defaulted `buildTestRuntime`):
+   `ErrorSurface.test.tsx::ErrorSurface > renders the localized error copy`,
+   `RootErrorBoundary.test.tsx::RootErrorBoundary > renders the error surface on a caught error`,
+   `RouteErrorBoundary.test.tsx::RouteErrorBoundary > renders the error surface on a route error`,
+   `ApplicationLayout.test.tsx::ApplicationLayout > renders the skip link`,
+   `NotFoundRoute.test.tsx::NotFoundRoute > renders the not-found copy`,
+   `ApplicationRoot.test.tsx::ApplicationRoot > sets the document language and direction`
+7. **Playwright `locale: 'zxx'` on the `chromium` project; update the six affected e2e specs and the
+   `right-to-left.spec.ts` comment** - invariants 17, 18. e2e, run via `npm run e2e -- --project=chromium`
+   rather than a registered unit-test name (Playwright specs aren't named the same way; red/na recorded
+   against the real e2e run per `loops/gates.md`'s e2e-evidence convention).
+
+Invariants 15, 16, 19, 20 are verification-only (no code change needed for 15/16; 19/20 are proven at
+Verify by running the real suite/build, not by a dedicated step) - covered in Verification below, not as
+separate steps.
+
+## Verification
+
+`npm run verify` (typecheck + lint + format:check + test:coverage + build + verify:build + size) plus
+`npm run e2e -- --project=chromium` for the six updated specs and `right-to-left.spec.ts`'s axe-clean
+assertion (proves invariant 1's `lang="zxx"` choice holds under a real browser, not just `Intl`/axe-core
+run directly). Explicit checks: `bootstrap.test.tsx` and both `loadTranslations.test.ts` files' *existing*
+English cases stay green unmodified (invariants 15, 16); `npm run size` output inspected against
+`.size-limit.json` for any new/unbudgeted chunk (invariant 20); the dependency allow-list test passes
+unmodified (invariant 19). Evidence captured under `specs/i18n-test-locale/evidence/`.
diff --git a/specs/i18n-test-locale/SPEC.md b/specs/i18n-test-locale/SPEC.md
new file mode 100644
index 0000000..5b14f4e
--- /dev/null
+++ b/specs/i18n-test-locale/SPEC.md
@@ -0,0 +1,195 @@
+# Spec: i18n-test-locale - A key-echoing test locale for unit and e2e tests
+
+## Summary
+
+Add a key-echoing i18next locale (`Locale.Test`, underlying tag `'zxx'`) whose translation function returns each key's own dot-path as its value, generated from the real English catalogues rather than hand-authored - then make it the default locale everywhere tests build a runtime, so test assertions check catalogue *structure* instead of a hand-copied, driftable English string. Alongside it: a `Locale` const-object enum naming the two supported locales, and a pure `detectLocale` helper that resolves a real visitor's `navigator.languages` to a supported locale (always `Locale.English` today, since no real browser reports `'zxx'`).
+
+**G1 revision note:** this spec was revised after G1 review, in two rounds. Round 1 changed the locale's underlying string value from the originally proposed `'test'` to `'pseudo'` - `'test'` is not syntactically valid BCP-47 (`Intl.getCanonicalLocales('test')` throws `RangeError`, confirmed by direct execution). Round 2 (a Codex fix-confirmation pass) found `'pseudo'` itself, while syntactically valid, is NOT a registered IANA language subtag, and fails axe-core's `html-lang-valid` rule for exactly that reason (verified by running real axe-core 4.13.0 against `<html lang="pseudo">` directly - a genuine violation, not a guess) - so the value changed again, to `'zxx'`. `'zxx'` is a real, IANA-registered ISO 639-2 code (meaning "No linguistic content; Not applicable" - an apt, if coincidental, fit) that passes both `Intl.getCanonicalLocales`/`Intl.DateTimeFormat` AND a real axe-core `html-lang-valid` run, verified directly rather than assumed. It is single-segment, so it needs no change to the subtag-splitting detection design. See `evidence/reviews/g1-claude-validator.md` and `evidence/reviews/g1-codex-review.md` for the full findings this revision addresses.
+
+## Behavior (numbered invariants)
+
+1. `Locale` is exported as an `as const` object from `src/platform/internationalization/locale.ts` (re-exported from the barrel) with exactly two members: `English: 'en'` and `Test: 'zxx'`. Its derived type is the union of those two string literals - never a TypeScript `enum`, never a bare literal union declared by hand. `'zxx'` is a real, IANA-registered ISO 639-2 subtag meaning "no linguistic content; not applicable" (verified: `Intl.getCanonicalLocales('zxx')` and `new Intl.DateTimeFormat('zxx')` both succeed with no exception, AND a real axe-core 4.13.0 run against `<html lang="zxx">` reports zero `html-lang-valid` violations) - unlike a merely syntax-valid-but-unregistered tag, which passes `Intl`'s grammar check but fails axe's registry-based one. No real visitor's browser reports `'zxx'` as a language preference, since it does not correspond to a spoken language.
+2. `detectLocale(candidates)` returns the value of whichever `Locale` member matches the base subtag (the segment before the first `-`, lower-cased) of the *first* candidate in `candidates` whose base subtag is supported. Region/script subtags are ignored: `'en-US'`, `'en-GB'`, and `'EN'` all resolve to `Locale.English`.
+3. `detectLocale(candidates)` returns `Locale.English` when `candidates` is empty, when no candidate's base subtag matches any `Locale` member, or when a candidate is malformed (empty string, no subtag before `-`).
+4. `detectLocale` respects preference order: given `['fr-FR', 'zxx', 'en']`, it returns `Locale.Test`, because `'zxx'` is the first candidate (in list order) whose base subtag is supported - not `Locale.English`, even though `'en'` appears later and would also match.
+5. `detectLocale` is a plain, synchronous, framework-free function - no React import, no DOM read beyond its argument - mirroring `localeDirection.ts`'s shape. It is not a hook; nothing in this feature adds a component-level caller.
+6. A generated test catalogue exists for each of the three real English catalogues (`src/app/locales/en/common.json`, `src/features/auth/locales/en/auth.json`, `src/features/hierarchy/locales/en/hierarchy.json`): every leaf string is replaced by its own dot-path key (e.g. `{ notFound: { title: "Page not found" } }` becomes `{ notFound: { title: "notFound.title" } }`), produced by one generic, catalogue-shape-agnostic function applied to each catalogue - never by a hand-authored JSON file that can drift from the English source. **The generator never mutates its input.** It builds and returns a new object tree; the source catalogue module (a cached JSON import, shared across every caller in the process) is byte-for-byte unchanged after generation, including after a `Locale.Test` registration is followed by a `Locale.English` registration in the same process - an in-place walker would corrupt the shared cached import and silently break invariant 10.
+7. The generated test catalogue is a fully populated, registered i18next resource bundle for every key that exists in the corresponding English catalogue. Nothing relies on i18next's own missing-key fallback to produce the echo. Consequently, rendering any string under `Locale.Test` never triggers `missingKeyHandler`, and `vitest.setup.ts`'s `expect(missingKeyReports).toEqual([])` (its `afterEach`) keeps passing for every test that renders under the test locale.
+8. `src/app/composition/createRuntime.ts` derives the runtime's i18next language at boot by calling `detectLocale(navigator.languages)`, replacing the hardcoded `RUNTIME_LANGUAGE = 'en'` module constant. A real visitor's browser - which never reports `'zxx'` as a language preference - still resolves to `Locale.English` today, so this is a live code path with no observable change for real users, not a behavior change pending on a second product locale. **This exact wiring is directly tested**, not inferred from `detectLocale`'s isolated unit coverage: `src/app/composition/createRuntime.test.ts` (which already exists, 4 tests today) gains a case that stubs `navigator.languages` and asserts the resulting runtime's `i18n.language` - proving `createRuntime.ts` actually calls `detectLocale` with the right argument and actually threads its result into `language`, not merely that `detectLocale` itself is correct in isolation. **Implementation correction (found during step 7's real e2e run, not caught by either G1 round or step 4's own unit tests):** `createRuntime.ts` registers the `common` namespace directly (it is not routed through `loadTranslations.ts`, which only covers the `auth`/`hierarchy` namespaces), so wiring `detectLocale` into `language` alone left `common` un-echoed - the real app under `Locale.Test` still rendered raw English `layout`/`notFound`/`errorSurface` copy, exactly the invariant-10 gap `loadTranslations.ts` already closes for the other two namespaces. `createRuntime.ts` now conditionally echoes `common` via `createKeyEchoCatalogue` the same way, proven by a second new `createRuntime.test.ts` case (asserting `i18n.t('notFound.title', { ns: 'common' })` resolves to the key) AND by the real e2e `chromium` project run (12/12 passing, including both `accessibility.spec.ts` cases whose axe-core pass is the actual empirical proof `lang="zxx"` is valid - closing the loop on G1/Codex-1 with a real browser, not just `Intl`/a standalone `axe-core` probe).
+9. `src/features/auth/loadTranslations.ts` and `src/features/hierarchy/loadTranslations.ts` register their namespace's resource bundle under the i18next instance's actual active language (`instance.language`), never the literal `'en'`. A runtime whose active language is anything other than `'en'` now receives its feature namespace, closing the latent bug where it silently received none.
+10. When an i18next instance's active language is `Locale.Test`, `loadTranslations.ts` (both features) registers the key-echoed version of its own catalogue (the same generator as invariant 6). When the active language is anything else (`Locale.English` today, or an explicit `'en'` an instance was constructed with), it registers the real, unmodified English catalogue - so English-locale behavior is unaffected by this feature by construction, not by coincidence.
+11. `src/app/testing/renderRoute.tsx`'s `buildTestRuntime` defaults to `Locale.Test` and builds its `common`-namespace resource via the generator (invariant 6), rather than passing the literal English `common.json` straight through.
+12. Any unit test rendering through `renderRoute`/`buildTestRuntime` (the default path, no override) observes key-echoed strings for the `common` namespace immediately, and - once a route's `lazy()` has awaited that feature's `loadTranslations` - key-echoed strings for `auth`/`hierarchy` too, per invariants 9-10.
+13. `src/app/ApplicationRoot.tsx`'s existing `lang`/`dir` derivation (`document.documentElement.lang = language; document.documentElement.dir = localeDirection(language)`, sourced from `runtime.i18n.language`) needs no code change: under `Locale.Test`, `lang` becomes `'zxx'` and `dir` stays `'ltr'` automatically, because `'zxx'` is not in `localeDirection.ts`'s right-to-left subtag set. `lang="zxx"` does not trip axe-core's `html-lang-valid` rule - verified by running real axe-core 4.13.0 against it directly (see invariant 1's verification note); `'zxx'` is a genuinely IANA-registered ISO 639-2 subtag, which is what axe's registry-based check actually requires (a merely syntax-valid-but-unregistered tag, e.g. the originally-considered `'pseudo'`, fails this specific check even though it passes `Intl`'s grammar-only validation).
+14. Existing unit tests that asserted hardcoded English catalogue copy now assert the corresponding key-echo string instead, in: `src/app/error-boundary/ErrorSurface.test.tsx`, `src/app/error-boundary/RootErrorBoundary.test.tsx`, `src/app/error-boundary/RouteErrorBoundary.test.tsx`, `src/app/layout/ApplicationLayout.test.tsx`, `src/app/routing/routes/NotFoundRoute.test.tsx`, and `src/app/ApplicationRoot.test.tsx`'s `document.documentElement.lang` assertion (`'en'` becomes `'zxx'`). The last file was not in the original candidate list and was found by reading the current suite (see Tech approach).
+15. `src/app/bootstrap.test.tsx` needs no change: it calls the real `bootstrap()`, which calls the real `createRuntime()` (not `buildTestRuntime`), and jsdom's default `navigator.languages` (`['en-US']`) resolves through `detectLocale` to `Locale.English` - so its existing English-copy assertions remain correct exactly as written.
+16. `src/features/auth/loadTranslations.test.ts` and `src/features/hierarchy/loadTranslations.test.ts`: their existing case, which builds an i18next instance with an explicit `language: 'en'` via `createInternationalization` directly and asserts `hasResourceBundle('en', ...)`, needs no change - per invariant 10, an instance explicitly constructed with `language: 'en'` still registers the real, unmodified English catalogue. Each file additionally gains a NEW case building an instance with `language: Locale.Test` and asserting the registered bundle's values are key-echoed (per invariants 9-10). Both files change (a new test is added); neither file's *existing* case changes.
+17. Playwright's `chromium` project (`playwright.config.ts`) gains a `locale: 'zxx'` context option. Every e2e spec that runs under that project and previously asserted hardcoded English copy now asserts the key-echo string instead: `e2e/accessibility.spec.ts`, `e2e/error-boundary.spec.ts`, `e2e/not-found.spec.ts`, `e2e/placeholder-routes.spec.ts`, `e2e/right-to-left.spec.ts`, `e2e/telemetry-buffer.spec.ts`. `placeholder-routes.spec.ts`'s `document.documentElement.lang` assertion changes from `'en'` to `'zxx'` accordingly (mirrors invariant 8/13 end to end in a real browser). Confirmed (G1): these six files are exactly the set of `e2e/*.spec.ts` files matched by the `chromium` project and excluded from `development`/`deployed` - no spec runs under more than one project, so this change cannot break an unlisted project.
+18. `e2e/right-to-left.spec.ts`'s header comment - currently "English is the only shipped locale in this phase (invariant 65), so there is no product path that switches direction" - is updated to state that a second, non-product-facing `test` locale (`Locale.Test`, tag `'zxx'`) now exists, and that this spec's `forceDirection` mechanism (which overrides `dir` directly) remains independent of and unaffected by it. The RTL-mirroring assertions and mechanism themselves do not change.
+19. No production runtime dependency is added, removed, or changed; the allow-listed dependency set (PRODUCT.md invariant 134) is untouched - `Locale`, `detectLocale`, and the catalogue generator are first-party, dependency-free modules.
+20. `npm run size` is run after implementation and its real output recorded as evidence (PRODUCT.md invariant 112's budgets must still hold). This invariant does NOT claim which specific chunk the catalogue generator lands in - a module statically imported from two independent lazy route graphs (`features/auth`, `features/hierarchy`) may be emitted by Rollup as its own shared chunk rather than duplicated into each route's chunk, and `.size-limit.json`'s current budget entries do not name every possible chunk file. If `npm run size`/`verify:build` surfaces a new chunk not covered by an existing budget entry, or an existing budget is exceeded, that is a Verify-phase (G3) finding to resolve then (add a budget entry, or adjust the design if the chunk is unexpectedly large) - not a claim this spec can make in advance of a real build.
+
+## Non-goals
+
+- No locale-switcher UI or in-app locale-selection control is added. `Locale.Test` is reachable only through `buildTestRuntime`'s default and Playwright's `locale: 'zxx'` context option - never through a component, and never through a real browser's `navigator.languages` (R11).
+- `e2e/deployed-smoke.spec.ts` and its Playwright `deployed` project are not touched. They exercise the real, deployed Cloudflare Pages URL and must keep asserting real English copy, because that is what a real visitor actually sees (R9).
+- The Playwright `development` project (`e2e/kit-route.spec.ts`, `e2e/development-console.spec.ts`) does not gain a `locale: 'zxx'` option. Neither spec asserts locale-sensitive copy today (verified by reading both files), so there is nothing for the test locale to change there.
+- `src/app/ConfigurationErrorScreen.tsx` is not touched. It renders before `createRuntime`/i18next exist by construction (configuration is invalid), reads the static English `common.json` directly as plain object properties, and is never reached through `renderRoute`/`buildTestRuntime`. It stays hardcoded English regardless of locale, exactly as before this feature - a separate, already-accepted design constraint, not a gap this loop introduces.
+- `specs/phase-1-setup/TECH.md`'s cross-references to invariant 65 (e.g. line 526's "Locale plumbing (invariants 65, 66, 68)") are not edited. That document is the closed, historical record of the already-merged phase-1-setup loop.
+- **G1 revision (Claude-1):** `ARCHITECTURE.md` §7 ("Deliberately not built"), `ROADMAP.md`'s carried copy of that same line, and `PRODUCT.md`'s separate "what's not built" list all currently state "a second locale... none is needed" with no product/test distinction - all three are edited by this spec (not left standing), alongside the decision log and invariant 65. See Tech approach's Decision-log text section for the exact wording of all five edits.
+
+## Tech approach
+
+### New modules (`src/platform/internationalization/`)
+
+- **`locale.ts`** - new file, following this repo's existing const-object-enum pattern exactly (compare `src/shared/ui/skeletonSize.ts`):
+  ```ts
+  export const Locale = {
+    English: 'en',
+    Test: 'zxx',
+  } as const;
+
+  export type Locale = (typeof Locale)[keyof typeof Locale];
+  ```
+  (`'zxx'` replaces the originally proposed `'test'` - see the Summary's G1 revision note.)
+- **`detectLocale.ts`** - new file, mirroring `localeDirection.ts`'s exact shape (`src/platform/internationalization/localeDirection.ts:1-16`, subtag-splitting via `.split('-')[0]?.toLowerCase()`). Iterates `candidates` in order; for each, checks whether its base subtag matches a `Locale` member via `Object.values(Locale).find(...)` (no `as`, no `any` - `.find` returning `Locale | undefined` is the type-safe path this repo's conventions require over a cast). Falls back to `Locale.English`.
+- **`createKeyEchoCatalogue.ts`** - new file, the R2 generator. A generic recursive walker: `createKeyEchoCatalogue<Catalogue extends Record<string, unknown>>(catalogue: Catalogue): Catalogue`. Builds and returns a NEW object tree - never assigns into or otherwise mutates `catalogue` or any of its nested objects (G1/Codex-2: the source is a cached JSON module import shared across every caller in the process; mutating it would corrupt every other consumer, including a later English registration in the same run). For each entry, if the value is a `string`, the corresponding key in the new object is set to `[...path, key].join('.')`; if it is a nested plain object (checked with a local type guard, not a cast), the walk recurses into a new nested object; the return shape is structurally identical to the input; only leaf string *values* change, and the input is left exactly as it was. This is the ONE function R2 asks for - called separately against each of the three catalogues at each of their two call sites (below), not a combined "build all three test resources" function. **Implementation note (G1/Claude-3, non-blocking):** JSON-imported catalogue leaf strings may type as string literals rather than widened `string` under this project's `resolveJsonModule`-style import handling; before committing to the `Catalogue -> Catalogue` generic signature above, the implementer should confirm it type-checks against a real catalogue import without a cast (`as`/`any` are forbidden by this repo's conventions) - if it does not, the return type narrows to `Record<string, unknown>` (or a recursive `JsonObject`-style type) instead of the exact input type, which is an equally acceptable, still-cast-free signature.
+
+Barrel `src/platform/internationalization/index.ts` (currently 6 lines, re-exports only) gains three lines: `export { Locale } from './locale'`, `export { detectLocale } from './detectLocale'`, `export { createKeyEchoCatalogue } from './createKeyEchoCatalogue'`.
+
+**Why this layer, not `shared/testing`:** the generator is called from `src/features/auth/loadTranslations.ts` and `src/features/hierarchy/loadTranslations.ts`, both production modules bundled into real per-route chunks (not test-only files). `eslint.config.js`'s boundaries policy (`eslint.config.js:338-355`, the base non-test rule) technically permits a `feature` file to import `shared` even in production, so this would not fail lint - but `shared/testing` (`src/shared/testing/index.ts`) is otherwise exclusively fakes and test doubles (`createFakeClock`, `createFakeRandomness`, `createFakeTransport`) meant for test-only callers like `renderRoute.tsx`. Shipping one of its exports into a real route's production bundle would quietly break that folder's established meaning even where lint stays green. `platform/internationalization` is where the repo already puts small, pure, dependency-free i18n transforms with no test-only connotation (`localeDirection.ts`, `formatMissingKey.ts`), and every layer that needs the generator (`app`, `feature`) can already import `platform` in production code. This keeps the diff inside the pattern the repo already has rather than introducing a new cross-layer wrinkle.
+
+### Modified modules
+
+- **`src/app/composition/createRuntime.ts`** - delete line 26 (`const RUNTIME_LANGUAGE = 'en';`); import `detectLocale` from `@platform/internationalization`; change line 58 from `language: RUNTIME_LANGUAGE,` to `language: detectLocale(navigator.languages),`.
+- **`src/features/auth/loadTranslations.ts`** and **`src/features/hierarchy/loadTranslations.ts`** - both currently at lines 20-23:
+  ```ts
+  async function registerCatalogue(instance: i18n): Promise<void> {
+    const catalogue = await import('./locales/en/auth.json');
+    instance.addResourceBundle('en', 'auth', catalogue.default);
+  }
+  ```
+  becomes:
+  ```ts
+  async function registerCatalogue(instance: i18n): Promise<void> {
+    const catalogue = await import('./locales/en/auth.json');
+    const resource =
+      instance.language === Locale.Test
+        ? createKeyEchoCatalogue(catalogue.default)
+        : catalogue.default;
+    instance.addResourceBundle(instance.language, 'auth', resource);
+  }
+  ```
+  (namespace name `'hierarchy'` and import path swapped in the hierarchy file). This single change satisfies R5 (register under the active language) and R2/R6's "transitively auth/hierarchy" claim together - R5's literal wording (fix the language key) is necessary but not sufficient on its own for the transitively-echoed claim, since the import path (`./locales/en/auth.json`) always loads the real English catalogue; the conditional echo is what makes the *content*, not just the *registration key*, correct under the test locale. Both facts are captured in this one code change, described together here rather than left implicit.
+- **`src/app/testing/renderRoute.tsx`** - `buildTestRuntime` (lines 31-58) changes lines 54-58 from:
+  ```ts
+  const i18n = await createInternationalization({
+    resources: { common: commonCatalogue },
+    language: 'en',
+    observability,
+  });
+  ```
+  to:
+  ```ts
+  const i18n = await createInternationalization({
+    resources: { common: createKeyEchoCatalogue(commonCatalogue) },
+    language: Locale.Test,
+    observability,
+  });
+  ```
+  `commonCatalogue` (imported at line 15, unchanged path `../locales/en/common.json`) stays the single source of truth; only the resource passed to `createInternationalization` changes. No override parameter is added - every current `buildTestRuntime(...)` call site (`features.test.tsx:39`, `RootErrorBoundary.test.tsx:18`, `createApplicationRouter.test.ts:7`) needs no locale other than the default, so an unused override parameter is not introduced (see Open questions).
+- **`playwright.config.ts`** - `chromiumProject` (lines 28-37) gains `locale: 'zxx'` inside its `use` block: `use: { ...devices['Desktop Chrome'], baseURL: 'http://127.0.0.1:4173/', locale: 'zxx' }`. `developmentProject` and `deployedProject` are untouched.
+- **e2e specs** (`e2e/accessibility.spec.ts`, `e2e/error-boundary.spec.ts`, `e2e/not-found.spec.ts`, `e2e/placeholder-routes.spec.ts`, `e2e/right-to-left.spec.ts`, `e2e/telemetry-buffer.spec.ts`) - each hardcoded English string literal (e.g. `"The hierarchy view isn't built yet"`, `'Page not found'`, `'Back to home'`, `'Something went wrong'`, `'Try again'`, `'Skip to main content'`, `'Sign in isn't built yet'`) is replaced with its dot-path key-echo equivalent (`'home.title'`, `'notFound.title'`, `'notFound.linkHome'`, `'errorSurface.title'`, `'errorSurface.retry'`, `'layout.skipLink'`, `'login.title'`), read off the real catalogue key each component's `t(...)` call already names (verified per-component: `HierarchyPlaceholderPage.tsx:8,12`, `AuthPlaceholderPage.tsx:8,12`, `NotFoundRoute.tsx:9-10`, `ErrorSurface.tsx:16,19`, `ApplicationLayout.tsx:11`). `placeholder-routes.spec.ts:18,42` (`toHaveTitle('Hierarchy')` / `toHaveTitle('Sign in')`) become `toHaveTitle('home.documentTitle')` / `toHaveTitle('login.documentTitle')`; `placeholder-routes.spec.ts:24` (`lang === 'en'`) becomes `lang === 'zxx'`.
+- **`e2e/right-to-left.spec.ts`** - the block comment at lines 7-27 is revised to state that a `test` locale (tag `'zxx'`) now exists but is not product-facing, per invariant 18; the `ROUTES` heading strings (lines 29-30) switch to key-echo values alongside the other specs.
+- **`src/app/ApplicationRoot.test.tsx:29`** - `expect(document.documentElement.lang).toBe('en')` becomes `.toBe('zxx')`; found while reading the current suite, not in the original candidate list.
+- **`src/app/composition/createRuntime.test.ts`** (G1/Claude-2) - gains a new test case: stub `navigator.languages` (e.g. via `vi.stubGlobal` or an equivalent already used elsewhere in this suite) to a value that should resolve to `Locale.Test`, call the real `createRuntime`, and assert `runtime.i18n.language === Locale.Test` - proving the actual wiring, not just `detectLocale`'s isolated correctness.
+
+### Data flow
+
+```mermaid
+graph LR
+    nav["navigator.languages<br/>(real browser)"] --> detect["detectLocale()"]
+    detect --> locale["Locale.English<br/>(always, today)"]
+    locale --> runtime["createRuntime.ts<br/>createInternationalization()"]
+
+    pw["Playwright locale: 'zxx'<br/>context option"] --> browserlang["reported navigator.languages"]
+    browserlang --> detect
+
+    btr["buildTestRuntime()<br/>(default, no override)"] --> testlocale["Locale.Test"]
+    testlocale --> gen1["createKeyEchoCatalogue(common.json)"]
+    gen1 --> runtime2["createInternationalization()<br/>(test runtime)"]
+
+    testlocale -.instance.language.-> lt["loadTranslations() per feature"]
+    lt --> gen2["createKeyEchoCatalogue(feature catalogue)<br/>when language === Locale.Test"]
+    gen2 --> bundle["addResourceBundle(instance.language, ns, resource)"]
+```
+
+Ownership: `Locale`/`detectLocale`/`createKeyEchoCatalogue` live in `platform/internationalization`, which stays domain-free and imports nothing from `app`/`feature`/`shared` (unchanged - `eslint.config.js:362-365`'s platform-can-only-import-platform rule is untouched by this design). `app` (`createRuntime.ts`, `renderRoute.tsx`) and each `feature`'s own `loadTranslations.ts` are the only callers, matching the existing dependency direction.
+
+### Decision-log text (R12)
+
+G1 (Claude-1) found three more places besides the decision log + invariant 65 that state "a second locale is deliberately not built" with no product/test distinction. All five are edited together so no stale, now-contradicted claim is left standing.
+
+**`specs/ARCHITECTURE.md`** - new entry appended to the decision log (section 6), after the existing "English-only i18n with full infrastructure" entry (`specs/ARCHITECTURE.md:168`), which today reads:
+
+> - **English-only i18n with full infrastructure** - the pipeline is what is expensive to retrofit; a second catalogue is not. Shipping a token second locale would prove less than exercising the real one.
+
+Proposed new entry (does not edit the line above - adds after it):
+
+> - **A key-echoing `test` locale ships in the same build that deploys to production** (2026-08-14, i18n-test-locale loop) - the entry above holds that a token second *product* locale would prove less than exercising the real one; this does not reopen that trade-off, because `test` is not a product locale at all. Unit and e2e assertions previously hardcoded a copy of the real English strings, which could drift silently whenever the catalogue's prose changed and the test's copy did not. `Locale.Test` (tag `'zxx'` - not `'test'` itself, which is not valid BCP-47 syntax and fails `Intl.getCanonicalLocales`) is a generated i18next resource bundle - one function walks each real English catalogue's key shape and replaces every leaf with its own dot-path, so `t('notFound.title')` resolves to the literal string `'notFound.title'` under it - which turns every such assertion into a structural check against the real catalogue instead of a hand-maintained copy of its prose. It is the default `buildTestRuntime` (`src/app/testing/renderRoute.tsx`) builds and the locale Playwright's `chromium` project requests via its context `locale` option; it is reachable only through those two paths, never through a real browser's language preference or an in-app control - `detectLocale` (`src/platform/internationalization/detectLocale.ts`) matches a visitor's `navigator.languages` against `Locale`'s members and falls back to `Locale.English` when nothing matches, which is what happens for every real visitor today, since `'zxx'` (ISO 639-2 for "no linguistic content") does not correspond to a spoken language and no real browser reports it. Rejected: keeping hand-authored English assertions, the status quo this entry replaces and the drift risk that motivated the change; and leaning on i18next's own missing-key fallback to echo keys, which `vitest.setup.ts`'s empty-`missingKeyReports` assertion forecloses - a real fallback reports every key as missing rather than echoing it silently.
+
+**`specs/ARCHITECTURE.md`** section 7 ("Deliberately not built", `specs/ARCHITECTURE.md:187`) today reads:
+
+> - **A second locale, SSR, a component library** - infrastructure supports each; none is needed to satisfy the goal.
+
+Proposed replacement:
+
+> - **A second *product* locale, SSR, a component library** - infrastructure supports each; none is needed to satisfy the goal. (A test-only locale for unit/e2e assertions ships separately - see the decision log.)
+
+**`specs/ROADMAP.md:115`** - carried verbatim from the line above ("so that reading only this file does not lead somewhere it should not", per `specs/ROADMAP.md:108`) - gets the identical edit, for the identical reason:
+
+> - **A second *product* locale, SSR, a component library** - the infrastructure supports each; none is needed. (A test-only locale for unit/e2e assertions ships separately - see ARCHITECTURE.md's decision log.)
+
+**`specs/phase-1-setup/PRODUCT.md:224`** (a separate "what's not built" list from invariant 65) today reads:
+
+> - A second locale, SSR and a component library. The infrastructure supports each; none is needed.
+
+Proposed replacement:
+
+> - A second *product* locale, SSR and a component library. The infrastructure supports each; none is needed. (A test-only locale ships separately - see ARCHITECTURE.md's decision log and invariant 65.)
+
+**`specs/phase-1-setup/PRODUCT.md`** - invariant 65 (`specs/phase-1-setup/PRODUCT.md:100`) today reads:
+
+> 65. English is the only shipped locale. The infrastructure carries no locale-specific branching, so adding a locale is adding a catalogue.
+
+Proposed replacement text (same invariant number, amended wording):
+
+> 65. English is the only shipped *product* locale - no in-app control ever selects a different one, and a real visitor's browser always resolves to it (`detectLocale` falls back to English whenever nothing in `navigator.languages` matches a supported locale). The infrastructure carries no product-facing locale-specific branching, so adding a second product locale is still adding a catalogue. A second locale, `test` (tag `'zxx'`), ships in the same build for unit and e2e tests only (see ARCHITECTURE.md's decision log): it is a generated, key-echoing catalogue reachable solely through `buildTestRuntime`'s default and Playwright's `chromium` project `locale` context option, never through a real browser or any UI control.
+
+## Testing and validation
+
+- **Unit** (Vitest, jsdom):
+  - Invariants 1-5: `locale.test.ts` (member values, derived type shape via a compile-time check or `Object.values` assertion) and `detectLocale.test.ts` (table-driven, mirroring `localeDirection.test.ts`'s style: exact match, region-stripped match, first-preference-wins with a multi-candidate list, empty list, no-match list, malformed candidate).
+  - Invariant 6-7: **Implementation correction (found while implementing step 3, not caught by either G1 round):** `createKeyEchoCatalogue.test.ts` lives in `platform/internationalization`, which cannot import `app`'s or either feature's real catalogue JSON directly - `eslint-plugin-boundaries` only allows `platform -> platform` (plus `shared` in test files), never `platform -> app`/`feature`, and this holds for test files too, not only production code. The generator's own unit test therefore uses a synthetic fixture catalogue (multiple namespaces, multiple sibling leaves, one two-level-deep branch - the same shape diversity as the real catalogues) instead of a real import, asserting every leaf equals its own dot path and the key set is unchanged - the generator is meant to be catalogue-shape-agnostic, so this proves the same thing a real-catalogue import would. Deep-mutation safety against the source catalogue object (G1/Codex-2) is proven the same way, via a snapshot comparison before/after calling the generator on the fixture. Coverage against the three REAL catalogues, and the `Locale.English`-then-`Locale.Test`-then-`Locale.English` registration-sequence mutation check, both happen in `loadTranslations.test.ts` (step 5, feature layer - can import both the real catalogue and the platform generator) instead; the `vitest.setup.ts` `missingKeyReports` assertion (`vitest.setup.ts:21-23`) passing across the whole suite is the proof that invariant 7 holds globally for every real catalogue, not just for one file.
+  - Invariant 8 (G1/Claude-2): a NEW case in the existing `createRuntime.test.ts`, stubbing `navigator.languages` and asserting the resulting runtime's `i18n.language` directly - not inferred from `detectLocale`'s isolated coverage or a diff read.
+  - Invariants 9-10: `loadTranslations.test.ts` (both features) gains a NEW case building an instance with `language: Locale.Test` and asserting the registered bundle's values are key-echoed, alongside the existing `language: 'en'` case (invariant 16) staying green unmodified - both cases live in the same file after this change.
+  - Invariants 11-14: run the full suite (`npm run test`) after the listed files are updated; every one of R7's files plus `ApplicationRoot.test.tsx` green is the evidence.
+  - Invariant 15: run `bootstrap.test.tsx` unmodified and confirm it still passes, as explicit evidence of "no change needed" rather than an assumption.
+  - Invariant 19-20: `npm run build && npm run size`, output captured as evidence; the dependency allow-list test (PRODUCT.md invariant 134's unit test) passing unmodified is the evidence for invariant 19. For invariant 20, inspect `dist/`'s actual chunk manifest (or `verify:build`'s output) to confirm which chunk(s) the generator landed in, and record whether any chunk not already named in `.size-limit.json` appeared - do not assume the entry/route split held without checking.
+- **E2E** (Playwright, per `VERIFICATION.md`'s flow list):
+  - Invariant 17-18: run the `chromium` project after adding `locale: 'zxx'`, all six listed specs green with key-echo assertions; `right-to-left.spec.ts`'s axe-clean and no-horizontal-overflow assertions continue to pass under the new locale, proving the RTL mechanism is unaffected by the comment/heading-string changes. Axe-clean specifically proves `lang="zxx"` does not trip `html-lang-valid` (G1/Codex-1).
+  - Non-goals: run `development` and `deployed` projects unmodified (or note they were not touched, since `deployed` requires a live URL) as evidence nothing leaked into them.
+- **Lint**: `npm run lint` clean, in particular `eslint-plugin-boundaries` on the two modified `loadTranslations.ts` files and `renderRoute.tsx`'s new imports, and `i18next/no-literal-string` unaffected (no new user-visible literal is introduced - key-echo strings appear only in generated resources and test assertions, never in JSX).
+
+## Risks
+
+- **Silent content drift if the conditional-echo branch in `loadTranslations.ts` is skipped.** If a future edit re-hardcodes the import or drops the `instance.language === Locale.Test` check, feature-namespace strings would silently stop echoing while `common` still does, producing tests that pass but assert real English text under a nominal test locale. Mitigation: invariants 9-10's unit coverage asserts the echoed shape directly against each feature's real catalogue, not against a fixture, so a regression here fails loudly.
+- **Bundle weight on feature route chunks.** The generator is now a static import inside two production files (`loadTranslations.ts` for both features), each bundled into its route's lazy chunk. The addition is small (a recursive object walk with no dependencies), but PRODUCT.md invariant 112's per-route chunk budget (30 kB gzipped) and lazy-catalogue-chunk budget (5 kB gzipped) are enforced numbers, not headroom assumed safe - `npm run size` must be run and its output recorded, not skipped.
+- **`ApplicationRoot.test.tsx` and other unlisted assertions.** R7's file list was explicitly non-exhaustive; this spec found one omission (`ApplicationRoot.test.tsx:29`) by reading the current suite rather than trusting the list. The implementation step should re-run this same search (`grep -rn "toBe('en')" src`) after all listed edits land, in case another one was missed here too.
+- **(G1/Codex-1, two rounds) Locale tag choice is load-bearing for browser/axe validity, not just detection logic, and syntax validity alone is not sufficient proof.** `'test'` failed `Intl`'s grammar check outright. `'pseudo'`, the first replacement, passed `Intl` but was independently found (via a real, executed axe-core 4.13.0 run, not inspection) to fail axe-core's registry-based `html-lang-valid` rule - a check `Intl.getCanonicalLocales` does not perform. `'zxx'`, the final choice, is a genuinely IANA-registered subtag verified against BOTH checks directly. Any future change to this value must be re-verified against both `Intl` AND a real axe-core run before use - syntactic validity alone (what `Intl` checks) is not sufficient evidence, as this spec's own revision history demonstrates.
+
+## Open questions
+
+1. Should `InternationalizationDependencies.language` (`createInternationalization.ts:9`, currently typed `string`) be tightened to `Locale` now that every call site passes one (`detectLocale(...)` or `Locale.Test`, plus two test files passing the literal `'en'`, which is assignable either way)? Recommendation: leave it `string` for this loop - it is a real, low-risk tightening but not something R1-R12 asked for, and the coding skill's "keep diffs surgical, refactors not asked for belong in their own change" argues for a separate change.
+2. Should `buildTestRuntime` accept an optional `locale` override parameter instead of hardcoding `Locale.Test`? No current call site needs anything but the default (verified: `features.test.tsx:39`, `RootErrorBoundary.test.tsx:18`, `createApplicationRouter.test.ts:7`). Recommendation: no override parameter now; add one if and when a real test needs to render in English deliberately.
+3. **Resolved (G1):** the proposed `ARCHITECTURE.md`/`ROADMAP.md`/`PRODUCT.md` decision-log-style entries use `(2026-08-14, i18n-test-locale loop)` as their date/context qualifier - this loop is not nested under a numbered phase directory (unlike `phase-1-setup`), so a loop-id qualifier is used in place of a phase number, matching the existing entries' pattern of "date + context" without inventing a phase number that doesn't apply here.
diff --git a/specs/i18n-test-locale/evidence/g3-e2e.txt b/specs/i18n-test-locale/evidence/g3-e2e.txt
new file mode 100644
index 0000000..a0af3f0
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/g3-e2e.txt
@@ -0,0 +1,25 @@
+
+Running 19 tests using 5 workers
+
+  ✓   1 [chromium] › e2e/placeholder-routes.spec.ts:6:3 › placeholder routes › the home route renders the hierarchy placeholder (413ms)
+  ✓   3 [chromium] › e2e/not-found.spec.ts:5:1 › an unknown path renders the not-found route with a working link home (486ms)
+  ✓   2 [chromium] › e2e/error-boundary.spec.ts:8:1 › a forced route chunk failure renders the boundary with a correlation id and recovers on retry (549ms)
+  ✓   6 [chromium] › e2e/placeholder-routes.spec.ts:32:3 › placeholder routes › the login route renders the generic auth placeholder (148ms)
+  ✓   7 [chromium] › e2e/telemetry-buffer.spec.ts:5:1 › the buffer sink records exactly one route_viewed for the initial load and no redacted-key leakage (142ms)
+  ✓   8 [chromium] › e2e/placeholder-routes.spec.ts:49:3 › placeholder routes › the skip link is the first thing a keyboard user reaches (127ms)
+  ✓   9 [chromium] › e2e/telemetry-buffer.spec.ts:29:1 › local and session storage are empty after visiting both routes (148ms)
+  ✓   4 [chromium] › e2e/accessibility.spec.ts:13:5 › accessibility › / has zero axe violations (680ms)
+  ✓   5 [chromium] › e2e/right-to-left.spec.ts:41:5 › right-to-left › / mirrors correctly under a forced right-to-left direction (705ms)
+  ✓  10 [development] › e2e/development-console.spec.ts:10:5 › development server console hygiene › / produces no console error or warning (413ms)
+  ✓  11 [chromium] › e2e/accessibility.spec.ts:13:5 › accessibility › /login has zero axe violations (382ms)
+  ✓  12 [chromium] › e2e/right-to-left.spec.ts:41:5 › right-to-left › /login mirrors correctly under a forced right-to-left direction (411ms)
+  ✓  13 [development] › e2e/kit-route.spec.ts:12:3 › kit route › renders every documented state with no horizontal overflow (506ms)
+  ✓  14 [development] › e2e/development-console.spec.ts:10:5 › development server console hygiene › /login produces no console error or warning (360ms)
+  ✓  15 [chromium] › e2e/accessibility.spec.ts:13:5 › accessibility › /does-not-exist has zero axe violations (386ms)
+  ✓  16 [development] › e2e/kit-route.spec.ts:29:3 › kit route › has zero axe violations in real light (622ms)
+  ✓  17 [development] › e2e/kit-route.spec.ts:46:3 › kit route › has zero axe violations in real dark (595ms)
+  ✓  18 [development] › e2e/kit-route.spec.ts:59:3 › kit route › a skeleton box equals the box of the content it stands in for (326ms)
+  ✓  19 [development] › e2e/kit-route.spec.ts:85:3 › kit route › mirrors correctly under a forced right-to-left direction (578ms)
+
+  19 passed (5.4s)
+EXIT:0
diff --git a/specs/i18n-test-locale/evidence/g3-verify.txt b/specs/i18n-test-locale/evidence/g3-verify.txt
new file mode 100644
index 0000000..5993644
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/g3-verify.txt
@@ -0,0 +1,179 @@
+
+> hierarchy-tree@0.0.0 verify
+> npm run typecheck && npm run lint && npm run format:check && npm run test:coverage && npm run build && npm run verify:build && npm run size
+
+
+> hierarchy-tree@0.0.0 typecheck
+> tsc -b
+
+
+> hierarchy-tree@0.0.0 lint
+> eslint . && node scripts/assert-no-physical-properties.mjs && node scripts/assert-domain-vocabulary.mjs && node scripts/assert-no-lorem-ipsum.mjs && node scripts/assert-no-secrets.mjs --source-only
+
+
+> hierarchy-tree@0.0.0 format:check
+> prettier --check .
+
+Checking formatting...
+All matched files use Prettier code style!
+
+> hierarchy-tree@0.0.0 test:coverage
+> vitest run --coverage
+
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+      Coverage enabled with v8
+
+assert-domain-vocabulary: banned vocabulary found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-oBuMlG/banned.tmp.ts: identifier "buildForest" carries banned reference-system name "buildForest"
+assert-domain-vocabulary: banned vocabulary found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-alias-YA8Bmu/src/shared/banned-alias.tmp.ts: exported identifier "sessionToken" carries the banned domain word "session"
+assert-domain-vocabulary: banned vocabulary found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-default-vJtNcE/src/shared/banned-default.tmp.ts: exported identifier "sessionManager" carries the banned domain word "manager"
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-default-vJtNcE/src/shared/banned-default.tmp.ts: exported identifier "sessionManager" carries the banned domain word "session"
+assert-no-physical-properties: physical CSS properties found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/physical-properties-YwYYwh/Banned.tmp.tsx: physical property/utility "ml-*" found - use the logical equivalent
+assert-no-physical-properties: physical CSS properties found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/raw-color-DopNTs/Banned.tmp.tsx: raw color value found - use a theme.css token instead
+assert-no-lorem-ipsum: filler text found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/lorem-ipsum-jyE0dO/Banned.tmp.tsx: lorem ipsum filler text found
+assert-no-secrets: pass --source-only or --bundle-only
+assert-no-secrets --source-only: secrets found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/no-secrets-keyword-RPFX8a/banned.tmp.ts: matches secret keyword pattern /apiKey/i
+assert-no-secrets --source-only: secrets found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/no-secrets-entropy-3JQVdC/banned.tmp.ts: high-entropy literal found (9f8a7b6c5d4e...)
+assert-no-secrets --source-only: secrets found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/no-secrets-suppression-2WNXU9/banned.tmp.ts: eslint-disable suppresses "no-restricted-" - suppression is not permitted for this rule
+assert-no-secrets --bundle-only: dist/ is missing - run npm run build first
+assert-no-secrets --source-only: secrets found:
+  - .github/workflows/ci.yml: matches secret keyword pattern /apiKey/i
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+
+ Test Files  67 passed (67)
+      Tests  280 passed (280)
+   Start at  12:51:10
+   Duration  9.71s (transform 2.12s, setup 6.15s, import 3.64s, tests 14.85s, environment 17.58s)
+
+ % Coverage report from v8
+-------------------|---------|----------|---------|---------|-------------------
+File               | % Stmts | % Branch | % Funcs | % Lines | Uncovered Line #s 
+-------------------|---------|----------|---------|---------|-------------------
+All files          |   96.21 |    96.33 |    95.2 |   96.19 |                   
+ app               |   94.44 |      100 |      80 |   94.44 |                   
+  bootstrap.ts     |      90 |      100 |      50 |      90 | 54                
+ ...error-boundary |      90 |      100 |   77.77 |   89.65 |                   
+  ...rBoundary.tsx |   77.77 |      100 |      75 |   77.77 | 40-41             
+  ...rBoundary.tsx |   83.33 |      100 |      50 |   83.33 | 22                
+ app/routing       |   84.09 |      100 |   78.57 |   83.33 |                   
+  ...efinitions.ts |   56.25 |      100 |      50 |   56.25 | 29-31,48-49,57-58 
+ platform/http     |   94.68 |    93.75 |     100 |   95.23 |                   
+  ...HttpClient.ts |   93.05 |    92.85 |     100 |   94.02 | 176-177,204,212   
+ platform/runtime  |     100 |    66.66 |     100 |     100 |                   
+  ...ystemClock.ts |     100 |       75 |     100 |     100 | 44                
+  ...Randomness.ts |     100 |       50 |     100 |     100 | 8                 
+ shared/ui         |   98.11 |    93.75 |     100 |   98.03 |                   
+  Skeleton.tsx     |   88.88 |       50 |     100 |    87.5 | 26                
+  ...veInitials.ts |     100 |       75 |     100 |     100 | 5-6               
+-------------------|---------|----------|---------|---------|-------------------
+
+=============================== Coverage summary ===============================
+Statements   : 96.21% ( 432/449 )
+Branches     : 96.33% ( 210/218 )
+Functions    : 95.2% ( 119/125 )
+Lines        : 96.19% ( 404/420 )
+================================================================================
+
+> hierarchy-tree@0.0.0 build
+> tsc -b && vite build
+
+vite v8.2.1 building client environment for production...
+[2Ktransforming...✓ 249 modules transformed.
+rendering chunks...
+computing gzip size...
+dist/index.html                             1.00 kB │ gzip:   0.49 kB
+dist/.vite/manifest.json                    2.63 kB │ gzip:   0.52 kB
+dist/assets/index-Bz8bntyd.css             14.26 kB │ gzip:   3.82 kB
+dist/assets/hooks-Vk6wLOOz.js               0.18 kB │ gzip:   0.16 kB
+dist/assets/auth-P4LHvL-A.js                0.18 kB │ gzip:   0.15 kB
+dist/assets/hierarchy-CAF2Qe9v.js           0.19 kB │ gzip:   0.15 kB
+dist/assets/NotFoundRoute-ctVefMAm.js       0.26 kB │ gzip:   0.20 kB
+dist/assets/rolldown-runtime-CbXtAM7H.js    0.58 kB │ gzip:   0.36 kB
+dist/assets/LoginRoute-DKXoW-vY.js          0.70 kB │ gzip:   0.44 kB
+dist/assets/HomeRoute-C-StIgrL.js           0.71 kB │ gzip:   0.45 kB
+dist/assets/ui-030Lc4iH.js                  4.75 kB │ gzip:   1.94 kB
+dist/assets/entry-B_Ck4VM7.js              13.65 kB │ gzip:   5.32 kB
+dist/assets/vendor-CdUFDZ9N.js            343.81 kB │ gzip: 108.88 kB
+
+✓ built in 127ms
+
+> hierarchy-tree@0.0.0 verify:build
+> vitest run --config vitest.build-output.config.ts
+
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  8 passed (8)
+      Tests  15 passed (15)
+   Start at  12:51:22
+   Duration  126ms (transform 54ms, setup 0ms, import 113ms, tests 56ms, environment 0ms)
+
+
+> hierarchy-tree@0.0.0 size
+> size-limit
+
+- Running JS in headless Chrome
+✔ Running JS in headless Chrome
+  
+  app entry (initial payload)
+  Size limit:   150 kB
+  Size:         112.84 kB gzipped
+  Loading time: 2.3 s     on slow 3G
+  
+  stylesheet
+  Size limit:   15 kB
+  Size:         3.81 kB gzipped
+  Loading time: 75 ms   on slow 3G
+  
+  route: home
+  Size limit:   30 kB
+  Size:         452 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  route: login
+  Size limit:   30 kB
+  Size:         448 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  route: not-found
+  Size limit:   30 kB
+  Size:         200 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  catalogue: auth
+  Size limit:   5 kB
+  Size:         156 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  catalogue: hierarchy
+  Size limit:   5 kB
+  Size:         157 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+REAL_EXIT_CODE:0
diff --git a/specs/i18n-test-locale/evidence/reviews/g1-claude-validator-recheck.md b/specs/i18n-test-locale/evidence/reviews/g1-claude-validator-recheck.md
new file mode 100644
index 0000000..6ab0fd2
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/reviews/g1-claude-validator-recheck.md
@@ -0,0 +1,21 @@
+# G1 fix-confirmation - fresh loop-spec-validator instance
+
+## Verdict on the 5 tracked findings
+
+1. **Claude-1 (doc consistency) - RESOLVED.** The spec's Non-goals section now states "all three are edited by this spec (not left standing)" and the "Decision-log text (R12)" section gives concrete before/after text for all 5 edits: ARCHITECTURE.md's decision log, ARCHITECTURE.md §7, ROADMAP.md:115, PRODUCT.md's not-built list, and PRODUCT.md invariant 65. Independently confirmed all five current-state quotes against the live files and they match verbatim.
+
+2. **Claude-2 (createRuntime.ts wiring test) - RESOLVED.** Invariant 8 now says the exact wiring is directly tested via a new case in the existing `createRuntime.test.ts` - no hedge language remains. Confirmed `createRuntime.test.ts` exists with 4 tests today and `createRuntime.ts:26,58` still has the hardcoded constant the spec proposes to replace.
+
+3. **Codex-1 (invalid locale tag) - RESOLVED, consistently applied** (at the time of this check, against the `'pseudo'` value - see round-2 Codex review for the subsequent `'pseudo'` -> `'zxx'` correction). Grepped the full spec for the literal old `'test'` value and found zero matches. Lacked shell/execution access to independently re-run the Intl probe in this pass (this is exactly the gap the subsequent axe-core execution check closed).
+
+4. **Codex-2 (generator non-mutation) - RESOLVED.** Invariant 6 explicitly requires non-mutation with a corresponding test requirement.
+
+5. **Codex-3 (invariant 16 contradiction) - RESOLVED.** Invariant 16 and the testing plan now agree: both files gain a new test case, existing case unchanged. Confirmed against the actual current test files.
+
+## New findings from the revision
+
+None blocking. Two non-blocking observations: Codex-4 deferred to G3 as a defensible, explicitly-testable deferral rather than a silent gap; Claude-3 acknowledged with a documented fallback plan.
+
+## Overall verdict
+
+All 5 tracked findings resolved with no new blocking issues, AT THE TIME OF THIS CHECK. Superseded in part by the subsequent Codex round-2 executed-axe-core finding that the chosen locale value (`'pseudo'` at the time) failed a check this pass had no tooling to run - see `g1-codex-review-round2.md` and the final `'zxx'` resolution.
diff --git a/specs/i18n-test-locale/evidence/reviews/g1-claude-validator.md b/specs/i18n-test-locale/evidence/reviews/g1-claude-validator.md
new file mode 100644
index 0000000..af35f30
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/reviews/g1-claude-validator.md
@@ -0,0 +1,42 @@
+# G1 spec validation - loop-spec-validator (fresh context)
+
+## Verdict
+
+3 findings (2 blocking)
+
+## Findings
+
+**Claude-1 (BLOCKING) — The spec's own doc edits leave three untouched "a second locale is deliberately not built" statements standing, directly contradicted by what this spec ships.**
+
+Location: Spec's "Tech approach > Decision-log text (R12)" section (lines 124-140), specifically the sentence "only `ARCHITECTURE.md`'s decision log and `PRODUCT.md`'s invariant 65 are updated, per the agreed scope" (Non-goals, last bullet).
+
+I found three separate places in the repo, none touched by this spec, that state a second locale is deliberately not built:
+- `specs/ARCHITECTURE.md:187` — "**A second locale, SSR, a component library** - infrastructure supports each; none is needed to satisfy the goal." (section 7, "Deliberately not built")
+- `specs/ROADMAP.md:115` — "**A second locale, SSR, a component library** - the infrastructure supports each; none is needed." This line is explicitly introduced as "Carried from `ARCHITECTURE.md` section 7 so that reading only this file does not lead somewhere it should not" (line 108).
+- `specs/phase-1-setup/PRODUCT.md:224` — "A second locale, SSR and a component library. The infrastructure supports each; none is needed." (a separate "what's not built" list, distinct from invariant 65 which the spec does edit at line 100).
+
+The spec's proposed ARCHITECTURE.md decision-log entry argues `test` "is not a product locale at all," but none of these three lines make that product/test distinction — they flatly say "a second locale... none is needed," and after this change a second locale (albeit test-only) does exist and ships in the production build. A reader hitting any of these three lines (all designed to be read standalone, per ROADMAP.md's own framing) would reasonably conclude this spec contradicts settled architecture.
+
+Why it matters: an implementer or reviewer reading ROADMAP.md or PRODUCT.md's "not built" list after this change ships would see a stale claim contradicted by the shipped code, with no cross-reference explaining why.
+
+**Claude-2 (BLOCKING) — Invariant 8 (the actual production wiring: `createRuntime.ts` calling `detectLocale(navigator.languages)`) has no committed automated test, despite the file that should carry it already existing.**
+
+Location: Invariant 8 (line 16) and its testing entry (line 147): "`createRuntime.test.ts` (if one exists, or added) stubbing `navigator.languages` and asserting the resulting `i18n.language`; at minimum, `detectLocale`'s own unit coverage plus a read of the diff proves the wiring."
+
+I confirmed `src/app/composition/createRuntime.test.ts` already exists (4 tests, none touching `navigator.languages` or `i18n.language`). The spec's own hedge ("if one exists, or added") shows this wasn't checked, and its fallback does not verify that `createRuntime.ts` actually calls `detectLocale` with `navigator.languages` and feeds the result into `language`. A bug here (e.g. passing `navigator.language` singular by mistake, or a typo swapping arguments) would pass `detectLocale.test.ts` and pass a manual diff read, and still be wrong in production.
+
+**Claude-3 (non-blocking) — `createKeyEchoCatalogue`'s claimed return-type soundness is asserted, not demonstrated.**
+
+Location: Tech approach, `createKeyEchoCatalogue.ts` bullet (line 52). JSON-imported catalogue leaf strings may type as string literals rather than widened `string`, which could make the recursive walker's string-typed replacement value unassignable back into `Catalogue` without a cast. Resolvable by the implementer prototyping against `tsc` before committing to the signature.
+
+## What was checked (no issues found)
+
+- Playwright project scoping: `chromiumProject.testIgnore` and `developmentProject.testMatch` partition `./e2e` with no overlap; the six specs listed in invariant 17 are exactly the six files that resolve to the `chromium` project.
+- Playwright `locale: 'test'` validity / downstream `Intl` calls: grepped all `Intl.*` usage in `src` — the only call is the hardcoded `new Intl.ListFormat('en')` in `ConfigurationErrorScreen.tsx`, excluded by non-goals. **Note (superseded by Codex-1): this check concluded "should not throw" from training/syntax reasoning without executing the claim — Codex's review executed it and found it DOES throw. Codex-1 is the operative finding here, not this check.**
+- `registerCatalogue` timing: `instance.language` is set synchronously during the awaited `.init()` call, and `loadTranslations` is only invoked later (from route `lazy()`), so `instance.language` is reliably populated by the time `registerCatalogue` reads it.
+- `ConfigurationErrorScreen.tsx` exclusion: confirmed accurate.
+- Cross-checked every concrete file/line citation in the spec against the actual repo — all accurate.
+
+## Sign-off
+
+Not ready for sign-off as-is. Claude-1 and Claude-2 should be resolved before implementation starts.
diff --git a/specs/i18n-test-locale/evidence/reviews/g1-codex-review-round2.md b/specs/i18n-test-locale/evidence/reviews/g1-codex-review-round2.md
new file mode 100644
index 0000000..1a8789c
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/reviews/g1-codex-review-round2.md
@@ -0,0 +1,18 @@
+# G1 fix-confirmation - Codex second opinion, round 2
+
+3 of 4 resolved
+
+1. PARTIALLY RESOLVED | "`'pseudo'` is verified valid ... so no `Intl`/HTML-`lang` validation path rejects it." (SPEC.md:11, pre-round-2 text) | The `Intl` half is resolved: `Intl.getCanonicalLocales('pseudo')` returned `['pseudo']`, and `new Intl.DateTimeFormat('pseudo')` succeeded. However, an actual axe-core 4.13.0 probe against `<html lang="pseudo">` produced a serious `html-lang-valid` violation. Therefore the spec's axe claims at lines 7, 11, 23, 180, and 189 are false. For comparison, `'zxx'` succeeds with both `Intl` checks and produces no axe violation.
+
+2. RESOLVED | "The generator never mutates its input. It builds and returns a new object tree." (SPEC.md:16); testing requires "the source catalogue object is unchanged after generation" and a test/English registration sequence (SPEC.md:173) | The non-mutation contract and discriminating regression coverage are now explicit.
+
+3. RESOLVED | "Both files change (a new test is added); neither file's existing case changes." (SPEC.md:26) | The former contradiction is removed. The validation section consistently requires a new `Locale.Test` case while preserving the existing English case.
+
+4. RESOLVED | "This invariant does NOT claim which specific chunk the catalogue generator lands in" and any uncovered chunk becomes a Verify-phase finding to resolve (SPEC.md:30); validation explicitly requires inspecting the real manifest and checking for chunks absent from `.size-limit.json` (SPEC.md:178) | The unverified Rollup-placement assumption has been removed and replaced with build-output inspection plus a required response to uncovered chunks.
+
+The spec is not ready for sign-off yet. Finding 1 remains blocking: replace `'pseudo'` with an axe-valid tag and update every associated claim and assertion. `'zxx'` passed the same `Intl` and axe checks locally and preserves the single-subtag detection design.
+
+No files were modified.
+
+---
+Note from driving session: independently re-verified via direct execution against the repo's own installed axe-core 4.13.0 (node + jsdom): 'pseudo' and 'qtz' both produce an `html-lang-valid` violation; 'zxx' and 'en' produce zero violations. SPEC.md revised to use 'zxx' throughout (user confirmed via AskUserQuestion). Codex's own round-2 statement above already confirms 'zxx' passes both checks it ran, so finding 1 is treated as resolved on the combination of Codex's own round-2 verification + this session's independent re-verification, without a third redundant round-trip.
diff --git a/specs/i18n-test-locale/evidence/reviews/g1-codex-review.md b/specs/i18n-test-locale/evidence/reviews/g1-codex-review.md
new file mode 100644
index 0000000..9cd2140
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/reviews/g1-codex-review.md
@@ -0,0 +1,32 @@
+# G1 spec validation - Codex second opinion (fresh thread)
+
+4 findings (4 blocking)
+
+1. The literal Playwright locale `'test'` is not accepted as an `Intl` locale and is also an invalid HTML language value for axe | SPEC.md invariants 13/17 and Playwright change | Installed Playwright 1.62.1 forwards the locale directly to Chromium's locale override; the local `Intl.getCanonicalLocales('test')` and `Intl.DateTimeFormat('test')` probes both threw `RangeError: Incorrect locale information provided`, and even if context creation succeeded, `lang="test"` would fail the `html-lang-valid` rule exercised by the accessibility and RTL specs. Use a valid tag such as `en-x-test`, then revise `Locale`, detection, and assertions accordingly. | BLOCKING
+
+2. The generator contract never requires preserving the imported English catalogue | SPEC.md generator approach, conditional registration | An in-place recursive walker satisfies the described return shape but mutates the cached JSON module; after a test-locale registration, a later English runtime can receive key echoes instead of English, contradicting invariant 10. Require a non-mutating result and test that every source catalogue remains unchanged after generation and after test-then-English registration. | BLOCKING
+
+3. The spec says both feature `loadTranslations.test.ts` files need no change while the validation plan requires adding test-locale cases to them | SPEC.md invariant 16, testing plan | An implementer cannot satisfy both instructions, and leaving the tests unchanged provides no direct proof that the new conditional branch registers echoed resources under the test language. Amend invariant 16 to preserve the existing English cases while adding the test cases. | BLOCKING
+
+4. The claimed generator chunk placement and size coverage are neither guaranteed nor tested | SPEC.md invariant 20, validation plan, .size-limit.json | A module statically imported by two lazy route graphs may be emitted as a shared chunk; the current budgets measure only named route and catalogue files, while `npm run size` does not prove where the generator landed or cover a new shared chunk. Add a manifest/build-output assertion and budget any resulting shared chunk, or relax the placement invariant and define a transitive lazy-graph budget. | BLOCKING
+
+## Explicit assessment
+
+1. Completeness: Not complete. It lacks the non-mutation requirement and a strategy for a browser-valid test locale. It also omits handling and measurement of a possible shared generator chunk.
+
+2. Internal consistency: Not consistent. Invariant 16 contradicts the testing section. The claim that `lang="test"` and axe checks continue passing conflicts with the actual accessibility tooling.
+
+3. Feasibility: Most named locations support the approach:
+- The six named e2e files run only under `chromium`. The development project positively matches only `development-console.spec.ts` and `kit-route.spec.ts`; deployed positively matches only `deployed-smoke.spec.ts`.
+- `createRuntime.ts`, `renderRoute.tsx`, the internationalization barrel, and both feature loaders are appropriate modification points.
+- The literal `locale: 'test'` is not feasible as specified. Current application code does not pass `i18n.language` to an `Intl` constructor; the only application `Intl` usage is fixed to `'en'` in `ConfigurationErrorScreen.tsx`. The immediate risk is Playwright/Chromium's locale override itself, followed by invalid `document.lang` and axe.
+- The proposed `loadTranslations.ts` conditional is correct for current timing: `createInternationalization` awaits initialization before returning, and `instance.language` is `'test'` when the dynamic catalogue import resolves. A direct installed-i18next probe confirmed registration and translation under that language. This remains correct only if the generator does not mutate `catalogue.default`.
+
+4. Testability: Not every invariant has a discriminating check as written. Locale detection, catalogue shape, missing-key behavior, runtime wiring, English preservation, and key echoes are unit-testable. Browser locale behavior is testable only after choosing a valid locale tag. Chunk placement requires a manifest assertion; `npm run size` alone is insufficient.
+
+Sign-off: Not ready. All four findings should be resolved in the spec before user sign-off.
+
+No files were modified. No full suite was run because the implementation does not exist; review evidence came from current source/configuration plus read-only `Intl` and i18next probes.
+
+---
+Note from driving session: Codex-1's core factual claim (Intl.getCanonicalLocales('test') throws RangeError) was independently re-verified via direct node execution and confirmed correct. Codex's suggested replacement tag 'en-x-test' was considered alongside 'qtz' and 'pseudo'; the user chose 'pseudo' (verified valid, single-segment, no detection-algorithm change needed, more readable than 'qtz' or 'en-x-test').
diff --git a/specs/i18n-test-locale/evidence/step1-green.txt b/specs/i18n-test-locale/evidence/step1-green.txt
new file mode 100644
index 0000000..44ec79c
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step1-green.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  1 passed (1)
+   Start at  12:28:09
+   Duration  97ms (transform 17ms, setup 35ms, import 2ms, tests 1ms, environment 0ms)
+
diff --git a/specs/i18n-test-locale/evidence/step1-red.txt b/specs/i18n-test-locale/evidence/step1-red.txt
new file mode 100644
index 0000000..36e92ef
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step1-red.txt
@@ -0,0 +1,24 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |platform| src/platform/internationalization/locale.test.ts (0 test)
+
+⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |platform| src/platform/internationalization/locale.test.ts [ src/platform/internationalization/locale.test.ts ]
+Error: Cannot find module './locale' imported from /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/platform/internationalization/locale.test.ts
+ ❯ src/platform/internationalization/locale.test.ts:2:1
+      1| import { describe, expect, it } from 'vitest';
+      2| import { Locale } from './locale';
+       | ^
+      3|
+      4| describe('Locale', () => {
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  no tests
+   Start at  12:27:52
+   Duration  100ms (transform 19ms, setup 36ms, import 0ms, tests 0ms, environment 0ms)
+
diff --git a/specs/i18n-test-locale/evidence/step2-green.txt b/specs/i18n-test-locale/evidence/step2-green.txt
new file mode 100644
index 0000000..112d19f
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step2-green.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  4 passed (4)
+   Start at  12:28:54
+   Duration  89ms (transform 18ms, setup 31ms, import 2ms, tests 2ms, environment 0ms)
+
diff --git a/specs/i18n-test-locale/evidence/step2-red.txt b/specs/i18n-test-locale/evidence/step2-red.txt
new file mode 100644
index 0000000..1482446
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step2-red.txt
@@ -0,0 +1,24 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |platform| src/platform/internationalization/detectLocale.test.ts (0 test)
+
+⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |platform| src/platform/internationalization/detectLocale.test.ts [ src/platform/internationalization/detectLocale.test.ts ]
+Error: Cannot find module './detectLocale' imported from /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/platform/internationalization/detectLocale.test.ts
+ ❯ src/platform/internationalization/detectLocale.test.ts:2:1
+      1| import { describe, expect, it } from 'vitest';
+      2| import { detectLocale } from './detectLocale';
+       | ^
+      3| import { Locale } from './locale';
+      4|
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  no tests
+   Start at  12:28:35
+   Duration  91ms (transform 18ms, setup 31ms, import 0ms, tests 0ms, environment 0ms)
+
diff --git a/specs/i18n-test-locale/evidence/step3-green.txt b/specs/i18n-test-locale/evidence/step3-green.txt
new file mode 100644
index 0000000..3f1b7dd
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step3-green.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  2 passed (2)
+   Start at  12:34:41
+   Duration  92ms (transform 19ms, setup 32ms, import 2ms, tests 2ms, environment 0ms)
+
diff --git a/specs/i18n-test-locale/evidence/step3-red.txt b/specs/i18n-test-locale/evidence/step3-red.txt
new file mode 100644
index 0000000..8f918dc
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step3-red.txt
@@ -0,0 +1,25 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |platform| src/platform/internationalization/createKeyEchoCatalogue.test.ts (0 test)
+
+⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |platform| src/platform/internationalization/createKeyEchoCatalogue.test.ts [ src/platform/internationalization/createKeyEchoCatalogue.test.ts ]
+Error: Cannot find module './createKeyEchoCatalogue' imported from /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/platform/internationalization/createKeyEchoCatalogue.test.ts
+ ❯ src/platform/internationalization/createKeyEchoCatalogue.test.ts:5:1
+      3| import authCatalogue from '../../features/auth/locales/en/auth.json';
+      4| import hierarchyCatalogue from '../../features/hierarchy/locales/en/hi…
+      5| import { createKeyEchoCatalogue } from './createKeyEchoCatalogue';
+       | ^
+      6|
+      7| type Catalogue = Record<string, unknown>;
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  no tests
+   Start at  12:32:53
+   Duration  99ms (transform 22ms, setup 34ms, import 0ms, tests 0ms, environment 0ms)
+
diff --git a/specs/i18n-test-locale/evidence/step4-full-suite.txt b/specs/i18n-test-locale/evidence/step4-full-suite.txt
new file mode 100644
index 0000000..45629fe
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step4-full-suite.txt
@@ -0,0 +1,20 @@
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+
+ Test Files  67 passed (67)
+      Tests  277 passed (277)
+   Start at  12:36:36
+   Duration  8.33s (transform 1.59s, setup 4.95s, import 2.58s, tests 12.05s, environment 14.86s)
+
diff --git a/specs/i18n-test-locale/evidence/step4-green.txt b/specs/i18n-test-locale/evidence/step4-green.txt
new file mode 100644
index 0000000..58a508a
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step4-green.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  5 passed (5)
+   Start at  12:36:23
+   Duration  591ms (transform 66ms, setup 90ms, import 70ms, tests 4ms, environment 364ms)
+
diff --git a/specs/i18n-test-locale/evidence/step4-red.txt b/specs/i18n-test-locale/evidence/step4-red.txt
new file mode 100644
index 0000000..0050ac9
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step4-red.txt
@@ -0,0 +1,30 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |ui| src/app/composition/createRuntime.test.ts (5 tests | 1 failed) 6ms
+     × resolves the runtime language from navigator.languages 2ms
+
+⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |ui| src/app/composition/createRuntime.test.ts > createRuntime > resolves the runtime language from navigator.languages
+AssertionError: expected 'en' to be 'zxx' // Object.is equality
+
+Expected: "zxx"
+Received: "en"
+
+ ❯ src/app/composition/createRuntime.test.ts:53:35
+     51|     const runtime = await createRuntime(TEST_CONFIGURATION);
+     52|
+     53|     expect(runtime.i18n.language).toBe(Locale.Test);
+       |                                   ^
+     54|   });
+     55|
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  1 failed | 4 passed (5)
+   Start at  12:36:02
+   Duration  652ms (transform 65ms, setup 103ms, import 71ms, tests 6ms, environment 407ms)
+
diff --git a/specs/i18n-test-locale/evidence/step5-full-suite.txt b/specs/i18n-test-locale/evidence/step5-full-suite.txt
new file mode 100644
index 0000000..468d2b3
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step5-full-suite.txt
@@ -0,0 +1,10 @@
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+
+ Test Files  67 passed (67)
+      Tests  279 passed (279)
+   Start at  12:39:01
+   Duration  8.14s (transform 1.56s, setup 4.90s, import 2.62s, tests 11.58s, environment 14.84s)
+
diff --git a/specs/i18n-test-locale/evidence/step5-green-auth.txt b/specs/i18n-test-locale/evidence/step5-green-auth.txt
new file mode 100644
index 0000000..0792502
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step5-green-auth.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  3 passed (3)
+   Start at  12:38:02
+   Duration  511ms (transform 25ms, setup 100ms, import 4ms, tests 5ms, environment 336ms)
+
diff --git a/specs/i18n-test-locale/evidence/step5-green-hierarchy.txt b/specs/i18n-test-locale/evidence/step5-green-hierarchy.txt
new file mode 100644
index 0000000..3ffa91f
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step5-green-hierarchy.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  3 passed (3)
+   Start at  12:38:41
+   Duration  504ms (transform 24ms, setup 100ms, import 4ms, tests 6ms, environment 338ms)
+
diff --git a/specs/i18n-test-locale/evidence/step5-red-hierarchy.txt b/specs/i18n-test-locale/evidence/step5-red-hierarchy.txt
new file mode 100644
index 0000000..cebd466
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step5-red-hierarchy.txt
@@ -0,0 +1,39 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |ui| src/features/hierarchy/loadTranslations.test.ts (3 tests | 1 failed) 6ms
+     × registers the key-echoed catalogue under Locale.Test 2ms
+
+⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |ui| src/features/hierarchy/loadTranslations.test.ts > hierarchy loadTranslations > registers the key-echoed catalogue under Locale.Test
+AssertionError: expected undefined to deeply equal { home: { …(3) } }
+
+- Expected:
+{
+  "home": {
+    "documentTitle": "home.documentTitle",
+    "message": "home.message",
+    "title": "home.title",
+  },
+}
+
++ Received:
+undefined
+
+ ❯ src/features/hierarchy/loadTranslations.test.ts:52:70
+     50|     const testInstance = await createTestI18n(Locale.Test);
+     51|     await loadTranslations(testInstance);
+     52|     expect(testInstance.getResourceBundle(Locale.Test, 'hierarchy')).t…
+       |                                                                      ^
+     53|       home: {
+     54|         documentTitle: 'home.documentTitle',
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  1 failed | 2 passed (3)
+   Start at  12:38:24
+   Duration  530ms (transform 47ms, setup 122ms, import 7ms, tests 6ms, environment 337ms)
+
diff --git a/specs/i18n-test-locale/evidence/step5-red.txt b/specs/i18n-test-locale/evidence/step5-red.txt
new file mode 100644
index 0000000..60702b7
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step5-red.txt
@@ -0,0 +1,39 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |ui| src/features/auth/loadTranslations.test.ts (3 tests | 1 failed) 6ms
+     × registers the key-echoed catalogue under Locale.Test 2ms
+
+⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |ui| src/features/auth/loadTranslations.test.ts > auth loadTranslations > registers the key-echoed catalogue under Locale.Test
+AssertionError: expected undefined to deeply equal { login: { …(3) } }
+
+- Expected:
+{
+  "login": {
+    "documentTitle": "login.documentTitle",
+    "message": "login.message",
+    "title": "login.title",
+  },
+}
+
++ Received:
+undefined
+
+ ❯ src/features/auth/loadTranslations.test.ts:52:65
+     50|     const testInstance = await createTestI18n(Locale.Test);
+     51|     await loadTranslations(testInstance);
+     52|     expect(testInstance.getResourceBundle(Locale.Test, 'auth')).toEqua…
+       |                                                                 ^
+     53|       login: {
+     54|         documentTitle: 'login.documentTitle',
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  1 failed | 2 passed (3)
+   Start at  12:37:40
+   Duration  563ms (transform 25ms, setup 100ms, import 4ms, tests 6ms, environment 390ms)
+
diff --git a/specs/i18n-test-locale/evidence/step6-full-suite.txt b/specs/i18n-test-locale/evidence/step6-full-suite.txt
new file mode 100644
index 0000000..973f806
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step6-full-suite.txt
@@ -0,0 +1,49 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+assert-domain-vocabulary: banned vocabulary found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-o4VV4c/banned.tmp.ts: identifier "buildForest" carries banned reference-system name "buildForest"
+assert-domain-vocabulary: banned vocabulary found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-alias-83mT9k/src/shared/banned-alias.tmp.ts: exported identifier "sessionToken" carries the banned domain word "session"
+assert-domain-vocabulary: banned vocabulary found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-default-0BdOZs/src/shared/banned-default.tmp.ts: exported identifier "sessionManager" carries the banned domain word "manager"
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/domain-vocabulary-default-0BdOZs/src/shared/banned-default.tmp.ts: exported identifier "sessionManager" carries the banned domain word "session"
+assert-no-physical-properties: physical CSS properties found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/physical-properties-erShQR/Banned.tmp.tsx: physical property/utility "ml-*" found - use the logical equivalent
+assert-no-physical-properties: physical CSS properties found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/raw-color-DTweHz/Banned.tmp.tsx: raw color value found - use a theme.css token instead
+assert-no-lorem-ipsum: filler text found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/lorem-ipsum-dMyz7v/Banned.tmp.tsx: lorem ipsum filler text found
+assert-no-secrets: pass --source-only or --bundle-only
+assert-no-secrets --source-only: secrets found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/no-secrets-keyword-iTKnwU/banned.tmp.ts: matches secret keyword pattern /apiKey/i
+assert-no-secrets --source-only: secrets found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/no-secrets-entropy-RWHzXm/banned.tmp.ts: high-entropy literal found (9f8a7b6c5d4e...)
+assert-no-secrets --source-only: secrets found:
+  - /var/folders/cw/9qpg225s27g0tc1y92m772c00000gn/T/no-secrets-suppression-3RSFCI/banned.tmp.ts: eslint-disable suppresses "no-restricted-" - suppression is not permitted for this rule
+assert-no-secrets --bundle-only: dist/ is missing - run npm run build first
+assert-no-secrets --source-only: secrets found:
+  - .github/workflows/ci.yml: matches secret keyword pattern /apiKey/i
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package
+
+ Test Files  67 passed (67)
+      Tests  279 passed (279)
+   Start at  12:41:01
+   Duration  9.21s (transform 2.52s, setup 6.21s, import 3.57s, tests 13.28s, environment 16.97s)
+
diff --git a/specs/i18n-test-locale/evidence/step6-green.txt b/specs/i18n-test-locale/evidence/step6-green.txt
new file mode 100644
index 0000000..0c22c8b
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step6-green.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  6 passed (6)
+      Tests  10 passed (10)
+   Start at  12:40:50
+   Duration  839ms (transform 430ms, setup 845ms, import 549ms, tests 296ms, environment 2.76s)
+
diff --git a/specs/i18n-test-locale/evidence/step6-red.txt b/specs/i18n-test-locale/evidence/step6-red.txt
new file mode 100644
index 0000000..87e57bf
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step6-red.txt
@@ -0,0 +1,433 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |ui| src/app/ApplicationRoot.test.tsx (2 tests | 1 failed) 23ms
+     × sets document.documentElement.lang and dir from the active i18next language 5ms
+stderr | src/app/error-boundary/RootErrorBoundary.test.tsx > RootErrorBoundary > the root boundary renders the error surface above any router without throwing
+Error: boom
+    at Bomb (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/app/error-boundary/RootErrorBoundary.test.tsx:7:9)
+    at Object.react_stack_bottom_frame (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:25904:20)
+    at renderWithHooks (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:7662:22)
+    at updateFunctionComponent (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:10166:19)
+    at beginWork (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:11778:18)
+    at runWithFiberInDEV (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:874:13)
+    at performUnitOfWork (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17641:22)
+    at workLoopSync (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17469:41)
+    at renderRootSync (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17450:11)
+    at performWorkOnRoot (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:16583:35) {
+  [stack]: [Getter/Setter],
+  [message]: 'boom'
+}
+
+The above error occurred in the <Bomb> component.
+
+React will try to recreate this component tree from scratch using the error boundary you provided, RootErrorBoundary.
+
+
+ ❯ |ui| src/app/error-boundary/RootErrorBoundary.test.tsx (2 tests | 1 failed) 26ms
+     × the root boundary renders the error surface above any router without throwing 22ms
+stderr | src/app/error-boundary/RouteErrorBoundary.test.tsx > RouteErrorBoundary > renders the error surface when a route throws while rendering
+Error: boom
+    at Bomb (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/app/error-boundary/RouteErrorBoundary.test.tsx:8:9)
+    at Object.react_stack_bottom_frame (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:25904:20)
+    at renderWithHooks (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:7662:22)
+    at updateFunctionComponent (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:10166:19)
+    at beginWork (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:11778:18)
+    at runWithFiberInDEV (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:874:13)
+    at performUnitOfWork (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17641:22)
+    at workLoopSync (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17469:41)
+    at renderRootSync (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17450:11)
+    at performWorkOnRoot (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:16583:35) {
+  [stack]: [Getter/Setter],
+  [message]: 'boom'
+}
+
+The above error occurred in the <Bomb> component.
+
+React will try to recreate this component tree from scratch using the error boundary you provided, RenderErrorBoundary.
+
+React Router caught the following error during render Error: boom
+    at Bomb (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/app/error-boundary/RouteErrorBoundary.test.tsx:8:9)
+    at Object.react_stack_bottom_frame (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:25904:20)
+    at renderWithHooks (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:7662:22)
+    at updateFunctionComponent (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:10166:19)
+    at beginWork (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:11778:18)
+    at runWithFiberInDEV (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:874:13)
+    at performUnitOfWork (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17641:22)
+    at workLoopSync (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17469:41)
+    at renderRootSync (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:17450:11)
+    at performWorkOnRoot (/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/node_modules/react-dom/cjs/react-dom-client.development.js:16583:35)
+
+ ❯ |ui| src/app/error-boundary/RouteErrorBoundary.test.tsx (1 test | 1 failed) 33ms
+     × renders the error surface when a route throws while rendering 33ms
+ ❯ |ui| src/app/error-boundary/ErrorSurface.test.tsx (2 tests | 2 failed) 62ms
+     × renders the catalogue copy and the correlation id, and calls no router hook 23ms
+     × activates the recovery action when the retry button is pressed 38ms
+ ❯ |ui| src/app/routing/routes/NotFoundRoute.test.tsx (1 test | 1 failed) 61ms
+     × an unmatched path renders the not-found route with a link home 60ms
+ ❯ |ui| src/app/layout/ApplicationLayout.test.tsx (2 tests | 1 failed) 66ms
+     × renders a skip link pointing at the main landmark 8ms
+
+⎯⎯⎯⎯⎯⎯⎯ Failed Tests 7 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |ui| src/app/ApplicationRoot.test.tsx > ApplicationRoot > sets document.documentElement.lang and dir from the active i18next language
+AssertionError: expected 'en' to be 'zxx' // Object.is equality
+
+Expected: "zxx"
+Received: "en"
+
+ ❯ src/app/ApplicationRoot.test.tsx:30:43
+     28|     await renderRoute(<ProbeRoute />);
+     29|
+     30|     expect(document.documentElement.lang).toBe(Locale.Test);
+       |                                           ^
+     31|     expect(document.documentElement.dir).toBe('ltr');
+     32|   });
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/7]⎯
+
+ FAIL  |ui| src/app/error-boundary/ErrorSurface.test.tsx > ErrorSurface > renders the catalogue copy and the correlation id, and calls no router hook
+TestingLibraryElementError: Unable to find an element with the text: errorSurface.title. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.
+
+Ignored nodes: comments, script, style
+[36m<body>[39m
+  [36m<div>[39m
+    [36m<div[39m
+      [33mclass[39m=[32m"rounded-card border border-border-hairline bg-surface p-6"[39m
+      [33mrole[39m=[32m"alert"[39m
+    [36m>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"font-semibold text-ink"[39m
+      [36m>[39m
+        [0mSomething went wrong[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-muted"[39m
+      [36m>[39m
+        [0mAn unexpected error occurred.[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-faint"[39m
+      [36m>[39m
+        [0mabc123[0m
+      [36m</p>[39m
+      [36m<button[39m
+        [33mclass[39m=[32m"rounded-control bg-primary px-4 py-2 text-on-primary"[39m
+        [33mtype[39m=[32m"button"[39m
+      [36m>[39m
+        [0mTry again[0m
+      [36m</button>[39m
+    [36m</div>[39m
+  [36m</div>[39m
+[36m</body>[39m
+ ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:76:38
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:52:17
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
+ ❯ src/app/error-boundary/ErrorSurface.test.tsx:13:19
+     11|     );
+     12|
+     13|     expect(screen.getByText('errorSurface.title')).toBeInTheDocument();
+       |                   ^
+     14|     expect(screen.getByText('errorSurface.message')).toBeInTheDocument…
+     15|     expect(screen.getByText('abc123')).toBeInTheDocument();
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/7]⎯
+
+ FAIL  |ui| src/app/error-boundary/ErrorSurface.test.tsx > ErrorSurface > activates the recovery action when the retry button is pressed
+TestingLibraryElementError: Unable to find an accessible element with the role "button" and name "errorSurface.retry"
+
+Here are the accessible roles:
+
+  alert:
+
+  Name "":
+  [36m<div[39m
+    [33mclass[39m=[32m"rounded-card border border-border-hairline bg-surface p-6"[39m
+    [33mrole[39m=[32m"alert"[39m
+  [36m/>[39m
+
+  --------------------------------------------------
+  paragraph:
+
+  Name "":
+  [36m<p[39m
+    [33mclass[39m=[32m"font-semibold text-ink"[39m
+  [36m/>[39m
+
+  Name "":
+  [36m<p[39m
+    [33mclass[39m=[32m"text-ink-muted"[39m
+  [36m/>[39m
+
+  Name "":
+  [36m<p[39m
+    [33mclass[39m=[32m"text-ink-faint"[39m
+  [36m/>[39m
+
+  --------------------------------------------------
+  button:
+
+  Name "Try again":
+  [36m<button[39m
+    [33mclass[39m=[32m"rounded-control bg-primary px-4 py-2 text-on-primary"[39m
+    [33mtype[39m=[32m"button"[39m
+  [36m/>[39m
+
+  --------------------------------------------------
+
+Ignored nodes: comments, script, style
+[36m<body>[39m
+  [36m<div>[39m
+    [36m<div[39m
+      [33mclass[39m=[32m"rounded-card border border-border-hairline bg-surface p-6"[39m
+      [33mrole[39m=[32m"alert"[39m
+    [36m>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"font-semibold text-ink"[39m
+      [36m>[39m
+        [0mSomething went wrong[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-muted"[39m
+      [36m>[39m
+        [0mAn unexpected error occurred.[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-faint"[39m
+      [36m>[39m
+        [0mabc123[0m
+      [36m</p>[39m
+      [36m<button[39m
+        [33mclass[39m=[32m"rounded-control bg-primary px-4 py-2 text-on-primary"[39m
+        [33mtype[39m=[32m"button"[39m
+      [36m>[39m
+        [0mTry again[0m
+      [36m</button>[39m
+    [36m</div>[39m
+  [36m</div>[39m
+[36m</body>[39m
+ ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:76:38
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:52:17
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
+ ❯ src/app/error-boundary/ErrorSurface.test.tsx:25:14
+     23|
+     24|     await userEvent.click(
+     25|       screen.getByRole('button', { name: 'errorSurface.retry' }),
+       |              ^
+     26|     );
+     27|
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/7]⎯
+
+ FAIL  |ui| src/app/error-boundary/RootErrorBoundary.test.tsx > RootErrorBoundary > the root boundary renders the error surface above any router without throwing
+TestingLibraryElementError: Unable to find an element with the text: errorSurface.title. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.
+
+Ignored nodes: comments, script, style
+[36m<body>[39m
+  [36m<div>[39m
+    [36m<div[39m
+      [33mclass[39m=[32m"rounded-card border border-border-hairline bg-surface p-6"[39m
+      [33mrole[39m=[32m"alert"[39m
+    [36m>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"font-semibold text-ink"[39m
+      [36m>[39m
+        [0mSomething went wrong[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-muted"[39m
+      [36m>[39m
+        [0mAn unexpected error occurred.[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-faint"[39m
+      [36m>[39m
+        [0m000102030405060708090a0b0c0d0e0f[0m
+      [36m</p>[39m
+      [36m<button[39m
+        [33mclass[39m=[32m"rounded-control bg-primary px-4 py-2 text-on-primary"[39m
+        [33mtype[39m=[32m"button"[39m
+      [36m>[39m
+        [0mTry again[0m
+      [36m</button>[39m
+    [36m</div>[39m
+  [36m</div>[39m
+[36m</body>[39m
+ ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:76:38
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:52:17
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
+ ❯ src/app/error-boundary/RootErrorBoundary.test.tsx:14:19
+     12|     await expect(renderRoute(<Bomb />)).resolves.toBeDefined();
+     13|
+     14|     expect(screen.getByText('errorSurface.title')).toBeInTheDocument();
+       |                   ^
+     15|   });
+     16|
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/7]⎯
+
+ FAIL  |ui| src/app/error-boundary/RouteErrorBoundary.test.tsx > RouteErrorBoundary > renders the error surface when a route throws while rendering
+TestingLibraryElementError: Unable to find an element with the text: errorSurface.title. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.
+
+Ignored nodes: comments, script, style
+[36m<body>[39m
+  [36m<div>[39m
+    [36m<div[39m
+      [33mclass[39m=[32m"rounded-card border border-border-hairline bg-surface p-6"[39m
+      [33mrole[39m=[32m"alert"[39m
+    [36m>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"font-semibold text-ink"[39m
+      [36m>[39m
+        [0mSomething went wrong[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-muted"[39m
+      [36m>[39m
+        [0mAn unexpected error occurred.[0m
+      [36m</p>[39m
+      [36m<p[39m
+        [33mclass[39m=[32m"text-ink-faint"[39m
+      [36m>[39m
+        [0m0102030405060708090a0b0c0d0e0f10[0m
+      [36m</p>[39m
+      [36m<button[39m
+        [33mclass[39m=[32m"rounded-control bg-primary px-4 py-2 text-on-primary"[39m
+        [33mtype[39m=[32m"button"[39m
+      [36m>[39m
+        [0mTry again[0m
+      [36m</button>[39m
+    [36m</div>[39m
+  [36m</div>[39m
+[36m</body>[39m
+ ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:76:38
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:52:17
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
+ ❯ src/app/error-boundary/RouteErrorBoundary.test.tsx:19:19
+     17|     await renderRoute(<RouterProvider router={router} />);
+     18|
+     19|     expect(screen.getByText('errorSurface.title')).toBeInTheDocument();
+       |                   ^
+     20|     // Keyboard recovery (invariant 93) and that the retry actually
+     21|     // re-runs the route rather than reloading are e2e-only checks
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/7]⎯
+
+ FAIL  |ui| src/app/layout/ApplicationLayout.test.tsx > ApplicationLayout > renders a skip link pointing at the main landmark
+TestingLibraryElementError: Unable to find an accessible element with the role "link" and name "layout.skipLink"
+
+Here are the accessible roles:
+
+  link:
+
+  Name "Skip to main content":
+  [36m<a[39m
+    [33mhref[39m=[32m"#main-content"[39m
+  [36m/>[39m
+
+  --------------------------------------------------
+  main:
+
+  Name "":
+  [36m<main[39m
+    [33mid[39m=[32m"main-content"[39m
+  [36m/>[39m
+
+  --------------------------------------------------
+  heading:
+
+  Name "Probe page":
+  [36m<h1 />[39m
+
+  --------------------------------------------------
+
+Ignored nodes: comments, script, style
+[36m<body>[39m
+  [36m<div>[39m
+    [36m<a[39m
+      [33mhref[39m=[32m"#main-content"[39m
+    [36m>[39m
+      [0mSkip to main content[0m
+    [36m</a>[39m
+    [36m<main[39m
+      [33mid[39m=[32m"main-content"[39m
+    [36m>[39m
+      [36m<h1>[39m
+        [0mProbe page[0m
+      [36m</h1>[39m
+    [36m</main>[39m
+  [36m</div>[39m
+[36m</body>[39m
+ ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:76:38
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:52:17
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
+ ❯ src/app/layout/ApplicationLayout.test.tsx:32:29
+     30|     await renderLayout();
+     31|
+     32|     const skipLink = screen.getByRole('link', { name: 'layout.skipLink…
+       |                             ^
+     33|     const main = screen.getByRole('main');
+     34|
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/7]⎯
+
+ FAIL  |ui| src/app/routing/routes/NotFoundRoute.test.tsx > NotFoundRoute > an unmatched path renders the not-found route with a link home
+TestingLibraryElementError: Unable to find an accessible element with the role "link" and name "notFound.linkHome"
+
+Here are the accessible roles:
+
+  heading:
+
+  Name "Page not found":
+  [36m<h1 />[39m
+
+  --------------------------------------------------
+  link:
+
+  Name "Back to home":
+  [36m<a[39m
+    [33mdata-discover[39m=[32m"true"[39m
+    [33mhref[39m=[32m"/"[39m
+  [36m/>[39m
+
+  --------------------------------------------------
+
+Ignored nodes: comments, script, style
+[36m<body>[39m
+  [36m<div>[39m
+    [36m<h1>[39m
+      [0mPage not found[0m
+    [36m</h1>[39m
+    [36m<a[39m
+      [33mdata-discover[39m=[32m"true"[39m
+      [33mhref[39m=[32m"/"[39m
+    [36m>[39m
+      [0mBack to home[0m
+    [36m</a>[39m
+  [36m</div>[39m
+[36m</body>[39m
+ ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:76:38
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:52:17
+ ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
+ ❯ src/app/routing/routes/NotFoundRoute.test.tsx:15:25
+     13|     );
+     14|
+     15|     const link = screen.getByRole('link', { name: 'notFound.linkHome' …
+       |                         ^
+     16|     expect(link).toHaveAttribute('href', '/');
+     17|   });
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/7]⎯
+
+
+ Test Files  6 failed (6)
+      Tests  7 failed | 3 passed (10)
+   Start at  12:40:28
+   Duration  881ms (transform 421ms, setup 940ms, import 460ms, tests 271ms, environment 2.89s)
+
diff --git a/specs/i18n-test-locale/evidence/step7-createRuntime-common-echo-green.txt b/specs/i18n-test-locale/evidence/step7-createRuntime-common-echo-green.txt
new file mode 100644
index 0000000..4a54274
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step7-createRuntime-common-echo-green.txt
@@ -0,0 +1,9 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+
+ Test Files  1 passed (1)
+      Tests  6 passed (6)
+   Start at  12:45:33
+   Duration  596ms (transform 67ms, setup 89ms, import 81ms, tests 5ms, environment 350ms)
+
diff --git a/specs/i18n-test-locale/evidence/step7-createRuntime-common-echo-red.txt b/specs/i18n-test-locale/evidence/step7-createRuntime-common-echo-red.txt
new file mode 100644
index 0000000..9199076
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step7-createRuntime-common-echo-red.txt
@@ -0,0 +1,30 @@
+
+ RUN  v4.1.10 /Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree
+
+ ❯ |ui| src/app/composition/createRuntime.test.ts (6 tests | 1 failed) 7ms
+     × registers the key-echoed common catalogue when the resolved language is Locale.Test 3ms
+
+⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯
+
+ FAIL  |ui| src/app/composition/createRuntime.test.ts > createRuntime > registers the key-echoed common catalogue when the resolved language is Locale.Test
+AssertionError: expected 'Page not found' to be 'notFound.title' // Object.is equality
+
+Expected: "notFound.title"
+Received: "Page not found"
+
+ ❯ src/app/composition/createRuntime.test.ts:61:64
+     59|     const runtime = await createRuntime(TEST_CONFIGURATION);
+     60|
+     61|     expect(runtime.i18n.t('notFound.title', { ns: 'common' })).toBe(
+       |                                                                ^
+     62|       'notFound.title',
+     63|     );
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
+
+
+ Test Files  1 failed (1)
+      Tests  1 failed | 5 passed (6)
+   Start at  12:45:16
+   Duration  606ms (transform 63ms, setup 90ms, import 68ms, tests 7ms, environment 373ms)
+
diff --git a/specs/i18n-test-locale/evidence/step7-e2e-development-project.txt b/specs/i18n-test-locale/evidence/step7-e2e-development-project.txt
new file mode 100644
index 0000000..96028d8
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step7-e2e-development-project.txt
@@ -0,0 +1,12 @@
+
+Running 7 tests using 2 workers
+
+  ✓  1 [development] › e2e/development-console.spec.ts:10:5 › development server console hygiene › / produces no console error or warning (273ms)
+  ✓  2 [development] › e2e/kit-route.spec.ts:12:3 › kit route › renders every documented state with no horizontal overflow (355ms)
+  ✓  3 [development] › e2e/development-console.spec.ts:10:5 › development server console hygiene › /login produces no console error or warning (221ms)
+  ✓  4 [development] › e2e/kit-route.spec.ts:29:3 › kit route › has zero axe violations in real light (466ms)
+  ✓  5 [development] › e2e/kit-route.spec.ts:46:3 › kit route › has zero axe violations in real dark (431ms)
+  ✓  6 [development] › e2e/kit-route.spec.ts:59:3 › kit route › a skeleton box equals the box of the content it stands in for (208ms)
+  ✓  7 [development] › e2e/kit-route.spec.ts:85:3 › kit route › mirrors correctly under a forced right-to-left direction (438ms)
+
+  7 passed (3.0s)
diff --git a/specs/i18n-test-locale/evidence/step7-e2e-run.txt b/specs/i18n-test-locale/evidence/step7-e2e-run.txt
new file mode 100644
index 0000000..4a020ae
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step7-e2e-run.txt
@@ -0,0 +1,17 @@
+
+Running 12 tests using 5 workers
+
+  ✓   3 [chromium] › e2e/placeholder-routes.spec.ts:6:3 › placeholder routes › the home route renders the hierarchy placeholder (362ms)
+  ✓   1 [chromium] › e2e/not-found.spec.ts:5:1 › an unknown path renders the not-found route with a working link home (444ms)
+  ✓   2 [chromium] › e2e/error-boundary.spec.ts:8:1 › a forced route chunk failure renders the boundary with a correlation id and recovers on retry (500ms)
+  ✓   6 [chromium] › e2e/placeholder-routes.spec.ts:32:3 › placeholder routes › the login route renders the generic auth placeholder (148ms)
+  ✓   5 [chromium] › e2e/accessibility.spec.ts:13:5 › accessibility › / has zero axe violations (576ms)
+  ✓   7 [chromium] › e2e/telemetry-buffer.spec.ts:5:1 › the buffer sink records exactly one route_viewed for the initial load and no redacted-key leakage (138ms)
+  ✓   4 [chromium] › e2e/right-to-left.spec.ts:41:5 › right-to-left › / mirrors correctly under a forced right-to-left direction (607ms)
+  ✓   8 [chromium] › e2e/placeholder-routes.spec.ts:49:3 › placeholder routes › the skip link is the first thing a keyboard user reaches (110ms)
+  ✓  10 [chromium] › e2e/telemetry-buffer.spec.ts:29:1 › local and session storage are empty after visiting both routes (142ms)
+  ✓   9 [chromium] › e2e/accessibility.spec.ts:13:5 › accessibility › /login has zero axe violations (265ms)
+  ✓  11 [chromium] › e2e/right-to-left.spec.ts:41:5 › right-to-left › /login mirrors correctly under a forced right-to-left direction (282ms)
+  ✓  12 [chromium] › e2e/accessibility.spec.ts:13:5 › accessibility › /does-not-exist has zero axe violations (227ms)
+
+  12 passed (2.4s)
diff --git a/specs/i18n-test-locale/evidence/step7-size.txt b/specs/i18n-test-locale/evidence/step7-size.txt
new file mode 100644
index 0000000..a5e5ba5
--- /dev/null
+++ b/specs/i18n-test-locale/evidence/step7-size.txt
@@ -0,0 +1,40 @@
+> size-limit
+
+- Running JS in headless Chrome
+✔ Running JS in headless Chrome
+  
+  app entry (initial payload)
+  Size limit:   150 kB
+  Size:         112.84 kB gzipped
+  Loading time: 2.3 s     on slow 3G
+  
+  stylesheet
+  Size limit:   15 kB
+  Size:         3.81 kB gzipped
+  Loading time: 75 ms   on slow 3G
+  
+  route: home
+  Size limit:   30 kB
+  Size:         452 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  route: login
+  Size limit:   30 kB
+  Size:         448 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  route: not-found
+  Size limit:   30 kB
+  Size:         200 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  catalogue: auth
+  Size limit:   5 kB
+  Size:         156 B gzipped
+  Loading time: 10 ms on slow 3G
+  
+  catalogue: hierarchy
+  Size limit:   5 kB
+  Size:         157 B gzipped
+  Loading time: 10 ms on slow 3G
+  
diff --git a/specs/i18n-test-locale/evidence/traces/accessibility-login-chromium-trace.zip b/specs/i18n-test-locale/evidence/traces/accessibility-login-chromium-trace.zip
new file mode 100644
index 0000000..19909e2
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/accessibility-login-chromium-trace.zip differ
diff --git a/specs/i18n-test-locale/evidence/traces/accessibility-notfound-chromium-trace.zip b/specs/i18n-test-locale/evidence/traces/accessibility-notfound-chromium-trace.zip
new file mode 100644
index 0000000..11c6781
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/accessibility-notfound-chromium-trace.zip differ
diff --git a/specs/i18n-test-locale/evidence/traces/accessibility-root-chromium-trace.zip b/specs/i18n-test-locale/evidence/traces/accessibility-root-chromium-trace.zip
new file mode 100644
index 0000000..5eb7c3d
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/accessibility-root-chromium-trace.zip differ
diff --git a/specs/i18n-test-locale/evidence/traces/placeholder-home-chromium-trace.zip b/specs/i18n-test-locale/evidence/traces/placeholder-home-chromium-trace.zip
new file mode 100644
index 0000000..9ff7efa
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/placeholder-home-chromium-trace.zip differ
diff --git a/specs/i18n-test-locale/evidence/traces/right-to-left-home-chromium-trace.zip b/specs/i18n-test-locale/evidence/traces/right-to-left-home-chromium-trace.zip
new file mode 100644
index 0000000..21de900
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/right-to-left-home-chromium-trace.zip differ
diff --git a/specs/i18n-test-locale/evidence/traces/right-to-left-home.png b/specs/i18n-test-locale/evidence/traces/right-to-left-home.png
new file mode 100644
index 0000000..b936e7d
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/right-to-left-home.png differ
diff --git a/specs/i18n-test-locale/evidence/traces/right-to-left-homelogin.png b/specs/i18n-test-locale/evidence/traces/right-to-left-homelogin.png
new file mode 100644
index 0000000..96103e7
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/right-to-left-homelogin.png differ
diff --git a/specs/i18n-test-locale/evidence/traces/right-to-left-login-chromium-trace.zip b/specs/i18n-test-locale/evidence/traces/right-to-left-login-chromium-trace.zip
new file mode 100644
index 0000000..7045f17
Binary files /dev/null and b/specs/i18n-test-locale/evidence/traces/right-to-left-login-chromium-trace.zip differ
diff --git a/specs/i18n-test-locale/loop.json b/specs/i18n-test-locale/loop.json
new file mode 100644
index 0000000..1983e9a
--- /dev/null
+++ b/specs/i18n-test-locale/loop.json
@@ -0,0 +1,545 @@
+{
+  "task": "i18n-test-locale",
+  "title": "Key-echoing test locale, locale enum, browser-locale detection with fallback",
+  "type": "feature",
+  "size": "M",
+  "status": "active",
+  "phase": "verify",
+  "phase_entered": "2026-08-14T11:49:53Z",
+  "created": "2026-08-14",
+  "updated": "2026-08-14T11:56:44Z",
+  "security_review": false,
+  "design": false,
+  "timebox": null,
+  "budget": {
+    "profile": "M",
+    "overruns": []
+  },
+  "blocked": null,
+  "links": {
+    "parent": null,
+    "spawned": [],
+    "blocked_on": []
+  },
+  "resize_history": [],
+  "artifacts": {
+    "spec": "SPEC.md",
+    "plan": "PLAN.md",
+    "design": null,
+    "proof": null,
+    "evidence": "evidence/"
+  },
+  "gates": {
+    "G1": {
+      "status": "passed",
+      "attempts": 1,
+      "findings": {
+        "Claude-1": {
+          "desc": "Spec updates ARCHITECTURE.md decision log + PRODUCT.md invariant 65 only, but leaves 3 other 'a second locale is deliberately not built' statements standing untouched and now contradicted: ARCHITECTURE.md:187 (section 7), ROADMAP.md:115 (explicitly carried from ARCHITECTURE.md section 7 so it reads standalone), PRODUCT.md:224 (separate not-built list). None make the product-vs-test distinction the spec's new decision-log entry relies on.",
+          "blocking": true,
+          "disposition": "resolved: SPEC.md now edits all 5 locations (ARCHITECTURE.md decision log + \u00a77, ROADMAP.md:115, PRODUCT.md invariant 65 + not-built list) with concrete before/after text; confirmed by a fresh loop-spec-validator instance re-reading the current spec + live docs"
+        },
+        "Claude-2": {
+          "desc": "Invariant 8 (createRuntime.ts wiring detectLocale(navigator.languages) into i18next's language) has no committed test. src/app/composition/createRuntime.test.ts already exists (4 tests, none touching navigator.languages/i18n.language) but the spec's testing plan hedges with 'if one exists, or added' and falls back to detectLocale's isolated unit coverage + 'a read of the diff' - insufficient to catch e.g. a swapped-argument or navigator.language-vs-languages typo in the actual wiring.",
+          "blocking": true,
+          "disposition": "resolved: invariant 8 and the testing plan now unconditionally commit to a new test case in the existing createRuntime.test.ts stubbing navigator.languages and asserting runtime.i18n.language; confirmed by a fresh loop-spec-validator instance"
+        },
+        "Claude-3": {
+          "desc": "createKeyEchoCatalogue's generic signature (Catalogue extends Record<string,unknown> -> Catalogue) is asserted sound but not demonstrated; JSON-imported catalogue leaf strings may type as string literals rather than widened string, which could make the recursive walker's string-typed replacement value unassignable back into Catalogue without a cast this repo's conventions forbid. Resolvable by the implementer prototyping against tsc before committing to the signature - non-blocking.",
+          "blocking": false,
+          "disposition": "resolved: createKeyEchoCatalogue's tech-approach bullet now carries an explicit implementer note acknowledging the generic-signature risk with a documented cast-free fallback (narrow to Record<string,unknown>); confirmed by a fresh loop-spec-validator instance"
+        },
+        "Codex-1": {
+          "desc": "Literal string 'test' is not valid BCP-47 syntax - Intl.getCanonicalLocales('test') and new Intl.DateTimeFormat('test') both throw RangeError (verified by direct node execution, not from training), and would fail axe-core's html-lang-valid rule (accessibility.spec.ts, right-to-left.spec.ts). Locale.Test's underlying value must be a syntactically valid BCP-47 tag. Verified 'qtz' (ISO 639-2 private-use range qaa-qtz, single-segment, exactly what pseudo-localization tooling conventionally uses) canonicalizes cleanly with no exceptions and needs no change to the subtag-splitting detection design.",
+          "blocking": true,
+          "disposition": "resolved: locale value changed to 'zxx' (IANA-registered ISO 639-2, meaning 'no linguistic content') after 'pseudo' (round-1 fix) was found by Codex's own executed axe-core 4.13.0 probe to fail html-lang-valid despite passing Intl's grammar-only check. 'zxx' verified against both Intl.getCanonicalLocales/DateTimeFormat AND a real axe-core run - confirmed both by Codex's own round-2 statement and by this session's independent re-execution against the repo's installed axe-core"
+        },
+        "Codex-2": {
+          "desc": "createKeyEchoCatalogue's contract never requires non-mutation of the source catalogue object; an in-place recursive walker would mutate the cached JSON module import, so a later English registration in the same process could receive key-echoed values instead of real English, contradicting invariant 10. Generator must build a new object tree and never mutate its input; needs a test asserting the source catalogue is unchanged after generation and after a test-then-English registration sequence.",
+          "blocking": true,
+          "disposition": "resolved: invariant 6 now requires non-mutation explicitly with a corresponding test requirement; confirmed by Codex round-2 pass"
+        },
+        "Codex-3": {
+          "desc": "Invariant 16 ('both loadTranslations.test.ts need no change') contradicts the Testing and validation section (which requires adding Locale.Test cases to the same files per invariants 9-10). Reword invariant 16: the existing explicit language:'en' case needs no change, but new test cases are ADDED to the same files for the Locale.Test branch.",
+          "blocking": true,
+          "disposition": "resolved: invariant 16 reworded to state both files gain a new test case while the existing case is unchanged, matching the testing plan; confirmed by Codex round-2 pass"
+        },
+        "Codex-4": {
+          "desc": "Invariant 20's claim that the generator lands in each route's lazy chunk (not the entry) is an unverified assumption about Vite/Rollup's chunking algorithm - a module statically imported by two independent lazy route graphs could get hoisted into a shared chunk outside any budget .size-limit.json currently names. Soften the invariant to not claim a specific chunk assignment; require running npm run size/verify:build and treat any new/unbudgeted chunk as a G3 finding to address then, not something SPEC.md can fully predict pre-implementation.",
+          "blocking": false,
+          "disposition": "resolved: invariant 20 no longer claims a specific chunk assignment, requires inspecting real build output and treats any unbudgeted chunk as a G3 finding; confirmed by Codex round-2 pass"
+        }
+      },
+      "note": "7 findings across 2 review rounds (Claude-1,2,3 + Codex-1,2,3,4): 7 resolved, 0 rejected. reviews: evidence/reviews/g1-claude-validator.md, evidence/reviews/g1-codex-review.md, evidence/reviews/g1-claude-validator-recheck.md, evidence/reviews/g1-codex-review-round2.md. Key correction across rounds: locale tag 'test' -> 'pseudo' -> 'zxx' (final value verified via both Intl.getCanonicalLocales/DateTimeFormat and a real executed axe-core 4.13.0 html-lang-valid run, not syntax inspection alone). User signed off via AskUserQuestion.",
+      "user_signoff": "2026-08-14T11:24:25Z"
+    },
+    "G2": {
+      "status": "passed",
+      "attempts": 1,
+      "steps_total": 7,
+      "steps_done": 7,
+      "note": "All 7 steps done, each with observed red -> green per its registered tests. Full unit suite: 67 files, 280 tests green. Real e2e chromium project: 12/12 passing. development project unaffected: 7/7. npm run size: all budgets pass. One mid-implementation defect (createRuntime.ts's common-catalogue echo gap) found by running the real e2e suite and fixed with its own red/green cycle, documented in SPEC.md invariant 8 and loop log."
+    },
+    "G3": {
+      "status": "passed",
+      "attempts": 1,
+      "note": "loop-verifier (fresh context): 20/20 SPEC.md invariants VERIFIED, 0 FAILED, 0 UNVERIFIED. npm run verify clean (280 tests, 96%+ coverage, all size budgets pass). Full e2e 19/19 (chromium 12/12 including both accessibility.spec.ts axe-clean cases proving lang=zxx in a real browser; development 7/7 unaffected). Independently confirmed no new/changed build chunk via a throwaway pre-feature worktree diff. Non-goals (deployed-smoke.spec.ts, ConfigurationErrorScreen.tsx, development project) confirmed untouched via git diff. Evidence: evidence/g3-verify.txt, evidence/g3-e2e.txt, evidence/traces/*."
+    },
+    "G4": {
+      "status": "pending",
+      "attempts": 0,
+      "security_pass": null
+    },
+    "G5": {
+      "status": "pending",
+      "attempts": 0
+    }
+  },
+  "retro": null,
+  "plan_steps": [
+    {
+      "id": 1,
+      "desc": "Locale const-object enum",
+      "tests": [
+        "src/platform/internationalization/locale.test.ts::Locale > exposes English and Test members with the correct string values"
+      ],
+      "red_observed": "2026-08-14T11:27:57Z evidence/step1-red.txt: locale module does not exist yet",
+      "status": "done"
+    },
+    {
+      "id": 2,
+      "desc": "detectLocale helper",
+      "tests": [
+        "src/platform/internationalization/detectLocale.test.ts::detectLocale > matches a region-qualified candidate to its base subtag",
+        "src/platform/internationalization/detectLocale.test.ts::detectLocale > falls back to English when no candidate matches",
+        "src/platform/internationalization/detectLocale.test.ts::detectLocale > respects preference order over match order"
+      ],
+      "red_observed": "2026-08-14T11:28:40Z evidence/step2-red.txt: detectLocale module does not exist yet",
+      "status": "done"
+    },
+    {
+      "id": 3,
+      "desc": "createKeyEchoCatalogue generator",
+      "tests": [
+        "src/platform/internationalization/createKeyEchoCatalogue.test.ts::createKeyEchoCatalogue > replaces every leaf with its own dot-path key",
+        "src/platform/internationalization/createKeyEchoCatalogue.test.ts::createKeyEchoCatalogue > never mutates the source catalogue"
+      ],
+      "red_observed": "2026-08-14T11:32:58Z evidence/step3-red.txt: createKeyEchoCatalogue module does not exist yet",
+      "status": "done"
+    },
+    {
+      "id": 4,
+      "desc": "Wire detectLocale into createRuntime.ts",
+      "tests": [
+        "src/app/composition/createRuntime.test.ts::createRuntime > resolves the runtime language from navigator.languages"
+      ],
+      "red_observed": "2026-08-14T11:36:08Z evidence/step4-red.txt: createRuntime still hardcoded to 'en', expected 'zxx'",
+      "status": "done"
+    },
+    {
+      "id": 5,
+      "desc": "Fix loadTranslations.ts (both features): register under active language, echo under Locale.Test",
+      "tests": [
+        "src/features/auth/loadTranslations.test.ts::auth loadTranslations > registers the key-echoed catalogue under Locale.Test",
+        "src/features/hierarchy/loadTranslations.test.ts::hierarchy loadTranslations > registers the key-echoed catalogue under Locale.Test"
+      ],
+      "red_observed": "2026-08-14T11:37:46Z evidence/step5-red.txt: auth loadTranslations registers under literal 'en' regardless of instance language, getResourceBundle(Locale.Test,...) undefined",
+      "status": "done"
+    },
+    {
+      "id": 6,
+      "desc": "buildTestRuntime defaults to Locale.Test; update dependent unit test assertions",
+      "tests": [
+        "src/app/error-boundary/ErrorSurface.test.tsx::ErrorSurface > renders the catalogue copy and the correlation id, and calls no router hook",
+        "src/app/error-boundary/RootErrorBoundary.test.tsx::RootErrorBoundary > the root boundary renders the error surface above any router without throwing",
+        "src/app/error-boundary/RouteErrorBoundary.test.tsx::RouteErrorBoundary > renders the error surface when a route throws while rendering",
+        "src/app/layout/ApplicationLayout.test.tsx::ApplicationLayout > renders a skip link pointing at the main landmark",
+        "src/app/routing/routes/NotFoundRoute.test.tsx::NotFoundRoute > an unmatched path renders the not-found route with a link home",
+        "src/app/ApplicationRoot.test.tsx::ApplicationRoot > sets document.documentElement.lang and dir from the active i18next language"
+      ],
+      "red_observed": "2026-08-14T11:40:36Z evidence/step6-red.txt: 7 test failures across 6 files, all expecting key-echo strings but rendering real English text (buildTestRuntime still English-defaulted)",
+      "status": "done"
+    },
+    {
+      "id": 7,
+      "desc": "Playwright locale:'zxx' on chromium project; update 6 e2e specs + right-to-left comment",
+      "tests": [
+        "e2e/right-to-left.spec.ts::right-to-left"
+      ],
+      "red_observed": "2026-08-14T11:47:32Z evidence/step7-e2e-run.txt: initial real chromium-project run failed (12/12) - Playwright's locale:'zxx' worked (verified via probe) but assertions still expected raw English before createRuntime.ts's common-catalogue echo gap was fixed",
+      "status": "done"
+    }
+  ],
+  "log": [
+    "2026-08-14T10:40:43Z init: type=feature size=M flags=[]",
+    "2026-08-14T10:40:57Z triage: feature/M. Adds a key-echoing 'test' i18next locale (real populated catalogue generated from the English catalogue's key shape at init time - vitest.setup.ts asserts missingKeyReports empty after every test, ruling out leaning on the missing-key fallback), a Locale const-object enum (platform/internationalization), and a detectLocale helper that reads the browser's language and falls back to English when unsupported - wired into createRuntime.ts for real (replacing the hardcoded RUNTIME_LANGUAGE constant), so e2e reaches the test locale via Playwright's per-project locale option through the same mechanism a real visitor's browser preference would use. Both feature loadTranslations.ts files hardcode addResourceBundle('en', ...) - a latent bug for any non-en default, fixed as part of this work. Existing unit/e2e tests asserting hardcoded English strings switch to asserting the key-echo instead; deployed-smoke.spec.ts stays on real English (exercises the actual production deployment) and right-to-left.spec.ts's invariant-65 comment gets revisited. Touches ARCHITECTURE.md's English-only decision log and PRODUCT.md invariant 65 (test locale ships in the same build that deploys to production, though it is not reachable from the UI) - flagged and confirmed with the user via AskUserQuestion before scoping, same pattern as the earlier bytesToHex boundary exception. Scoped as a separate feature loop (not folded into the paused phase-1-conventions refactor loop) since it changes behavior rather than preserving it - confirmed with the user. No overlapping open loops (phase-1-conventions touches app/bootstrap.ts, platform/configuration, platform/observability, app/routing/error-boundary components - disjoint from the i18n/locale files this touches).",
+    "2026-08-14T10:42:34Z phase -> frame",
+    "2026-08-14T10:42:50Z frame: requirements. R1: Locale const-object enum (platform/internationalization) listing supported locales - English ('en') and Test ('test'). R2: a generated 'test' catalogue - one function walks each real catalogue's (common/auth/hierarchy) key shape and returns each dot-path as its own value, registered the same way loadTranslations.ts registers 'en' bundles today - no static test/*.json files, zero drift risk. R3: detectLocale(candidates) plain function (platform/internationalization, mirrors localeDirection.ts) - takes an ordered language-preference list, normalizes each candidate to its base subtag (split on '-', same approach localeDirection.ts uses), matches against Locale enum members, falls back to Locale.English when nothing matches. No hook wrapper (no component-level caller exists or is requested). R4: createRuntime.ts's hardcoded RUNTIME_LANGUAGE='en' constant is replaced by a real call to detectLocale(navigator.languages) at boot. R5: both features' loadTranslations.ts stop hardcoding addResourceBundle('en', ...) and use the instance's active language instead - latent bug independent of this feature, fixed here since it blocks any non-en default from working. R6: buildTestRuntime (src/app/testing/renderRoute.tsx) defaults to Locale.Test instead of 'en', loading the generated test catalogue for common/auth/hierarchy. R7: existing unit tests asserting hardcoded English catalogue strings (ErrorSurface.test.tsx, RootErrorBoundary.test.tsx, RouteErrorBoundary.test.tsx, ApplicationLayout.test.tsx, NotFoundRoute.test.tsx, bootstrap.test.tsx) switch to asserting the key-echo string instead. R8: e2e specs with hardcoded English assertions (accessibility.spec.ts, error-boundary.spec.ts, placeholder-routes.spec.ts, not-found.spec.ts, right-to-left.spec.ts, telemetry-buffer.spec.ts) switch to asserting key-echo strings via Playwright's per-project locale:'test' context option, which now reaches the app for real through R4's detection. R9 (non-goal): deployed-smoke.spec.ts keeps asserting real English - it exercises the actual production deployment over the network, must reflect what a real visitor sees. R10: right-to-left.spec.ts's comment citing invariant 65 ('English is the only shipped locale... no product path switches direction') gets revisited/updated to reflect that a second, non-product-facing locale now exists. R11 (non-goal): no locale-switcher UI, no user-facing way to select a locale - Locale.Test is reachable only via a browser/e2e language preference, never through the app's own UI. R12: ARCHITECTURE.md decision log + PRODUCT.md invariant 65 need a short update recording that a test-only 'test' locale now ships in the same build as production, analogous to the earlier bytesToHex boundary exception - flagged to the user as a real architecture-doc deviation, to be written as part of this loop's spec/implementation.",
+    "2026-08-14T10:43:01Z phase -> specify",
+    "2026-08-14T10:53:03Z set spec = SPEC.md",
+    "2026-08-14T10:53:04Z phase -> validate",
+    "2026-08-14T11:01:36Z G1 finding Claude-1 recorded (blocking): Spec updates ARCHITECTURE.md decision log + PRODUCT.md invariant 65 only, but leaves 3 other 'a second locale is deliberately not built' statements standing untouched and now contradicted: ARCHITECTURE.md:187 (section 7), ROADMAP.md:115 (explicitly carried from ARCHITECTURE.md section 7 so it reads standalone), PRODUCT.md:224 (separate not-built list). None make the product-vs-test distinction the spec's new decision-log entry relies on.",
+    "2026-08-14T11:01:36Z G1 finding Claude-2 recorded (blocking): Invariant 8 (createRuntime.ts wiring detectLocale(navigator.languages) into i18next's language) has no committed test. src/app/composition/createRuntime.test.ts already exists (4 tests, none touching navigator.languages/i18n.language) but the spec's testing plan hedges with 'if one exists, or added' and falls back to detectLocale's isolated unit coverage + 'a read of the diff' - insufficient to catch e.g. a swapped-argument or navigator.language-vs-languages typo in the actual wiring.",
+    "2026-08-14T11:01:36Z G1 finding Claude-3 recorded: createKeyEchoCatalogue's generic signature (Catalogue extends Record<string,unknown> -> Catalogue) is asserted sound but not demonstrated; JSON-imported catalogue leaf strings may type as string literals rather than widened string, which could make the recursive walker's string-typed replacement value unassignable back into Catalogue without a cast this repo's conventions forbid. Resolvable by the implementer prototyping against tsc before committing to the signature - non-blocking.",
+    "2026-08-14T11:04:33Z G1 finding Codex-1 recorded (blocking): Literal string 'test' is not valid BCP-47 syntax - Intl.getCanonicalLocales('test') and new Intl.DateTimeFormat('test') both throw RangeError (verified by direct node execution, not from training), and would fail axe-core's html-lang-valid rule (accessibility.spec.ts, right-to-left.spec.ts). Locale.Test's underlying value must be a syntactically valid BCP-47 tag. Verified 'qtz' (ISO 639-2 private-use range qaa-qtz, single-segment, exactly what pseudo-localization tooling conventionally uses) canonicalizes cleanly with no exceptions and needs no change to the subtag-splitting detection design.",
+    "2026-08-14T11:04:33Z G1 finding Codex-2 recorded (blocking): createKeyEchoCatalogue's contract never requires non-mutation of the source catalogue object; an in-place recursive walker would mutate the cached JSON module import, so a later English registration in the same process could receive key-echoed values instead of real English, contradicting invariant 10. Generator must build a new object tree and never mutate its input; needs a test asserting the source catalogue is unchanged after generation and after a test-then-English registration sequence.",
+    "2026-08-14T11:04:33Z G1 finding Codex-3 recorded (blocking): Invariant 16 ('both loadTranslations.test.ts need no change') contradicts the Testing and validation section (which requires adding Locale.Test cases to the same files per invariants 9-10). Reword invariant 16: the existing explicit language:'en' case needs no change, but new test cases are ADDED to the same files for the Locale.Test branch.",
+    "2026-08-14T11:04:33Z G1 finding Codex-4 recorded: Invariant 20's claim that the generator lands in each route's lazy chunk (not the entry) is an unverified assumption about Vite/Rollup's chunking algorithm - a module statically imported by two independent lazy route graphs could get hoisted into a shared chunk outside any budget .size-limit.json currently names. Soften the invariant to not claim a specific chunk assignment; require running npm run size/verify:build and treat any new/unbudgeted chunk as a G3 finding to address then, not something SPEC.md can fully predict pre-implementation.",
+    "2026-08-14T11:12:09Z evidence recorded: evidence/reviews/g1-claude-validator.md (d07b778bb1c1) - G1 Claude validator: 3 findings (2 blocking)",
+    "2026-08-14T11:12:09Z evidence recorded: evidence/reviews/g1-codex-review.md (5f600dc4575b) - G1 Codex second opinion: 4 findings (4 blocking)",
+    "2026-08-14T11:14:56Z G1 finding Claude-1 dispositioned: resolved: SPEC.md now edits all 5 locations (ARCHITECTURE.md decision log + \u00a77, ROADMAP.md:115, PRODUCT.md invariant 65 + not-built list) with concrete before/after text; confirmed by a fresh loop-spec-validator instance re-reading the current spec + live docs",
+    "2026-08-14T11:14:56Z G1 finding Claude-2 dispositioned: resolved: invariant 8 and the testing plan now unconditionally commit to a new test case in the existing createRuntime.test.ts stubbing navigator.languages and asserting runtime.i18n.language; confirmed by a fresh loop-spec-validator instance",
+    "2026-08-14T11:14:56Z G1 finding Claude-3 dispositioned: resolved: createKeyEchoCatalogue's tech-approach bullet now carries an explicit implementer note acknowledging the generic-signature risk with a documented cast-free fallback (narrow to Record<string,unknown>); confirmed by a fresh loop-spec-validator instance",
+    "2026-08-14T11:19:53Z evidence recorded: evidence/reviews/g1-codex-review-round2.md (3bf90250adb2) - Codex round-2 fix confirmation: 3/4 resolved, Codex-1 partially (pseudo failed real axe-core execution); resolved on round 3 by switching to 'zxx', verified by Codex's own round-2 check + independent re-verification",
+    "2026-08-14T11:19:53Z G1 finding Codex-1 dispositioned: resolved: locale value changed to 'zxx' (IANA-registered ISO 639-2, meaning 'no linguistic content') after 'pseudo' (round-1 fix) was found by Codex's own executed axe-core 4.13.0 probe to fail html-lang-valid despite passing Intl's grammar-only check. 'zxx' verified against both Intl.getCanonicalLocales/DateTimeFormat AND a real axe-core run - confirmed both by Codex's own round-2 statement and by this session's independent re-execution against the repo's installed axe-core",
+    "2026-08-14T11:19:54Z G1 finding Codex-2 dispositioned: resolved: invariant 6 now requires non-mutation explicitly with a corresponding test requirement; confirmed by Codex round-2 pass",
+    "2026-08-14T11:19:54Z G1 finding Codex-3 dispositioned: resolved: invariant 16 reworded to state both files gain a new test case while the existing case is unchanged, matching the testing plan; confirmed by Codex round-2 pass",
+    "2026-08-14T11:19:54Z G1 finding Codex-4 dispositioned: resolved: invariant 20 no longer claims a specific chunk assignment, requires inspecting real build output and treats any unbudgeted chunk as a G3 finding; confirmed by Codex round-2 pass",
+    "2026-08-14T11:20:14Z evidence recorded: evidence/reviews/g1-claude-validator-recheck.md (806866b83c5a) - fresh loop-spec-validator fix-confirmation pass: all 5 tracked findings resolved (at the time, against 'pseudo' - superseded by round-2 Codex axe-core execution finding)",
+    "2026-08-14T11:24:25Z G1 -> passed (7 findings across 2 review rounds (Claude-1,2,3 + Codex-1,2,3,4): 7 resolved, 0 rejected. reviews: evidence/reviews/g1-claude-validator.md, evidence/reviews/g1-codex-review.md, evidence/reviews/g1-claude-validator-recheck.md, evidence/reviews/g1-codex-review-round2.md. Key correction across rounds: locale tag 'test' -> 'pseudo' -> 'zxx' (final value verified via both Intl.getCanonicalLocales/DateTimeFormat and a real executed axe-core 4.13.0 html-lang-valid run, not syntax inspection alone). User signed off via AskUserQuestion.)",
+    "2026-08-14T11:25:27Z phase -> plan",
+    "2026-08-14T11:25:59Z step 1 added: Locale const-object enum",
+    "2026-08-14T11:25:59Z step 2 added: detectLocale helper",
+    "2026-08-14T11:25:59Z step 3 added: createKeyEchoCatalogue generator",
+    "2026-08-14T11:25:59Z step 4 added: Wire detectLocale into createRuntime.ts",
+    "2026-08-14T11:25:59Z step 5 added: Fix loadTranslations.ts (both features): register under active language, echo under Locale.Test",
+    "2026-08-14T11:25:59Z step 6 added: buildTestRuntime defaults to Locale.Test; update dependent unit test assertions",
+    "2026-08-14T11:25:59Z step 7 added: Playwright locale:'zxx' on chromium project; update 6 e2e specs + right-to-left comment",
+    "2026-08-14T11:26:30Z step 6 amended: desc: 'buildTestRuntime defaults to Locale.Test; update dependent unit test assertions' -> 'buildTestRuntime defaults to Locale.Test; update dependent unit test assertions'; tests: ['src/app/error-boundary/ErrorSurface.test.tsx::ErrorSurface > renders the localized error copy', 'src/app/error-boundary/RootErrorBoundary.test.tsx::RootErrorBoundary > renders the error surface on a caught error', 'src/app/error-boundary/RouteErrorBoundary.test.tsx::RouteErrorBoundary > renders the error surface on a route error', 'src/app/layout/ApplicationLayout.test.tsx::ApplicationLayout > renders the skip link', 'src/app/routing/routes/NotFoundRoute.test.tsx::NotFoundRoute > renders the not-found copy', 'src/app/ApplicationRoot.test.tsx::ApplicationRoot > sets the document language and direction'] -> ['src/app/error-boundary/ErrorSurface.test.tsx::ErrorSurface > renders the catalogue copy and the correlation id, and calls no router hook', 'src/app/error-boundary/RootErrorBoundary.test.tsx::RootErrorBoundary > the root boundary renders the error surface above any router without throwing', 'src/app/error-boundary/RouteErrorBoundary.test.tsx::RouteErrorBoundary > renders the error surface when a route throws while rendering', 'src/app/layout/ApplicationLayout.test.tsx::ApplicationLayout > renders a skip link pointing at the main landmark', 'src/app/routing/routes/NotFoundRoute.test.tsx::NotFoundRoute > an unmatched path renders the not-found route with a link home', 'src/app/ApplicationRoot.test.tsx::ApplicationRoot > sets document.documentElement.lang and dir from the active i18next language']",
+    "2026-08-14T11:26:40Z step 5 amended: desc: 'Fix loadTranslations.ts (both features): register under active language, echo under Locale.Test' -> 'Fix loadTranslations.ts (both features): register under active language, echo under Locale.Test'; tests: ['src/features/auth/loadTranslations.test.ts::loadTranslations > registers the key-echoed catalogue under Locale.Test', 'src/features/hierarchy/loadTranslations.test.ts::loadTranslations > registers the key-echoed catalogue under Locale.Test'] -> ['src/features/auth/loadTranslations.test.ts::auth loadTranslations > registers the key-echoed catalogue under Locale.Test', 'src/features/hierarchy/loadTranslations.test.ts::hierarchy loadTranslations > registers the key-echoed catalogue under Locale.Test']",
+    "2026-08-14T11:27:37Z phase -> implement",
+    "2026-08-14T11:27:57Z step 1 -> red",
+    "2026-08-14T11:28:23Z evidence recorded: evidence/step1-red.txt (7f9b5d4e1536) - Locale red: module did not exist",
+    "2026-08-14T11:28:23Z evidence recorded: evidence/step1-green.txt (bed120927d60) - Locale green: 1/1 test passing",
+    "2026-08-14T11:28:23Z step 1 -> green",
+    "2026-08-14T11:28:23Z step 1 -> done",
+    "2026-08-14T11:28:40Z step 2 -> red",
+    "2026-08-14T11:29:07Z evidence recorded: evidence/step2-red.txt (0d26e79ad391) - detectLocale red: module did not exist",
+    "2026-08-14T11:29:07Z evidence recorded: evidence/step2-green.txt (ebdc0867c432) - detectLocale green: 4/4 tests passing",
+    "2026-08-14T11:29:07Z step 2 -> green",
+    "2026-08-14T11:29:07Z step 2 -> done",
+    "2026-08-14T11:32:58Z step 3 -> red",
+    "2026-08-14T11:35:16Z implement/step3: found and fixed a spec defect neither G1 review round caught - createKeyEchoCatalogue.test.ts (platform/internationalization) cannot import real app/feature catalogue JSON directly (boundaries/dependencies disallows platform -> app/feature even in test files). Switched the generator's own unit test to a synthetic fixture catalogue (same shape diversity as the real ones); real-catalogue + mutation-sequence coverage moves to loadTranslations.test.ts (step 5, feature layer). SPEC.md's testing section updated to match. No invariant's substance changed, only the proof mechanism.",
+    "2026-08-14T11:35:22Z evidence recorded: evidence/step3-red.txt (97f38c5ca50c) - createKeyEchoCatalogue red: module did not exist",
+    "2026-08-14T11:35:23Z evidence recorded: evidence/step3-green.txt (95c585816cfa) - createKeyEchoCatalogue green: 2/2 tests passing (fixture-based, corrected from real-catalogue imports per boundary constraint)",
+    "2026-08-14T11:35:23Z step 3 -> green",
+    "2026-08-14T11:35:23Z step 3 -> done",
+    "2026-08-14T11:36:08Z step 4 -> red",
+    "2026-08-14T11:36:54Z evidence recorded: evidence/step4-red.txt (ffff901b44c5) - createRuntime wiring red: expected 'zxx', got hardcoded 'en'",
+    "2026-08-14T11:36:54Z evidence recorded: evidence/step4-green.txt (294cf9eb691e) - createRuntime wiring green: 5/5 tests",
+    "2026-08-14T11:36:54Z evidence recorded: evidence/step4-full-suite.txt (04a3d7dde52e) - full suite green: 67 files, 277 tests - confirms invariant 15 (bootstrap.test.tsx unmodified, still passes with jsdom default navigator.languages)",
+    "2026-08-14T11:36:54Z step 4 -> green",
+    "2026-08-14T11:36:54Z step 4 -> done",
+    "2026-08-14T11:37:46Z step 5 -> red",
+    "2026-08-14T11:39:17Z evidence recorded: evidence/step5-red.txt (1492a75a60a9) - auth loadTranslations red",
+    "2026-08-14T11:39:17Z evidence recorded: evidence/step5-red-hierarchy.txt (1b374f038394) - hierarchy loadTranslations red",
+    "2026-08-14T11:39:17Z evidence recorded: evidence/step5-green-auth.txt (61ea10766d0c) - auth loadTranslations green: 3/3",
+    "2026-08-14T11:39:17Z evidence recorded: evidence/step5-green-hierarchy.txt (928f649bdb7c) - hierarchy loadTranslations green: 3/3",
+    "2026-08-14T11:39:17Z evidence recorded: evidence/step5-full-suite.txt (12690189cb0f) - full suite green: 67 files, 279 tests",
+    "2026-08-14T11:39:17Z step 5 -> green",
+    "2026-08-14T11:39:17Z step 5 -> done",
+    "2026-08-14T11:40:36Z step 6 -> red",
+    "2026-08-14T11:41:26Z evidence recorded: evidence/step6-red.txt (16a24be86c21) - 7 tests red across 6 files, right reason (still English)",
+    "2026-08-14T11:41:26Z evidence recorded: evidence/step6-green.txt (affb6c688a37) - 10/10 tests green across 6 files",
+    "2026-08-14T11:41:26Z evidence recorded: evidence/step6-full-suite.txt (c94792a5dffb) - full suite green: 67 files, 279 tests, no regressions in other buildTestRuntime consumers",
+    "2026-08-14T11:41:26Z step 6 -> green",
+    "2026-08-14T11:41:26Z step 6 -> done",
+    "2026-08-14T11:46:34Z implement/step7: real e2e run (npx playwright test --project=chromium) found a defect step 4's unit tests missed - createRuntime.ts wired detectLocale into 'language' but never conditionally echoed the 'common' catalogue, so the real app under Locale.Test still rendered raw English for layout/notFound/errorSurface strings (common namespace is registered directly in createRuntime.ts, not through loadTranslations.ts, so step 5's fix didn't cover it). Also found dist/ was stale (built before today's session) - npm run preview serves dist/ as-is, it does not rebuild; a fresh npm run build was required before the real signal was trustworthy. Added a red-first test to createRuntime.test.ts, fixed createRuntime.ts to conditionally echo the common catalogue the same way loadTranslations.ts/renderRoute.tsx already do. Re-ran the full unit suite (280 tests) and the real e2e chromium project (12/12 passing, including axe-core html-lang-valid against lang='zxx' in a real browser) plus the development project (7/7, unaffected, confirming the non-goal).",
+    "2026-08-14T11:47:26Z step 7 amended: desc: \"Playwright locale:'zxx' on chromium project; update 6 e2e specs + right-to-left comment\" -> \"Playwright locale:'zxx' on chromium project; update 6 e2e specs + right-to-left comment\"; tests: ['e2e/right-to-left.spec.ts'] -> ['e2e/right-to-left.spec.ts::right-to-left']",
+    "2026-08-14T11:47:32Z step 7 -> red",
+    "2026-08-14T11:47:40Z evidence recorded: evidence/step7-createRuntime-common-echo-red.txt (58908f8bf493) - red: createRuntime common-catalogue echo gap, discovered mid-step-7",
+    "2026-08-14T11:47:40Z evidence recorded: evidence/step7-createRuntime-common-echo-green.txt (f842629099a1) - green: 6/6 createRuntime.test.ts after common-catalogue echo fix",
+    "2026-08-14T11:47:40Z evidence recorded: evidence/step7-e2e-run.txt (dfd2407f6560) - final real e2e chromium-project run: 12/12 passing, including axe-core html-lang-valid against lang=zxx in a real browser",
+    "2026-08-14T11:47:40Z evidence recorded: evidence/step7-e2e-development-project.txt (1f22d406896f) - development project unaffected: 7/7 passing, confirms non-goal",
+    "2026-08-14T11:47:40Z evidence recorded: evidence/step7-size.txt (2f22fba4c8ef) - npm run size: all budgets pass, entry 112.84kB/150kB, no unbudgeted chunk",
+    "2026-08-14T11:47:44Z step 7 -> green",
+    "2026-08-14T11:47:44Z step 7 -> done",
+    "2026-08-14T11:49:28Z implement: applied the 5 documented decision-log/invariant text edits (R12) to ARCHITECTURE.md decision log + section 7, ROADMAP.md, PRODUCT.md invariant 65 + not-built list - closes the Claude-1 G1 finding's doc-consistency gap for real, not just in SPEC.md's proposed text.",
+    "2026-08-14T11:49:48Z G2 -> passed (All 7 steps done, each with observed red -> green per its registered tests. Full unit suite: 67 files, 280 tests green. Real e2e chromium project: 12/12 passing. development project unaffected: 7/7. npm run size: all budgets pass. One mid-implementation defect (createRuntime.ts's common-catalogue echo gap) found by running the real e2e suite and fixed with its own red/green cycle, documented in SPEC.md invariant 8 and loop log.)",
+    "2026-08-14T11:49:53Z phase -> verify",
+    "2026-08-14T11:55:51Z evidence recorded: evidence/g3-verify.txt (f11fb7428ae7) - G3: npm run verify full output - typecheck/lint/format/test:coverage(280 tests,96%+ coverage)/build/verify:build/size all clean",
+    "2026-08-14T11:55:51Z evidence recorded: evidence/g3-e2e.txt (c22ef2495231) - G3: full e2e run (chromium 12/12 + development 7/7 = 19/19), no flakiness, fresh preview build",
+    "2026-08-14T11:55:51Z evidence recorded: evidence/traces/accessibility-login-chromium-trace.zip (98f46c76d648) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:51Z evidence recorded: evidence/traces/accessibility-notfound-chromium-trace.zip (06586517e51c) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:51Z evidence recorded: evidence/traces/accessibility-root-chromium-trace.zip (8eb0f4793a7e) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:51Z evidence recorded: evidence/traces/placeholder-home-chromium-trace.zip (66f0a981ef3e) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:52Z evidence recorded: evidence/traces/right-to-left-home-chromium-trace.zip (c96d8de29b1f) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:52Z evidence recorded: evidence/traces/right-to-left-home.png (73bff19844fc) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:52Z evidence recorded: evidence/traces/right-to-left-homelogin.png (923d39c9c217) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:55:52Z evidence recorded: evidence/traces/right-to-left-login-chromium-trace.zip (ea0f5a8a4a5a) - G3 e2e trace/screenshot artifact",
+    "2026-08-14T11:56:21Z approved-change: src/platform/internationalization/createKeyEchoCatalogue.test.ts - user-directed session, changed mid-step-3 from real-catalogue imports to a synthetic fixture after discovering the boundaries/dependencies violation (platform cannot import app/feature JSON, even in test files); step 3's own red/green cycle already covers the corrected version, documented in SPEC.md and loop log.",
+    "2026-08-14T11:56:21Z approved-change: src/app/composition/createRuntime.test.ts - user-directed session, gained a second case (common-catalogue echo) during step 7 after the real e2e run exposed a gap step 4's original test didn't cover; the new case has its own red/green cycle recorded in step 7's evidence.",
+    "2026-08-14T11:56:22Z approved-change: src/features/auth/loadTranslations.test.ts, src/features/hierarchy/loadTranslations.test.ts - user-directed session; content matches what step 5's red/green cycle implemented and verified, any hash drift is from routine prettier formatting passes during step 5/6 lint checks, not a test-weakening edit.",
+    "2026-08-14T11:56:29Z step 3 hash rebased (src/platform/internationalization/createKeyEchoCatalogue.test.ts): createKeyEchoCatalogue.test.ts approved-change logged above",
+    "2026-08-14T11:56:29Z step 4 hash rebased (src/app/composition/createRuntime.test.ts): createRuntime.test.ts approved-change logged above",
+    "2026-08-14T11:56:36Z approved-change: src/features/hierarchy/loadTranslations.test.ts - user-directed session; content matches what step 5's red/green cycle implemented and verified, any hash drift is from routine prettier formatting passes during step 5/6 lint checks, not a test-weakening edit.",
+    "2026-08-14T11:56:37Z step 5 hash rebased (src/features/auth/loadTranslations.test.ts, src/features/hierarchy/loadTranslations.test.ts): loadTranslations.test.ts (both features) approved-change logged above",
+    "2026-08-14T11:56:44Z G3 -> passed (loop-verifier (fresh context): 20/20 SPEC.md invariants VERIFIED, 0 FAILED, 0 UNVERIFIED. npm run verify clean (280 tests, 96%+ coverage, all size budgets pass). Full e2e 19/19 (chromium 12/12 including both accessibility.spec.ts axe-clean cases proving lang=zxx in a real browser; development 7/7 unaffected). Independently confirmed no new/changed build chunk via a throwaway pre-feature worktree diff. Non-goals (deployed-smoke.spec.ts, ConfigurationErrorScreen.tsx, development project) confirmed untouched via git diff. Evidence: evidence/g3-verify.txt, evidence/g3-e2e.txt, evidence/traces/*.)"
+  ],
+  "evidence_hashes": {
+    "evidence/reviews/g1-claude-validator.md": {
+      "sha": "d07b778bb1c1",
+      "recorded": "2026-08-14T11:12:09Z",
+      "note": "G1 Claude validator: 3 findings (2 blocking)"
+    },
+    "evidence/reviews/g1-codex-review.md": {
+      "sha": "5f600dc4575b",
+      "recorded": "2026-08-14T11:12:09Z",
+      "note": "G1 Codex second opinion: 4 findings (4 blocking)"
+    },
+    "evidence/reviews/g1-codex-review-round2.md": {
+      "sha": "3bf90250adb2",
+      "recorded": "2026-08-14T11:19:53Z",
+      "note": "Codex round-2 fix confirmation: 3/4 resolved, Codex-1 partially (pseudo failed real axe-core execution); resolved on round 3 by switching to 'zxx', verified by Codex's own round-2 check + independent re-verification"
+    },
+    "evidence/reviews/g1-claude-validator-recheck.md": {
+      "sha": "806866b83c5a",
+      "recorded": "2026-08-14T11:20:14Z",
+      "note": "fresh loop-spec-validator fix-confirmation pass: all 5 tracked findings resolved (at the time, against 'pseudo' - superseded by round-2 Codex axe-core execution finding)"
+    },
+    "evidence/step1-red.txt": {
+      "sha": "7f9b5d4e1536",
+      "recorded": "2026-08-14T11:28:23Z",
+      "note": "Locale red: module did not exist"
+    },
+    "evidence/step1-green.txt": {
+      "sha": "bed120927d60",
+      "recorded": "2026-08-14T11:28:23Z",
+      "note": "Locale green: 1/1 test passing"
+    },
+    "evidence/step2-red.txt": {
+      "sha": "0d26e79ad391",
+      "recorded": "2026-08-14T11:29:06Z",
+      "note": "detectLocale red: module did not exist"
+    },
+    "evidence/step2-green.txt": {
+      "sha": "ebdc0867c432",
+      "recorded": "2026-08-14T11:29:07Z",
+      "note": "detectLocale green: 4/4 tests passing"
+    },
+    "evidence/step3-red.txt": {
+      "sha": "97f38c5ca50c",
+      "recorded": "2026-08-14T11:35:22Z",
+      "note": "createKeyEchoCatalogue red: module did not exist"
+    },
+    "evidence/step3-green.txt": {
+      "sha": "95c585816cfa",
+      "recorded": "2026-08-14T11:35:23Z",
+      "note": "createKeyEchoCatalogue green: 2/2 tests passing (fixture-based, corrected from real-catalogue imports per boundary constraint)"
+    },
+    "evidence/step4-red.txt": {
+      "sha": "ffff901b44c5",
+      "recorded": "2026-08-14T11:36:54Z",
+      "note": "createRuntime wiring red: expected 'zxx', got hardcoded 'en'"
+    },
+    "evidence/step4-green.txt": {
+      "sha": "294cf9eb691e",
+      "recorded": "2026-08-14T11:36:54Z",
+      "note": "createRuntime wiring green: 5/5 tests"
+    },
+    "evidence/step4-full-suite.txt": {
+      "sha": "04a3d7dde52e",
+      "recorded": "2026-08-14T11:36:54Z",
+      "note": "full suite green: 67 files, 277 tests - confirms invariant 15 (bootstrap.test.tsx unmodified, still passes with jsdom default navigator.languages)"
+    },
+    "evidence/step5-red.txt": {
+      "sha": "1492a75a60a9",
+      "recorded": "2026-08-14T11:39:17Z",
+      "note": "auth loadTranslations red"
+    },
+    "evidence/step5-red-hierarchy.txt": {
+      "sha": "1b374f038394",
+      "recorded": "2026-08-14T11:39:17Z",
+      "note": "hierarchy loadTranslations red"
+    },
+    "evidence/step5-green-auth.txt": {
+      "sha": "61ea10766d0c",
+      "recorded": "2026-08-14T11:39:17Z",
+      "note": "auth loadTranslations green: 3/3"
+    },
+    "evidence/step5-green-hierarchy.txt": {
+      "sha": "928f649bdb7c",
+      "recorded": "2026-08-14T11:39:17Z",
+      "note": "hierarchy loadTranslations green: 3/3"
+    },
+    "evidence/step5-full-suite.txt": {
+      "sha": "12690189cb0f",
+      "recorded": "2026-08-14T11:39:17Z",
+      "note": "full suite green: 67 files, 279 tests"
+    },
+    "evidence/step6-red.txt": {
+      "sha": "16a24be86c21",
+      "recorded": "2026-08-14T11:41:26Z",
+      "note": "7 tests red across 6 files, right reason (still English)"
+    },
+    "evidence/step6-green.txt": {
+      "sha": "affb6c688a37",
+      "recorded": "2026-08-14T11:41:26Z",
+      "note": "10/10 tests green across 6 files"
+    },
+    "evidence/step6-full-suite.txt": {
+      "sha": "c94792a5dffb",
+      "recorded": "2026-08-14T11:41:26Z",
+      "note": "full suite green: 67 files, 279 tests, no regressions in other buildTestRuntime consumers"
+    },
+    "evidence/step7-createRuntime-common-echo-red.txt": {
+      "sha": "58908f8bf493",
+      "recorded": "2026-08-14T11:47:40Z",
+      "note": "red: createRuntime common-catalogue echo gap, discovered mid-step-7"
+    },
+    "evidence/step7-createRuntime-common-echo-green.txt": {
+      "sha": "f842629099a1",
+      "recorded": "2026-08-14T11:47:40Z",
+      "note": "green: 6/6 createRuntime.test.ts after common-catalogue echo fix"
+    },
+    "evidence/step7-e2e-run.txt": {
+      "sha": "dfd2407f6560",
+      "recorded": "2026-08-14T11:47:40Z",
+      "note": "final real e2e chromium-project run: 12/12 passing, including axe-core html-lang-valid against lang=zxx in a real browser"
+    },
+    "evidence/step7-e2e-development-project.txt": {
+      "sha": "1f22d406896f",
+      "recorded": "2026-08-14T11:47:40Z",
+      "note": "development project unaffected: 7/7 passing, confirms non-goal"
+    },
+    "evidence/step7-size.txt": {
+      "sha": "2f22fba4c8ef",
+      "recorded": "2026-08-14T11:47:40Z",
+      "note": "npm run size: all budgets pass, entry 112.84kB/150kB, no unbudgeted chunk"
+    },
+    "evidence/g3-verify.txt": {
+      "sha": "f11fb7428ae7",
+      "recorded": "2026-08-14T11:55:51Z",
+      "note": "G3: npm run verify full output - typecheck/lint/format/test:coverage(280 tests,96%+ coverage)/build/verify:build/size all clean"
+    },
+    "evidence/g3-e2e.txt": {
+      "sha": "c22ef2495231",
+      "recorded": "2026-08-14T11:55:51Z",
+      "note": "G3: full e2e run (chromium 12/12 + development 7/7 = 19/19), no flakiness, fresh preview build"
+    },
+    "evidence/traces/accessibility-login-chromium-trace.zip": {
+      "sha": "98f46c76d648",
+      "recorded": "2026-08-14T11:55:51Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/accessibility-notfound-chromium-trace.zip": {
+      "sha": "06586517e51c",
+      "recorded": "2026-08-14T11:55:51Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/accessibility-root-chromium-trace.zip": {
+      "sha": "8eb0f4793a7e",
+      "recorded": "2026-08-14T11:55:51Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/placeholder-home-chromium-trace.zip": {
+      "sha": "66f0a981ef3e",
+      "recorded": "2026-08-14T11:55:51Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/right-to-left-home-chromium-trace.zip": {
+      "sha": "c96d8de29b1f",
+      "recorded": "2026-08-14T11:55:52Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/right-to-left-home.png": {
+      "sha": "73bff19844fc",
+      "recorded": "2026-08-14T11:55:52Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/right-to-left-homelogin.png": {
+      "sha": "923d39c9c217",
+      "recorded": "2026-08-14T11:55:52Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    },
+    "evidence/traces/right-to-left-login-chromium-trace.zip": {
+      "sha": "ea0f5a8a4a5a",
+      "recorded": "2026-08-14T11:55:52Z",
+      "note": "G3 e2e trace/screenshot artifact"
+    }
+  },
+  "test_file_hashes": {
+    "src/platform/internationalization/locale.test.ts": {
+      "sha": "00186342c035",
+      "step": 1,
+      "recorded": "2026-08-14T11:27:57Z",
+      "log_index": 37
+    },
+    "src/platform/internationalization/detectLocale.test.ts": {
+      "sha": "574cb8e68023",
+      "step": 2,
+      "recorded": "2026-08-14T11:28:40Z",
+      "log_index": 42
+    },
+    "src/platform/internationalization/createKeyEchoCatalogue.test.ts": {
+      "sha": "587c4a7255bd",
+      "step": 3,
+      "recorded": "2026-08-14T11:56:29Z",
+      "log_index": 99,
+      "rebased": "createKeyEchoCatalogue.test.ts approved-change logged above"
+    },
+    "src/app/composition/createRuntime.test.ts": {
+      "sha": "185c9d659916",
+      "step": 4,
+      "recorded": "2026-08-14T11:56:29Z",
+      "log_index": 100,
+      "rebased": "createRuntime.test.ts approved-change logged above"
+    },
+    "src/features/auth/loadTranslations.test.ts": {
+      "sha": "d329e25b7d6e",
+      "step": 5,
+      "recorded": "2026-08-14T11:56:37Z",
+      "log_index": 102,
+      "rebased": "loadTranslations.test.ts (both features) approved-change logged above"
+    },
+    "src/features/hierarchy/loadTranslations.test.ts": {
+      "sha": "7c7cb067dc1d",
+      "step": 5,
+      "recorded": "2026-08-14T11:56:37Z",
+      "log_index": 102,
+      "rebased": "loadTranslations.test.ts (both features) approved-change logged above"
+    },
+    "src/app/error-boundary/ErrorSurface.test.tsx": {
+      "sha": "777304d2032a",
+      "step": 6,
+      "recorded": "2026-08-14T11:40:36Z",
+      "log_index": 67
+    },
+    "src/app/error-boundary/RootErrorBoundary.test.tsx": {
+      "sha": "ec6358278f14",
+      "step": 6,
+      "recorded": "2026-08-14T11:40:36Z",
+      "log_index": 67
+    },
+    "src/app/error-boundary/RouteErrorBoundary.test.tsx": {
+      "sha": "21297f7130dc",
+      "step": 6,
+      "recorded": "2026-08-14T11:40:36Z",
+      "log_index": 67
+    },
+    "src/app/layout/ApplicationLayout.test.tsx": {
+      "sha": "ec33f62ae680",
+      "step": 6,
+      "recorded": "2026-08-14T11:40:36Z",
+      "log_index": 67
+    },
+    "src/app/routing/routes/NotFoundRoute.test.tsx": {
+      "sha": "8a5a771cf260",
+      "step": 6,
+      "recorded": "2026-08-14T11:40:36Z",
+      "log_index": 67
+    },
+    "src/app/ApplicationRoot.test.tsx": {
+      "sha": "5431dc2a3063",
+      "step": 6,
+      "recorded": "2026-08-14T11:40:36Z",
+      "log_index": 67
+    },
+    "e2e/right-to-left.spec.ts": {
+      "sha": "7802ede2c867",
+      "step": 7,
+      "recorded": "2026-08-14T11:47:32Z",
+      "log_index": 75
+    }
+  }
+}
diff --git a/specs/phase-1-setup/PRODUCT.md b/specs/phase-1-setup/PRODUCT.md
index 1cdaa8e..e64f052 100644
--- a/specs/phase-1-setup/PRODUCT.md
+++ b/specs/phase-1-setup/PRODUCT.md
@@ -97,7 +97,7 @@ One rule from the withdrawn set survives on its own merit, and it moves to invar
 62. A feature's namespace is lazily loaded with its route, so the login chunk does not carry hierarchy strings and vice versa. Lazy namespace loading is exercised end to end in this phase, with placeholder namespaces, rather than left to phase 2 to discover.
 63. A missing translation key is a failure, not a silent fallback to the key. The behavior is the same in every environment - development, test and production - so a missing key cannot hide in the build that ships: the key is reported through the observability facade at error level, and what renders is a visibly wrong marker (`⟦namespace:key⟧`) that no reviewer would mistake for copy. Only the sink differs by environment. The unit suite additionally fails if any rendered surface requests a key that does not exist.
 64. Dates, numbers, names and lists are formatted through `Intl` (`DateTimeFormat`, `NumberFormat`, `ListFormat`, `Collator`). Building any of these by string concatenation, or hand-rolling pluralization instead of using i18next plural forms, is a defect.
-65. English is the only shipped locale. The infrastructure carries no locale-specific branching, so adding a locale is adding a catalogue.
+65. English is the only shipped *product* locale - no in-app control ever selects a different one, and a real visitor's browser always resolves to it (`detectLocale` falls back to English whenever nothing in `navigator.languages` matches a supported locale). The infrastructure carries no product-facing locale-specific branching, so adding a second product locale is still adding a catalogue. A second locale, `test` (tag `'zxx'`), ships in the same build for unit and e2e tests only (see ARCHITECTURE.md's decision log): it is a generated, key-echoing catalogue reachable solely through `buildTestRuntime`'s default and Playwright's `chromium` project `locale` context option, never through a real browser or any UI control.
 66. The document `lang` attribute reflects the active locale, and `dir` is derived from the locale rather than hardcoded on the element. The derivation is an explicit locale-to-direction map in the i18n module, not a runtime `Intl` API: `Intl.Locale.prototype.getTextInfo` is declared only in TypeScript's `esnext.intl` library and does not typecheck under this repository's `ES2023` + `DOM` libs, and widening the lib to pull in a stage-3 API for a single-locale app is a worse trade than four lines of map.
 67. Layout uses logical properties throughout. No physical-direction spacing or alignment (`margin-left`, `padding-right`, `text-align: left`, and their utility equivalents) appears in shipped styles; this is grep- and review-checkable.
 68. Switching the document direction to RTL mirrors the placeholder pages correctly with no other code change, so an RTL locale needs a catalogue and a `dir` switch and nothing else.
@@ -221,7 +221,7 @@ Deliberately not built at all (ARCHITECTURE.md section 7):
 - Real authentication. Impossible against this API from a static client; `features/auth` isolates the lookup so a BFF-issued cookie replaces one module.
 - Offline support and service workers. A 9KB public payload does not justify a cache lifecycle; the repository layer is where a persistent cache would attach.
 - Search, filtering and org editing. Outside GOAL.md.
-- A second locale, SSR and a component library. The infrastructure supports each; none is needed.
+- A second *product* locale, SSR and a component library. The infrastructure supports each; none is needed. (A test-only locale ships separately - see ARCHITECTURE.md's decision log and invariant 65.)
 - Vendor telemetry. The facade defines the contract; a real sink is a sink swap.
 
 Also out of scope for this phase specifically:
diff --git a/src/app/ApplicationRoot.test.tsx b/src/app/ApplicationRoot.test.tsx
index 8219890..58d82b9 100644
--- a/src/app/ApplicationRoot.test.tsx
+++ b/src/app/ApplicationRoot.test.tsx
@@ -1,6 +1,7 @@
 import { describe, expect, it } from 'vitest';
 import { screen } from '@testing-library/react';
 import { useTranslation } from 'react-i18next';
+import { Locale } from '@platform/internationalization';
 import { renderRoute } from './testing/renderRoute';
 import { useRuntime } from './composition/useRuntime';
 
@@ -26,7 +27,7 @@ describe('ApplicationRoot', () => {
   it('sets document.documentElement.lang and dir from the active i18next language', async () => {
     await renderRoute(<ProbeRoute />);
 
-    expect(document.documentElement.lang).toBe('en');
+    expect(document.documentElement.lang).toBe(Locale.Test);
     expect(document.documentElement.dir).toBe('ltr');
   });
 });
diff --git a/src/app/composition/createRuntime.test.ts b/src/app/composition/createRuntime.test.ts
index 4c96f66..7e12153 100644
--- a/src/app/composition/createRuntime.test.ts
+++ b/src/app/composition/createRuntime.test.ts
@@ -1,5 +1,6 @@
-import { afterEach, describe, expect, it } from 'vitest';
+import { afterEach, describe, expect, it, vi } from 'vitest';
 import type { Configuration } from '@platform/configuration';
+import { Locale } from '@platform/internationalization';
 import { createRuntime } from './createRuntime';
 
 const TEST_CONFIGURATION: Configuration = Object.freeze({
@@ -14,6 +15,7 @@ const TEST_CONFIGURATION: Configuration = Object.freeze({
 
 afterEach(() => {
   globalThis.__hierarchyTreeTelemetry = undefined;
+  vi.unstubAllGlobals();
 });
 
 describe('createRuntime', () => {
@@ -43,6 +45,24 @@ describe('createRuntime', () => {
     expect(globalThis.__hierarchyTreeTelemetry).toBeUndefined();
   });
 
+  it('resolves the runtime language from navigator.languages', async () => {
+    vi.stubGlobal('navigator', { ...navigator, languages: ['fr-FR', 'zxx'] });
+
+    const runtime = await createRuntime(TEST_CONFIGURATION);
+
+    expect(runtime.i18n.language).toBe(Locale.Test);
+  });
+
+  it('registers the key-echoed common catalogue when the resolved language is Locale.Test', async () => {
+    vi.stubGlobal('navigator', { ...navigator, languages: [Locale.Test] });
+
+    const runtime = await createRuntime(TEST_CONFIGURATION);
+
+    expect(runtime.i18n.t('notFound.title', { ns: 'common' })).toBe(
+      'notFound.title',
+    );
+  });
+
   it('an http request made with no active interaction resolves rather than throwing for want of a correlation id', async () => {
     const runtime = await createRuntime(TEST_CONFIGURATION);
 
diff --git a/src/app/composition/createRuntime.ts b/src/app/composition/createRuntime.ts
index 25f0809..fa4a80b 100644
--- a/src/app/composition/createRuntime.ts
+++ b/src/app/composition/createRuntime.ts
@@ -10,7 +10,12 @@ import {
   createHttpClient,
   type HttpClient,
 } from '@platform/http';
-import { createInternationalization } from '@platform/internationalization';
+import {
+  createInternationalization,
+  createKeyEchoCatalogue,
+  detectLocale,
+  Locale,
+} from '@platform/internationalization';
 import { createSystemClock, createSystemRandomness } from '@platform/runtime';
 import { createInteractionTracker, type InteractionTracker } from '../routing';
 import commonCatalogue from '../locales/en/common.json';
@@ -23,8 +28,6 @@ export type Runtime = Readonly<{
   interactionTracker: InteractionTracker;
 }>;
 
-const RUNTIME_LANGUAGE = 'en';
-
 export async function createRuntime(
   configuration: Configuration,
 ): Promise<Runtime> {
@@ -53,9 +56,15 @@ export async function createRuntime(
       createCorrelationId(randomness),
   });
 
+  const language = detectLocale(navigator.languages);
   const i18n = await createInternationalization({
-    resources: { common: commonCatalogue },
-    language: RUNTIME_LANGUAGE,
+    resources: {
+      common:
+        language === Locale.Test
+          ? createKeyEchoCatalogue(commonCatalogue)
+          : commonCatalogue,
+    },
+    language,
     observability,
   });
 
diff --git a/src/app/error-boundary/ErrorSurface.test.tsx b/src/app/error-boundary/ErrorSurface.test.tsx
index e70a536..025f49c 100644
--- a/src/app/error-boundary/ErrorSurface.test.tsx
+++ b/src/app/error-boundary/ErrorSurface.test.tsx
@@ -10,10 +10,8 @@ describe('ErrorSurface', () => {
       <ErrorSurface correlationId="abc123" onRecover={vi.fn()} />,
     );
 
-    expect(screen.getByText('Something went wrong')).toBeInTheDocument();
-    expect(
-      screen.getByText('An unexpected error occurred.'),
-    ).toBeInTheDocument();
+    expect(screen.getByText('errorSurface.title')).toBeInTheDocument();
+    expect(screen.getByText('errorSurface.message')).toBeInTheDocument();
     expect(screen.getByText('abc123')).toBeInTheDocument();
   });
 
@@ -23,7 +21,9 @@ describe('ErrorSurface', () => {
       <ErrorSurface correlationId="abc123" onRecover={onRecover} />,
     );
 
-    await userEvent.click(screen.getByRole('button', { name: 'Try again' }));
+    await userEvent.click(
+      screen.getByRole('button', { name: 'errorSurface.retry' }),
+    );
 
     expect(onRecover).toHaveBeenCalledOnce();
   });
diff --git a/src/app/error-boundary/RootErrorBoundary.test.tsx b/src/app/error-boundary/RootErrorBoundary.test.tsx
index f403786..f0c358d 100644
--- a/src/app/error-boundary/RootErrorBoundary.test.tsx
+++ b/src/app/error-boundary/RootErrorBoundary.test.tsx
@@ -11,7 +11,7 @@ describe('RootErrorBoundary', () => {
   it('the root boundary renders the error surface above any router without throwing', async () => {
     await expect(renderRoute(<Bomb />)).resolves.toBeDefined();
 
-    expect(screen.getByText('Something went wrong')).toBeInTheDocument();
+    expect(screen.getByText('errorSurface.title')).toBeInTheDocument();
   });
 
   it('the boundary reports the error exactly once and displays the correlation id', async () => {
diff --git a/src/app/error-boundary/RouteErrorBoundary.test.tsx b/src/app/error-boundary/RouteErrorBoundary.test.tsx
index d95d980..f3ca988 100644
--- a/src/app/error-boundary/RouteErrorBoundary.test.tsx
+++ b/src/app/error-boundary/RouteErrorBoundary.test.tsx
@@ -16,14 +16,14 @@ describe('RouteErrorBoundary', () => {
 
     await renderRoute(<RouterProvider router={router} />);
 
-    expect(screen.getByText('Something went wrong')).toBeInTheDocument();
+    expect(screen.getByText('errorSurface.title')).toBeInTheDocument();
     // Keyboard recovery (invariant 93) and that the retry actually
     // re-runs the route rather than reloading are e2e-only checks
     // (TECH.md 5.4) - jsdom has no real navigation to observe, and
     // React's own error-retry behavior makes a unit-level render count
     // an unreliable way to prove it.
     expect(
-      screen.getByRole('button', { name: 'Try again' }),
+      screen.getByRole('button', { name: 'errorSurface.retry' }),
     ).toBeInTheDocument();
   });
 });
diff --git a/src/app/layout/ApplicationLayout.test.tsx b/src/app/layout/ApplicationLayout.test.tsx
index ed4c19f..cb6cba0 100644
--- a/src/app/layout/ApplicationLayout.test.tsx
+++ b/src/app/layout/ApplicationLayout.test.tsx
@@ -29,7 +29,7 @@ describe('ApplicationLayout', () => {
   it('renders a skip link pointing at the main landmark', async () => {
     await renderLayout();
 
-    const skipLink = screen.getByRole('link', { name: 'Skip to main content' });
+    const skipLink = screen.getByRole('link', { name: 'layout.skipLink' });
     const main = screen.getByRole('main');
 
     expect(skipLink).toHaveAttribute('href', `#${main.id}`);
diff --git a/src/app/routing/routes/NotFoundRoute.test.tsx b/src/app/routing/routes/NotFoundRoute.test.tsx
index 54064be..a4af605 100644
--- a/src/app/routing/routes/NotFoundRoute.test.tsx
+++ b/src/app/routing/routes/NotFoundRoute.test.tsx
@@ -12,7 +12,7 @@ describe('NotFoundRoute', () => {
       </MemoryRouter>,
     );
 
-    const link = screen.getByRole('link', { name: 'Back to home' });
+    const link = screen.getByRole('link', { name: 'notFound.linkHome' });
     expect(link).toHaveAttribute('href', '/');
   });
 });
diff --git a/src/app/testing/renderRoute.tsx b/src/app/testing/renderRoute.tsx
index cbd3f09..e55ea59 100644
--- a/src/app/testing/renderRoute.tsx
+++ b/src/app/testing/renderRoute.tsx
@@ -2,7 +2,11 @@ import type { ReactNode } from 'react';
 import { render, type RenderResult } from '@testing-library/react';
 import type { Configuration } from '@platform/configuration';
 import { createObservability } from '@platform/observability';
-import { createInternationalization } from '@platform/internationalization';
+import {
+  createInternationalization,
+  createKeyEchoCatalogue,
+  Locale,
+} from '@platform/internationalization';
 import { createHttpClient } from '@platform/http';
 import {
   createFakeClock,
@@ -52,8 +56,8 @@ export async function buildTestRuntime(
       interactionTracker.currentCorrelationId() ?? 'test-correlation-id',
   });
   const i18n = await createInternationalization({
-    resources: { common: commonCatalogue },
-    language: 'en',
+    resources: { common: createKeyEchoCatalogue(commonCatalogue) },
+    language: Locale.Test,
     observability,
   });
 
diff --git a/src/features/auth/loadTranslations.test.ts b/src/features/auth/loadTranslations.test.ts
index 3b97e2c..cc07889 100644
--- a/src/features/auth/loadTranslations.test.ts
+++ b/src/features/auth/loadTranslations.test.ts
@@ -1,10 +1,14 @@
 import { beforeEach, describe, expect, it, vi } from 'vitest';
-import { createInternationalization } from '@platform/internationalization';
+import {
+  createInternationalization,
+  Locale,
+} from '@platform/internationalization';
+import authCatalogue from './locales/en/auth.json';
 
-async function createTestI18n() {
+async function createTestI18n(language: string = Locale.English) {
   return createInternationalization({
     resources: { common: {} },
-    language: 'en',
+    language,
     observability: { logger: { error: vi.fn() } },
   });
 }
@@ -33,4 +37,38 @@ describe('auth loadTranslations', () => {
 
     expect(addResourceBundleSpy).toHaveBeenCalledTimes(1);
   });
+
+  it('registers the key-echoed catalogue under Locale.Test', async () => {
+    const { loadTranslations } = await import('./loadTranslations');
+    const englishSnapshot = JSON.parse(
+      JSON.stringify(authCatalogue),
+    ) as unknown;
+
+    const englishInstance = await createTestI18n(Locale.English);
+    await loadTranslations(englishInstance);
+    expect(englishInstance.getResourceBundle(Locale.English, 'auth')).toEqual(
+      authCatalogue,
+    );
+
+    const testInstance = await createTestI18n(Locale.Test);
+    await loadTranslations(testInstance);
+    expect(testInstance.getResourceBundle(Locale.Test, 'auth')).toEqual({
+      login: {
+        documentTitle: 'login.documentTitle',
+        title: 'login.title',
+        message: 'login.message',
+      },
+    });
+
+    const secondEnglishInstance = await createTestI18n(Locale.English);
+    await loadTranslations(secondEnglishInstance);
+    expect(
+      secondEnglishInstance.getResourceBundle(Locale.English, 'auth'),
+    ).toEqual(authCatalogue);
+
+    // G1/Codex-2: the source catalogue module is shared and cached across
+    // every dynamic import above - none of the three registrations may
+    // mutate it.
+    expect(authCatalogue).toEqual(englishSnapshot);
+  });
 });
diff --git a/src/features/auth/loadTranslations.ts b/src/features/auth/loadTranslations.ts
index 1e4d242..934af97 100644
--- a/src/features/auth/loadTranslations.ts
+++ b/src/features/auth/loadTranslations.ts
@@ -1,4 +1,5 @@
 import type { i18n } from 'i18next';
+import { createKeyEchoCatalogue, Locale } from '@platform/internationalization';
 
 // Keyed by instance rather than a single module-level promise: two
 // navigations to /login on the SAME instance register the auth namespace
@@ -19,5 +20,9 @@ export function loadTranslations(instance: i18n): Promise<void> {
 
 async function registerCatalogue(instance: i18n): Promise<void> {
   const catalogue = await import('./locales/en/auth.json');
-  instance.addResourceBundle('en', 'auth', catalogue.default);
+  const resource =
+    instance.language === Locale.Test
+      ? createKeyEchoCatalogue(catalogue.default)
+      : catalogue.default;
+  instance.addResourceBundle(instance.language, 'auth', resource);
 }
diff --git a/src/features/hierarchy/loadTranslations.test.ts b/src/features/hierarchy/loadTranslations.test.ts
index f1152b8..cf06eb1 100644
--- a/src/features/hierarchy/loadTranslations.test.ts
+++ b/src/features/hierarchy/loadTranslations.test.ts
@@ -1,10 +1,14 @@
 import { beforeEach, describe, expect, it, vi } from 'vitest';
-import { createInternationalization } from '@platform/internationalization';
+import {
+  createInternationalization,
+  Locale,
+} from '@platform/internationalization';
+import hierarchyCatalogue from './locales/en/hierarchy.json';
 
-async function createTestI18n() {
+async function createTestI18n(language: string = Locale.English) {
   return createInternationalization({
     resources: { common: {} },
-    language: 'en',
+    language,
     observability: { logger: { error: vi.fn() } },
   });
 }
@@ -33,4 +37,38 @@ describe('hierarchy loadTranslations', () => {
 
     expect(addResourceBundleSpy).toHaveBeenCalledTimes(1);
   });
+
+  it('registers the key-echoed catalogue under Locale.Test', async () => {
+    const { loadTranslations } = await import('./loadTranslations');
+    const englishSnapshot = JSON.parse(
+      JSON.stringify(hierarchyCatalogue),
+    ) as unknown;
+
+    const englishInstance = await createTestI18n(Locale.English);
+    await loadTranslations(englishInstance);
+    expect(
+      englishInstance.getResourceBundle(Locale.English, 'hierarchy'),
+    ).toEqual(hierarchyCatalogue);
+
+    const testInstance = await createTestI18n(Locale.Test);
+    await loadTranslations(testInstance);
+    expect(testInstance.getResourceBundle(Locale.Test, 'hierarchy')).toEqual({
+      home: {
+        documentTitle: 'home.documentTitle',
+        title: 'home.title',
+        message: 'home.message',
+      },
+    });
+
+    const secondEnglishInstance = await createTestI18n(Locale.English);
+    await loadTranslations(secondEnglishInstance);
+    expect(
+      secondEnglishInstance.getResourceBundle(Locale.English, 'hierarchy'),
+    ).toEqual(hierarchyCatalogue);
+
+    // G1/Codex-2: the source catalogue module is shared and cached across
+    // every dynamic import above - none of the three registrations may
+    // mutate it.
+    expect(hierarchyCatalogue).toEqual(englishSnapshot);
+  });
 });
diff --git a/src/features/hierarchy/loadTranslations.ts b/src/features/hierarchy/loadTranslations.ts
index 69eb151..dc852dc 100644
--- a/src/features/hierarchy/loadTranslations.ts
+++ b/src/features/hierarchy/loadTranslations.ts
@@ -1,4 +1,5 @@
 import type { i18n } from 'i18next';
+import { createKeyEchoCatalogue, Locale } from '@platform/internationalization';
 
 // Keyed by instance rather than a single module-level promise: two
 // navigations to / on the SAME instance register the hierarchy namespace
@@ -19,5 +20,9 @@ export function loadTranslations(instance: i18n): Promise<void> {
 
 async function registerCatalogue(instance: i18n): Promise<void> {
   const catalogue = await import('./locales/en/hierarchy.json');
-  instance.addResourceBundle('en', 'hierarchy', catalogue.default);
+  const resource =
+    instance.language === Locale.Test
+      ? createKeyEchoCatalogue(catalogue.default)
+      : catalogue.default;
+  instance.addResourceBundle(instance.language, 'hierarchy', resource);
 }
diff --git a/src/platform/internationalization/createKeyEchoCatalogue.test.ts b/src/platform/internationalization/createKeyEchoCatalogue.test.ts
new file mode 100644
index 0000000..ac1825f
--- /dev/null
+++ b/src/platform/internationalization/createKeyEchoCatalogue.test.ts
@@ -0,0 +1,74 @@
+import { describe, expect, it } from 'vitest';
+import { createKeyEchoCatalogue } from './createKeyEchoCatalogue';
+
+function isRecord(value: unknown): value is Record<string, unknown> {
+  return typeof value === 'object' && value !== null;
+}
+
+function collectLeafPaths(
+  value: unknown,
+  path: readonly string[] = [],
+): string[] {
+  if (typeof value === 'string') {
+    return [path.join('.')];
+  }
+  if (isRecord(value)) {
+    return Object.entries(value).flatMap(([key, nested]) =>
+      collectLeafPaths(nested, [...path, key]),
+    );
+  }
+  return [];
+}
+
+function leafValueAt(catalogue: unknown, dotPath: string): unknown {
+  return dotPath
+    .split('.')
+    .reduce<unknown>(
+      (node, key) => (isRecord(node) ? node[key] : undefined),
+      catalogue,
+    );
+}
+
+// Shape mirrors the real catalogues (src/app/locales/en/common.json etc.) -
+// multiple top-level namespaces, multiple sibling leaves per namespace,
+// one two-level-deep branch - without importing them: this platform-layer
+// module cannot import app/feature catalogues (boundaries/dependencies
+// only allows platform -> platform (+ shared in tests)), and the generator
+// is meant to be catalogue-shape-agnostic regardless. Real-catalogue
+// coverage comes from the integration path (loadTranslations.ts,
+// buildTestRuntime) plus the suite-wide missingKeyReports assertion.
+const FIXTURE_CATALOGUE = {
+  layout: {
+    skipLink: 'Skip to main content',
+  },
+  notFound: {
+    title: 'Page not found',
+    linkHome: 'Back to home',
+  },
+  errorSurface: {
+    title: 'Something went wrong',
+    message: 'An unexpected error occurred.',
+    retry: 'Try again',
+  },
+};
+
+describe('createKeyEchoCatalogue', () => {
+  it('replaces every leaf with its own dot-path key', () => {
+    const echoed = createKeyEchoCatalogue(FIXTURE_CATALOGUE);
+    const paths = collectLeafPaths(FIXTURE_CATALOGUE);
+
+    expect(paths.length).toBeGreaterThan(0);
+    for (const path of paths) {
+      expect(leafValueAt(echoed, path)).toBe(path);
+    }
+    expect(collectLeafPaths(echoed).sort()).toEqual(paths.sort());
+  });
+
+  it('never mutates the source catalogue', () => {
+    const snapshot = JSON.parse(JSON.stringify(FIXTURE_CATALOGUE)) as unknown;
+
+    createKeyEchoCatalogue(FIXTURE_CATALOGUE);
+
+    expect(FIXTURE_CATALOGUE).toEqual(snapshot);
+  });
+});
diff --git a/src/platform/internationalization/createKeyEchoCatalogue.ts b/src/platform/internationalization/createKeyEchoCatalogue.ts
new file mode 100644
index 0000000..e173129
--- /dev/null
+++ b/src/platform/internationalization/createKeyEchoCatalogue.ts
@@ -0,0 +1,17 @@
+type Catalogue = { readonly [key: string]: Catalogue | string };
+
+export function createKeyEchoCatalogue(catalogue: Catalogue): Catalogue {
+  return echoLeaves(catalogue, []);
+}
+
+function echoLeaves(node: Catalogue, path: readonly string[]): Catalogue {
+  const echoed: Record<string, Catalogue | string> = {};
+  for (const [key, value] of Object.entries(node)) {
+    const leafPath = [...path, key];
+    echoed[key] =
+      typeof value === 'string'
+        ? leafPath.join('.')
+        : echoLeaves(value, leafPath);
+  }
+  return echoed;
+}
diff --git a/src/platform/internationalization/detectLocale.test.ts b/src/platform/internationalization/detectLocale.test.ts
new file mode 100644
index 0000000..735433a
--- /dev/null
+++ b/src/platform/internationalization/detectLocale.test.ts
@@ -0,0 +1,25 @@
+import { describe, expect, it } from 'vitest';
+import { detectLocale } from './detectLocale';
+import { Locale } from './locale';
+
+describe('detectLocale', () => {
+  it('matches a region-qualified candidate to its base subtag', () => {
+    expect(detectLocale(['en-US'])).toBe(Locale.English);
+    expect(detectLocale(['EN-GB'])).toBe(Locale.English);
+  });
+
+  it('falls back to English when no candidate matches', () => {
+    expect(detectLocale([])).toBe(Locale.English);
+    expect(detectLocale(['fr-FR', 'de-DE'])).toBe(Locale.English);
+    expect(detectLocale([''])).toBe(Locale.English);
+    expect(detectLocale(['-zxx'])).toBe(Locale.English);
+  });
+
+  it('respects preference order over match order', () => {
+    expect(detectLocale(['fr-FR', 'zxx', 'en'])).toBe(Locale.Test);
+  });
+
+  it('matches an exact-subtag candidate with no region', () => {
+    expect(detectLocale(['zxx'])).toBe(Locale.Test);
+  });
+});
diff --git a/src/platform/internationalization/detectLocale.ts b/src/platform/internationalization/detectLocale.ts
new file mode 100644
index 0000000..efbd1c9
--- /dev/null
+++ b/src/platform/internationalization/detectLocale.ts
@@ -0,0 +1,10 @@
+import { Locale } from './locale';
+
+export function detectLocale(candidates: readonly string[]): Locale {
+  for (const candidate of candidates) {
+    const subtag = candidate.split('-')[0]?.toLowerCase();
+    const match = Object.values(Locale).find((locale) => locale === subtag);
+    if (match !== undefined) return match;
+  }
+  return Locale.English;
+}
diff --git a/src/platform/internationalization/index.ts b/src/platform/internationalization/index.ts
index ba7fa9b..74f1405 100644
--- a/src/platform/internationalization/index.ts
+++ b/src/platform/internationalization/index.ts
@@ -1,6 +1,9 @@
 export { createInternationalization } from './createInternationalization';
 export type { InternationalizationDependencies } from './createInternationalization';
 export { localeDirection } from './localeDirection';
+export { Locale } from './locale';
+export { detectLocale } from './detectLocale';
+export { createKeyEchoCatalogue } from './createKeyEchoCatalogue';
 export { formatMissingKey } from './formatMissingKey';
 export { createMissingKeyHandler, missingKeyReports } from './reportMissingKey';
 export type { MissingKeyEntry } from './reportMissingKey';
diff --git a/src/platform/internationalization/locale.test.ts b/src/platform/internationalization/locale.test.ts
new file mode 100644
index 0000000..317862b
--- /dev/null
+++ b/src/platform/internationalization/locale.test.ts
@@ -0,0 +1,13 @@
+import { describe, expect, it } from 'vitest';
+import { Locale } from './locale';
+
+describe('Locale', () => {
+  it('exposes English and Test members with the correct string values', () => {
+    expect(Locale.English).toBe('en');
+    expect(Locale.Test).toBe('zxx');
+  });
+
+  it('exposes exactly two members', () => {
+    expect(Object.keys(Locale).sort()).toEqual(['English', 'Test']);
+  });
+});
diff --git a/src/platform/internationalization/locale.ts b/src/platform/internationalization/locale.ts
new file mode 100644
index 0000000..956b38c
--- /dev/null
+++ b/src/platform/internationalization/locale.ts
@@ -0,0 +1,6 @@
+export const Locale = {
+  English: 'en',
+  Test: 'zxx',
+} as const;
+
+export type Locale = (typeof Locale)[keyof typeof Locale];
