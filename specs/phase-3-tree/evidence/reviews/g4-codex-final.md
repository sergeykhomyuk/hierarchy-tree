Verdict: 6 findings (5 blocking)

1. Photo failure state survives a refreshed payload, so Retry neither retries the image nor reports a fresh failure. | `src/shared/ui/Avatar.tsx:29-30`; `src/features/hierarchy/HierarchyTree.tsx:222-225`; `src/features/hierarchy/HierarchyTree.test.tsx:304-357` | React Suspense preserves the keyed row and its `imageFailed` state across revalidation; the test masks this by explicitly unmounting, violating invariant 97 and leaving a subsequently fixed photo stuck on initials. | BLOCKING

2. The real URL-backed expansion integration changes every row's `onKeyDown` callback on each toggle, defeating `TreeRow` memoization. | `src/features/hierarchy/useExpansion.ts:58-69`; `src/features/hierarchy/HierarchyTree.tsx:187-212,222-245`; `src/features/hierarchy/HierarchyTree.test.tsx:36-68,800-829` | `expandMany` changes with `expandedIds`, cascading through `handleExpandSiblings` and `useTreeKeyboard`; the render-count test substitutes an empty-dependency callback and therefore misses the invariant-91 failure. | BLOCKING

3. Validation reporting discards each malformed element's position, and an all-invalid payload reports no field-level validation details. | `src/features/hierarchy/data/parsePeople.ts:38-64`; `src/features/hierarchy/data/fetchPeople.ts:98-128`; `src/features/hierarchy/data/personSchema.test.ts:66-97` | On-call receives only deduplicated field names for partial drops and only `allRowsInvalid` otherwise; the named test constructs positions locally instead of exercising production reporting, so invariant 53 is unproven and unsatisfied. | BLOCKING

4. Skipped `expanded` segments are counted by the parser and then silently discarded. | `src/features/hierarchy/domain/expansionParameter.ts:31-68`; `src/features/hierarchy/useExpansion.ts:19-24`; `specs/phase-3-tree/TECH.md:102-118` | A stale link such as `?expanded=999,abc` produces no telemetry or log report despite invariant 121; parser unit tests assert only the unused return value. | BLOCKING

5. Default expansion can serialize a childless root into the manager-only `expanded` parameter. | `src/features/hierarchy/domain/defaultExpansion.ts:8-13`; `src/features/hierarchy/useExpansion.ts:37-48`; `src/features/hierarchy/domain/defaultExpansion.test.ts:7-19` | With one manager root and one leaf root, collapsing the manager writes the leaf ID to the URL; this violates invariant 116, and existing hook tests use no childless-root fixture. | BLOCKING

6. The branch changes phase 2's header presentation despite invariants 3, 187, and 190 declaring it unchanged and excluding it from the shared-surface list. | `src/app/layout/SignedInHeader.tsx:44`; `specs/phase-3-tree/PRODUCT.md:52-56,607-632` | `main..HEAD` replaces the header's intrinsic padding with fixed 60px geometry, new padding, and a background, affecting every authenticated page; rerunning phase 2's behavioral tests does not discriminate this visual regression. | non-blocking
