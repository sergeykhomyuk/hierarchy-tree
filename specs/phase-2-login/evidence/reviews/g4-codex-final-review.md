3 findings (2 blocking).

1. The approved `/users.json` fix was not propagated through the binding acceptance contract or test plan | [PRODUCT.md](/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/specs/phase-2-login/PRODUCT.md:195), [TECH.md](/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/specs/phase-2-login/TECH.md:653), `loop.json plan_steps[4]`, [ARCHITECTURE.md](/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/specs/ARCHITECTURE.md:190) | The implementation correctly follows the latest architecture decision, but PRODUCT invariants 97/97d still prohibit the collection request, TECH still specifies `/users/{id}.json`, and step 5 names a deleted `userResourcePath.test.ts`; the phase therefore cannot truthfully claim that its numbered acceptance criteria and invariant-to-test map pass | BLOCKING
2. The structural guard test cannot detect a loader returned by a lazy authenticated route | [routeDefinitions.test.ts](/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/app/routing/routeDefinitions.test.ts:55), [routeDefinitions.ts](/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/src/app/routing/routeDefinitions.ts:33) | Adding `loader: () => fetch('/users.json')` to the existing child's `lazy()` result leaves `child.loader` undefined during this test, so it stays green even though React Router would run that loader in parallel with the parent guard and expose exactly the unauthenticated data-fetch race invariants 85/146 intended to prevent | BLOCKING
3. The live-smoke suite writes the real resolved user/session identifier to retained console output | [live-smoke.test.ts](/Users/sergeykhomyuk/System/Areas/Dev/github/hierarchy-tree/scripts/live-smoke/live-smoke.test.ts:151) | The login leak assertions inspect only email, password and derived secret, so they do not catch this identifier; it is a public-database identifier rather than an authentication token, but logging it is unnecessary and falls outside the otherwise identifier-free telemetry posture | non-blocking

1. Yes. `normalizeToCodeUnits` reproduces the legacy sequence exactly: repeat by UTF-16 length, truncate with `substring`, then iterate by code point using `Array.from(...charCodeAt(0))`. Consequently, intact astral characters contribute only their high surrogate and shorten the array; `Number(undefined) ^ x` correctly preserves the tail defect. The table matches the extracted literal and recorded checksum. The tests are reasonably discriminating because the reference algorithm, document-extracted table comparison, checksum, Unicode case and live ASCII account proof cover different failure modes.

2. Yes for the current phase. The authenticated parent loader calls `requireSession` before starting the name request, and React Router settles loaders before constructing authenticated components. Direct loads, reloads and ordinary Back/Forward navigation therefore redirect without authenticated content or user requests. Signed-in visitors are similarly redirected before the login component renders. The accepted bfcache interval remains, and finding 2 means the promised protection is not mechanically preserved once a lazy child loader is added.

3. I found no unintended production leak of email or password into URLs, logs, telemetry or storage. The derived secret necessarily appears in the single Firebase request URL; timing, cancellation and invalid-path records pass through centralized path-segment redaction. The session user id intentionally appears in `sessionStorage`. The uncovered identifier leak is the live-smoke console output in finding 3. The focused review suite passed 49/49 tests across 13 files; no source files were modified.

